---
phase: 02-stability
plan: 05
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/useForge/useForge.tsx
  - src/Forger/Forger.tsx
autonomous: true
requirements: [STAB-05, STAB-03]
must_haves:
  truths:
    - "useForge augments control by mutating the RHF instance in place (Object.assign) and returns that SAME instance — stable identity, prototype preserved"
    - "useForge returns control typed as ForgeControl<T> with no `as any` on the public RETURN path (the binding STAB-05 check is the returned `control: ForgeControl<T>`, not the internal input spread)"
    - "The internal `...(props as any)` input spread at :30 is either narrowed OR retained-with-classification: if retained, the SUMMARY explicitly classifies it as internal-input (not public surface) so it does not count against the STAB-05 public-return gate"
    - "Wizard re-render still flows from currentStep React state, not control identity"
    - "Forger.tsx uses local deepEqual instead of lodash isEqual; the memo comparator behavior is preserved"
    - "Forger's `const Component = component as any` becomes `const Component = component` now that component is React.ElementType"
  artifacts:
    - path: "src/useForge/useForge.tsx"
      provides: "In-place control augmentation (D-11), stable control reference, typed public return"
      contains: "Object.assign"
    - path: "src/Forger/Forger.tsx"
      provides: "deepEqual memo comparator; typed component (no as any)"
      contains: "deepEqual"
  key_links:
    - from: "src/useForge/useForge.tsx"
      to: "Forge.tsx / Forger.tsx consumers of control.hasFields/fields/handleWizardSubmit"
      via: "in-place Object.assign keeps same property names on the real instance"
      pattern: "Object.assign\\(methods.control"
---

<objective>
Switch `useForge`'s control augmentation from the buggy spread (`{ ...methods.control }`) to in-place `Object.assign` (D-11), producing a single stable `control` instance typed as `ForgeControl<T>` with no `as any` on the public return path. Concurrently swap `Forger.tsx`'s lodash `isEqual` for the local `deepEqual` (D-13) and drop its `component as any` now that `ForgerControllerProps.component` is `React.ElementType` (from Plan 01).

Purpose: STAB-05 (typed stable control, fixes the unstable-control identity that misfires downstream effects) + STAB-03 (last two lodash/`as any` sites in Forger).
Output: A stable in-place-augmented `control` typed `ForgeControl<T>` on the public return, and a lodash-free, `as any`-free `Forger.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stability/02-CONTEXT.md
@.planning/phases/02-stability/02-RESEARCH.md
@.planning/phases/02-stability/02-PATTERNS.md
@.planning/phases/02-stability/02-01-SUMMARY.md
@.planning/phases/02-stability/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: In-place control augmentation in useForge (D-11)</name>
  <files>src/useForge/useForge.tsx</files>
  <read_first>
    - src/useForge/useForge.tsx (full file: dead `"use strict"` at :1, `...(props as any)` at :30, the `wizardProps` object at :60-69, and the buggy spread return at :71-79)
    - .planning/phases/02-stability/02-PATTERNS.md (section "src/useForge/useForge.tsx (hook, control-augmentation D-11)" — anti-pattern removed + Replacement pattern + the discretionary cleanups)
    - .planning/phases/02-stability/02-RESEARCH.md (sections "Pattern 4: In-place control augmentation (D-11)", "D-11 in-place augmentation safety", and the "Anti-Patterns to Avoid" entry on spreading control)
    - .planning/phases/02-stability/02-01-SUMMARY.md (confirms ForgeControl<T> shape after the type sweep)
  </read_first>
  <action>
    In src/useForge/useForge.tsx:
    - Replace the return spread at :71-79. Build `const forgeProps = { hasFields, fields, ...wizardProps };`, then `Object.assign(methods.control, forgeProps);` to mutate the SAME RHF instance in place (prototype + non-enumerable internals preserved, stable identity across renders — fixes the D-07 unstable-effect dep). Return `{ ...methods, control: methods.control as ForgeControl<TFieldValues, TFieldProps> };`. Do NOT spread `methods.control` (the current bug: drops RHF prototype, new identity every render). This returned `control: ForgeControl<T>` IS the binding STAB-05 public-return surface and MUST be `as any`-free.
    - Keep `wizardProps` (:60-69) the SAME shape; only the attach mechanism changes. The `currentStep` useState (:37) STAYS — wizard re-render flows from React state, not control identity.
    - Discretionary cleanups: delete the dead `"use strict";` at :1. The `...(props as any)` at :30 is an INTERNAL INPUT spread (the args passed into `useForm`), NOT the public return surface — STAB-05's gate is the typed RETURN, not this input. Narrow it to a precise type only if doing so does not cascade type errors; if you retain it, that is acceptable, but the SUMMARY MUST explicitly classify it as "internal input spread, not public surface" so it is not mistaken for an unresolved STAB-05 violation.
  </action>
  <verify>
    <automated>npx tsc --noEmit (exits 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Object.assign(methods.control" src/useForge/useForge.tsx` returns 1 match
    - `grep -n "\.\.\.methods.control" src/useForge/useForge.tsx` returns 0 matches (no control spread)
    - `grep -n "use strict" src/useForge/useForge.tsx` returns 0 matches
    - The public RETURN is `control: ... as ForgeControl<...>` with NO `as any` — `grep -n "control:.*as any" src/useForge/useForge.tsx` returns 0 matches (the binding STAB-05 check is the typed return, not the `...(props as any)` input spread)
    - If `...(props as any)` at :30 is retained, the SUMMARY classifies it as internal-input (not public surface); if narrowed, no `as any` remains on line 30
    - `npx tsc --noEmit` exits 0
    - Manual (STAB-05 / D-07): logging `control` reference equality across renders shows a STABLE identity; wizard navigation still re-renders on step change
  </acceptance_criteria>
  <done>control is augmented in place via Object.assign, the public return is typed ForgeControl<T> with no `as any`, stable identity; the internal `...(props as any)` input spread is narrowed or retained-and-classified-in-SUMMARY; wizard still works; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Forger.tsx lodash isEqual -> deepEqual + typed component (D-13/D-10)</name>
  <files>src/Forger/Forger.tsx</files>
  <read_first>
    - src/Forger/Forger.tsx (the `import { isEqual } from "lodash";` at :3, the public-delegation idiom at :27-30, the `const Component = component as any;` at :31, and the memo comparator `if (!isEqual(rest, others))` at :105)
    - .planning/phases/02-stability/02-PATTERNS.md (section "src/Forger/Forger.tsx (component, lodash isEqual swap D-13)" — analog is utils.ts default-export deepEqual; the single call site)
    - .planning/phases/02-stability/02-RESEARCH.md (lodash table row for `isEqual` — deepEqual is cycle-safe, ref-key-skipping, NaN/RegExp edge cases acceptable for a memo bail-out)
    - .planning/phases/02-stability/02-01-SUMMARY.md (confirms ForgerControllerProps.component is now React.ElementType)
  </read_first>
  <action>
    In src/Forger/Forger.tsx:
    - Remove `import { isEqual } from "lodash";` (:3). Import the default-export `deepEqual` from `../utils` (e.g. `import deepEqual from "../utils";`).
    - Change the single memo-comparator call site (:105) from `if (!isEqual(rest, others))` to `if (!deepEqual(rest, others))`. No other comparator logic changes.
    - Change `const Component = component as any;` (:31) to `const Component = component;` — valid now that `ForgerControllerProps.component` is typed `React.ElementType` (Plan 01).
  </action>
  <verify>
    <automated>npx tsc --noEmit (exits 0) AND grep -c "from \"lodash\"" src/Forger/Forger.tsx returns 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from \"lodash\"" src/Forger/Forger.tsx` returns 0
    - `grep -n "isEqual" src/Forger/Forger.tsx` returns 0 matches
    - `grep -n "component as any" src/Forger/Forger.tsx` returns 0 matches
    - `grep -n "deepEqual" src/Forger/Forger.tsx` confirms deepEqual is used in the comparator
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Forger uses local deepEqual in its memo comparator, has no lodash import, and renders the component without `as any`; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan changes how Forge augments the RHF control object (in-memory, in-process) and swaps an internal equality utility. No external input boundary is crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.05-01 | Tampering | Object.assign onto RHF control instance | accept | RESEARCH "D-11 in-place augmentation safety": added enumerable keys are inert to RHF (RHF reads internal subjects/methods, never iterates control's own keys for behavior). Assumption A2 (no key collision with RHF ^7) noted for the Phase 4 RHF bump. No new attack surface. |
| T-02.05-02 | (none) | deepEqual swap | accept | Internal memo-comparator utility on props Forge already holds; no new threat. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `grep -n "Object.assign(methods.control" src/useForge/useForge.tsx` -> present; no `...methods.control` spread; no `"use strict"`; public return typed ForgeControl<T> with no `as any` (input-spread `...(props as any)` classified in SUMMARY if retained)
- `grep -c "from \"lodash\"" src/Forger/Forger.tsx` -> 0; `component as any` -> 0; deepEqual used
- Manual: control identity stable across renders; wizard nav still re-renders
</verification>

<success_criteria>
`control` is augmented in place (stable, typed `ForgeControl<T>` on the public return, no `as any`); the internal `...(props as any)` input spread is narrowed or explicitly classified in the SUMMARY as internal-input; `Forger.tsx` is lodash-free and `as any`-free. Unblocks Plans 06/07 (hook rewrites that rely on a stable control) and contributes to STAB-05/STAB-03.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stability/02-05-SUMMARY.md`
</output>
