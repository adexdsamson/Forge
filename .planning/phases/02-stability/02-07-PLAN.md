---
phase: 02-stability
plan: 07
type: execute
wave: 3
depends_on: ["02-01", "02-05"]
files_modified:
  - src/useForgeValues/useForgeValues.tsx
autonomous: true
requirements: [STAB-02]
must_haves:
  truths:
    - "useForgeValues is a thin pass-through over RHF public setValue/getValues (and trigger/watch as needed); zero control._* access"
    - "It remains a public export with the setValue/getValue/getValues surface intact"
    - "getValue(name) maps to getValues(name) but THROWS a Forge-named error for an unknown field — no `return undefined as any`"
    - "Field existence is detected via key-presence in the whole-form getValues() object (dot-path walk), NOT via control._names/_fields"
    - "No lodash import and no `as any` remain in the wrapper"
  artifacts:
    - path: "src/useForgeValues/useForgeValues.tsx"
      provides: "Thin public-API value read/write convenience hook"
      contains: "getValues"
  key_links:
    - from: "src/useForgeValues/useForgeValues.tsx getValue"
      to: "RHF getValues() whole-form object"
      via: "dot-path key-presence existence check (RISK-01)"
      pattern: "getValues\\(\\)"
---

<objective>
Collapse `useForgeValues` (~562 lines re-implementing RHF's `setValue`/`trigger`/`getValues` pipeline) into a thin pass-through over RHF's public methods (D-03/D-04). It stays a public export with the `setValue`/`getValue`/`getValues` surface; `getValue` throws a Forge-named error for unknown fields using a public-API existence check (RISK-01: dot-path key-presence in the whole-form `getValues()` object, NOT `_names`/`_fields`).

Purpose: STAB-02 — remove all `control._*` access and the `return undefined as any` typed lie from the value-convenience hook.
Output: A small `useForgeValues.tsx` delegating to public RHF methods, with zero `_*`, zero `as any`, zero lodash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stability/02-CONTEXT.md
@.planning/phases/02-stability/02-RESEARCH.md
@.planning/phases/02-stability/02-PATTERNS.md
@.planning/phases/02-stability/02-VALIDATION.md
@.planning/phases/02-stability/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse useForgeValues to a thin public-API wrapper (D-03/D-04, RISK-01)</name>
  <files>src/useForgeValues/useForgeValues.tsx</files>
  <read_first>
    - src/useForgeValues/useForgeValues.tsx (full current file — the lodash import ~:37, the ~562-line setValue/setValues/setFieldValue/updateTouchAndDirty/executeBuiltInValidation/executeSchemaAndUpdateState re-implementation, and the `return undefined as any` at ~:554)
    - .planning/phases/02-stability/02-PATTERNS.md (section "src/useForgeValues/useForgeValues.tsx (hook, thin wrapper D-03/04)" — the Forger delegation analog, the anti-pattern being deleted, and the Replacement pattern)
    - .planning/phases/02-stability/02-RESEARCH.md (sections "Pattern 2: Thin pass-through wrapper (D-03/D-04)", "RISK-01 — public existence check via dot-path key presence" with the hasPath algorithm AND its caveats, "Pitfall 1", and the "Don't Hand-Roll" rows for setValue/trigger/getValues)
    - src/Forger/Forger.tsx (the :27-30 useFormContext delegation idiom this rewrite mirrors)
  </read_first>
  <action>
    Rewrite src/useForgeValues/useForgeValues.tsx as a thin wrapper:
    - Keep the public signature accepting `{ control }: { control: ForgeControl<T> }` (the documented public surface, D-04). Derive RHF public methods from `useFormContext<T>()` (mirrors Forger's :27-30 idiom). Per RESEARCH Open Q #2 you MAY also accept an optional `methods` for out-of-provider usage; keep `{ control }` as the primary surface.
    - DELETE the entire ~562-line body (setValue/setValues/setFieldValue/updateTouchAndDirty/executeBuiltInValidation/executeSchemaAndUpdateState/getValues reimplementation) and the lodash import (~:37). Do NOT re-implement RHF's setValue/trigger/dirty pipeline.
    - Implement `setValue` = RHF's public `setValue` (pass options through). Implement `getValues` = RHF's public `getValues`.
    - Implement `getValue<N extends Path<T>>(name: N): PathValue<T, N>`: read `const all = ctx.getValues();` then check existence via a `hasPath(all, name)` dot-path key-presence walk (normalize `items[0].name` -> `items.0.name`, split on `.`, walk keys, return false if any segment missing or a non-object is hit). If absent, `throw new Error(\`useForgeValues.getValue: field "\${String(name)}" is not registered\`)`. Otherwise return `ctx.getValues(name)`. Do NOT use `control._names`/`control._fields` for existence (RISK-01); do NOT use `getFieldState` (returns defaults for unknown names). Replace the old `return undefined as any` entirely.
    - Add the `hasPath` helper (local, native checks only — no lodash). Document the known limitation (RISK-01 caveat / Assumption A5): a register-only field with no default may be absent from `getValues()` until first write; existence detection is reliable for fields with a default or that have been written (matches Forge's controlled `<Forger>` usage).
    - Return `{ setValue, getValue, getValues }`. No `as any` anywhere.
    - If collapsing orphans any `logic/` helper (e.g. getResolverOptions, hasPromiseValidation) used ONLY by this file, note it in SUMMARY but DO NOT delete shared helpers without confirming no other importer (getDirtyFields/getFieldValueAs are used elsewhere — leave them).
  </action>
  <verify>
    <automated>npx tsc --noEmit (exits 0) AND grep -c "control\._" src/useForgeValues/useForgeValues.tsx returns 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "control\._" src/useForgeValues/useForgeValues.tsx` returns 0
    - `grep -n "_subjects\|_formValues\|_names\|_fields\|_state" src/useForgeValues/useForgeValues.tsx` returns 0 matches
    - `grep -n "as any" src/useForgeValues/useForgeValues.tsx` returns 0 matches
    - `grep -n "from \"lodash\"" src/useForgeValues/useForgeValues.tsx` returns 0 matches
    - `grep -n "undefined as any" src/useForgeValues/useForgeValues.tsx` returns 0 matches
    - `npx tsc --noEmit` exits 0
    - MANDATORY MANUAL (STAB-02 / RISK-01): `getValue("does-not-exist")` throws the Forge-named error; `getValue("<a real field with a default>")` returns its value (not a throw); `setValue` then `getValues` round-trips a value with dirty/validate options passing through
  </acceptance_criteria>
  <done>useForgeValues is a thin wrapper over public setValue/getValues; getValue throws a Forge-named error on unknown fields via dot-path key-presence; zero `_*`/`as any`/lodash; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

`useForgeValues` reads/writes form values through RHF's public methods. `getValue` accepts a field-name argument from consumer code; an unknown name now produces a controlled, named throw rather than a silent `undefined`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.07-01 | Tampering | getValue silent-undefined on typo'd field | mitigate | D-04: replace `return undefined as any` with a Forge-named throw gated on a dot-path key-presence existence check — surfaces programmer error instead of silently returning undefined. Known limitation (default-less register-only fields) documented per Assumption A5. |
| T-02.07-02 | (none) | delegation to public setValue/getValues | accept | Delegating to RHF's canonical methods removes 562 lines of duplicated state logic; no new attack surface. |
</threat_model>

<verification>
- `grep -c "control\._" src/useForgeValues/useForgeValues.tsx` -> 0; no `_subjects`/`_formValues`/`_names`/`_fields`
- No `as any`, no lodash, no `undefined as any`
- `npx tsc --noEmit` exits 0
- MANUAL: getValue throws on unknown, returns value on known; setValue/getValues round-trip
</verification>

<success_criteria>
`useForgeValues` is a thin public-API wrapper with zero `_*` access and a Forge-named throw on unknown fields. Satisfies STAB-02.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stability/02-07-SUMMARY.md`
</output>
