---
phase: 02-stability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Forge/Forge.tsx
  - rollup.config.mjs
autonomous: false
requirements: [STAB-04, STAB-05]
user_setup: []
must_haves:
  truths:
    - "@hookform/devtools is no longer imported at the top of Forge.tsx; it is loaded via a synchronous guarded require inside `if (debug)`"
    - "When debug={true} and @hookform/devtools is absent, a synchronous Forge-named error is thrown instructing `npm i -D @hookform/devtools`"
    - "@hookform/devtools stays in the rollup external list so the require survives bundling in BOTH CJS and ESM output"
    - "processChildrenRecursively is retyped to React.ReactElement<Record<string,unknown>> via isValidElement with NO runtime/branch change"
    - "Native <form> submit, Enter-to-submit, and wizard last-step submit all still fire (Phase-1 CORR-01/CORR-04 preserved)"
  artifacts:
    - path: "src/Forge/Forge.tsx"
      provides: "Dev-gated lazy devtools load + as-any-free child-walker"
      contains: "require(\"@hookform/devtools\")"
    - path: "rollup.config.mjs"
      provides: "@hookform/devtools in the external list"
      contains: "@hookform/devtools"
  key_links:
    - from: "src/Forge/Forge.tsx debug path"
      to: "@hookform/devtools (optional peer)"
      via: "synchronous require in try/catch"
      pattern: "require\\(\"@hookform/devtools\"\\)"
    - from: "src/Forge/Forge.tsx processChildrenRecursively"
      to: "submit button / wizard nav onClick wiring"
      via: "cloneElement prop-merge (byte-for-byte preserved)"
      pattern: "cloneElement"
---

<objective>
Make `@hookform/devtools` dev-only and lazy-loaded (D-08/D-09, RISK-03) and retype the `processChildrenRecursively` child-walker to be `as any`-free (D-10, RISK-04) WITHOUT any runtime/branch change so the Phase-1 native-submit, Enter-to-submit, and wizard-last-step-submit fixes survive. This is the highest-risk Phase 2 plan because the child-walker carries CORR-01/CORR-04 and there are NO automated tests this phase.

Purpose: STAB-04 (devtools out of production) + STAB-05 (type hardening of the walker).
Output: `Forge.tsx` with a synchronous guarded `require` behind `if (debug)`, a typed child-walker, and `rollup.config.mjs` keeping `@hookform/devtools` externalized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stability/02-CONTEXT.md
@.planning/phases/02-stability/02-RESEARCH.md
@.planning/phases/02-stability/02-PATTERNS.md
@.planning/phases/02-stability/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dev-gate + lazy-load @hookform/devtools (D-08/D-09, RISK-03)</name>
  <files>src/Forge/Forge.tsx, rollup.config.mjs</files>
  <read_first>
    - src/Forge/Forge.tsx (the top-level `import { DevTool } from "@hookform/devtools";` at ~:29 and the unguarded `{debug && <DevTool control={control} />}` render at ~:323)
    - rollup.config.mjs (the `external` array — it is derived from `dependencies` + `peerDependencies`; once devtools leaves `dependencies` it must be added explicitly to `external`)
    - .planning/phases/02-stability/02-RESEARCH.md (section "RISK-03 — synchronous guarded require (CJS + ESM)" — the exact loadDevTool mechanism, why dynamic import() is rejected, and the dual-build grep note)
    - .planning/phases/02-stability/02-PATTERNS.md (section "src/Forge/Forge.tsx ... devtools D-08/09" — Replacement A)
  </read_first>
  <action>
    In src/Forge/Forge.tsx:
    - Remove the top-level `import { DevTool } from "@hookform/devtools";` (~:29).
    - Add a `loadDevTool()` helper that does a SYNCHRONOUS `require("@hookform/devtools").DevTool` inside try/catch; on catch, `throw new Error("Forge: debug mode requires '@hookform/devtools'. Install it with \`npm i -D @hookform/devtools\`.")`. Type its return as `React.ComponentType<{ control: unknown }>`. Do NOT use dynamic `import()` / `React.lazy` — async cannot throw synchronously during render (D-09 violated, RISK-03).
    - In the render path, replace the inline `{debug && <DevTool .../>}` with: build a `devtools` ReactNode only inside `if (debug) { const DevTool = loadDevTool(); devtools = <DevTool control={control} />; }`, then render `{devtools}`. The synchronous require throw happens during render when debug is on and the package is missing.
    In rollup.config.mjs:
    - Add `"@hookform/devtools"` to the `external` array explicitly (it currently comes from `dependencies`; Plan 08 removes it from `dependencies`, so it must be externalized by name here or the bundler will try to inline/error). This keeps `require("@hookform/devtools")` as an external call in both CJS and ESM output.
  </action>
  <verify>
    <automated>npx tsc --noEmit (exits 0) AND npx rollup -c (builds dist/index.cjs.js + dist/index.esm.js with no error)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import { DevTool }" src/Forge/Forge.tsx` returns 0 matches
    - `grep -n "require(\"@hookform/devtools\")" src/Forge/Forge.tsx` returns at least 1 match (inside try/catch)
    - `grep -n "@hookform/devtools" rollup.config.mjs` returns at least 1 match (in external)
    - `npx rollup -c` exits 0
    - Built output keeps devtools external: `grep -c "@hookform/devtools" dist/index.cjs.js` and `dist/index.esm.js` show the require/import is referenced, never inlined (no DevTool implementation pulled in)
    - Manual (RISK-03): in a scratch consumer WITHOUT @hookform/devtools installed, rendering `<Forge debug={true}>` throws the Forge-named error synchronously; rendering without `debug` shows zero @hookform/devtools in the consumer graph
  </acceptance_criteria>
  <done>devtools is no longer top-level imported; guarded synchronous require throws a Forge-named error when missing+debug; externalized in rollup; both bundles build.</done>
</task>

<task type="auto">
  <name>Task 2: Retype processChildrenRecursively child-walker as-any-free (D-10, RISK-04, type-only)</name>
  <files>src/Forge/Forge.tsx</files>
  <read_first>
    - src/Forge/Forge.tsx (the full `processChildrenRecursively` function and its ~14 `as any` casts: `(child as any).props` ~:113, `cloneElement(child, {...} as any)` ~:101-103/:136-143, the FormProvider spread `{...(control as unknown as any)} control={control as unknown as any}` ~:301-303, the native `<form onSubmit>` ~:315-318, wizard nav wiring ~:124-126/:253-257, button onClick wiring ~:101-108, `key={index}` ~:281, commented-out imports ~:18-26)
    - .planning/phases/02-stability/02-RESEARCH.md (section "RISK-04 — typed child-walker (no runtime change)" — the AnyElement type and isValidElement narrowing, and "Pitfall 3" warning signs)
    - .planning/phases/02-stability/02-PATTERNS.md (section "src/Forge/Forge.tsx ... child-walker retype D-10" — Replacement B and the MANDATORY manual gate list)
    - .planning/phases/02-stability/02-VALIDATION.md (Manual-Only Verifications table — the three RISK-04 submit checks)
  </read_first>
  <action>
    TYPE-ONLY changes to src/Forge/Forge.tsx — every branch, every cloneElement/createElement prop-merge must stay byte-for-byte equivalent:
    - Define `type AnyElement = React.ReactElement<Record<string, unknown>>;`.
    - In the `Children.map` callback, keep the existing `isValidElement(child)` guard (or add it) to narrow, then `const el = child as AnyElement;` and read `const childProps = el.props;` as typed `Record<string, unknown>` — remove `(child as any).props`.
    - Remove the trailing `as any` on each `cloneElement(el, {...childProps, onClick, ...})` and `createElement` call; `el.type` is `string | JSXElementConstructor<unknown>` and passes through directly. Do NOT change which props are merged or which branch runs.
    - Retype the FormProvider spread (~:301-303) honestly: pass the real `methods` if available or narrow the cast — but MUST NOT change runtime wiring (Phase-1 fixes ride on it). If a clean retype risks behavior change, keep a minimal narrow cast and note it in SUMMARY rather than altering wiring.
    - Discretionary cleanups (fold in): delete commented-out imports at ~:18-26 (`// isWeb`, `// mergePlatformProps`, `// REACT_NATIVE_COMPONENTS`); change `key={index}` at ~:281 to a stable key (use the field id/keyName if available, else keep index and note why).
  </action>
  <verify>
    <automated>npx tsc --noEmit (exits 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "as any" src/Forge/Forge.tsx` returns 0 (or a single justified internal cast documented in SUMMARY for the FormProvider spread only)
    - `grep -n "(child as any)" src/Forge/Forge.tsx` returns 0 matches
    - `npx tsc --noEmit` exits 0
    - MANDATORY MANUAL (RISK-04, no automated test exists — these are blocking): (1) clicking a submit button inside `<Forge>` fires `onSubmit`; (2) pressing Enter in a focused text field submits the form; (3) a wizard advanced to its last step fires `handleWizardSubmit` on the submit button. All three MUST be verified before this plan is marked done.
  </acceptance_criteria>
  <done>child-walker is `as any`-free with identical runtime branching; the three RISK-04 manual submit checks pass; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer dependency graph -> Forge debug path | `@hookform/devtools` is loaded only when the consumer opts into `debug`; an absent package is the intended state for production consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.03-01 | Information Disclosure | @hookform/devtools shipped to production | mitigate | D-08/D-09: devtools moves to optional peer + dev-only guarded require; a production consumer who never sets `debug` has zero devtools trace, removing the dev-only inspector (which can expose form internals) from production bundles. |
| T-02.03-02 | Denial of Service | guarded require throws during render | accept | The synchronous throw is the intended D-09 behavior — it is a developer-facing setup error in dev/debug only, never on the production path (debug is opt-in). Not a runtime DoS vector for end users. |
| T-02.03-03 | Tampering | child-walker retype silently regresses submit | mitigate | Type-only change with byte-for-byte prop-merge preservation; the three mandatory RISK-04 manual submit checks gate completion. No automated coverage until Phase 3. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx rollup -c` exits 0 (both bundles)
- `grep -n "import { DevTool }" src/Forge/Forge.tsx` -> 0; guarded `require("@hookform/devtools")` present
- `@hookform/devtools` in rollup `external`; not inlined in dist
- `grep -c "as any" src/Forge/Forge.tsx` -> 0 (or one justified FormProvider cast)
- MANUAL RISK-04: native submit, Enter-to-submit, wizard last-step submit all fire
- MANUAL RISK-03: debug+missing-package -> synchronous Forge-named throw; no-debug consumer -> zero devtools in graph
</verification>

<success_criteria>
@hookform/devtools is dev-only + lazy + loud-on-missing; the child-walker is `as any`-free with the Phase-1 submit/wizard behaviors intact (manually verified). Unblocks Plan 08 (package.json devtools demotion relies on this externalization).
</success_criteria>

<output>
After completion, create `.planning/phases/02-stability/02-03-SUMMARY.md`
</output>
