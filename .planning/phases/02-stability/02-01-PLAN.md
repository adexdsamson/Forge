---
phase: 02-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
autonomous: true
requirements: [STAB-05]
must_haves:
  truths:
    - "Public type surface in types.ts has no `as any` and no bare `any` on ForgerProps.component / ForgerControllerProps.component"
    - "ForgeControl<T> augmentation shape is preserved (success criterion #5 locks it)"
    - "react-dropzone is imported as a type-only import so it is erased at runtime"
    - "tsc --noEmit passes cleanly after the sweep"
  artifacts:
    - path: "src/types.ts"
      provides: "Hardened public type definitions: ForgeControl<T>, ForgerProps, ForgerControllerProps"
      contains: "import type { Accept }"
  key_links:
    - from: "src/types.ts ForgeControl<T>"
      to: "useForge return / Forge+Forger consumers"
      via: "augmentation shape (hasFields, fields, wizard props)"
      pattern: "ForgeControl"
---

<objective>
Harden the public type surface in `src/types.ts` so it is `as any`-free and `tsc --noEmit` clean, without changing the locked `ForgeControl<T>` augmentation shape. This is the foundation plan: `useForge` (Plan 05) types its returned control as `ForgeControl<T>`, and `Forger` (Plan 05) drops `component as any` once `ForgerControllerProps.component` is `React.ElementType`.

Purpose: STAB-05 — make TypeScript strict mode actually have teeth on the public API.
Output: A swept `src/types.ts` with type-only react-dropzone import, `React.ElementType` component types, `React.ReactElement` over `JSX.Element`, and `Record<string, unknown>` over `Record<string, any>` where it does not cascade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stability/02-CONTEXT.md
@.planning/phases/02-stability/02-RESEARCH.md
@.planning/phases/02-stability/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep types.ts public type surface (D-10)</name>
  <files>src/types.ts</files>
  <read_first>
    - src/types.ts (the file being modified — read the full current type definitions, esp. lines :2, :17-32, :43, :44, :49, :64, :68, :82)
    - .planning/phases/02-stability/02-PATTERNS.md (section "src/types.ts (types, hardening sweep D-10)" — exact per-line sweep targets and the KEEP-shape directive)
    - .planning/phases/02-stability/02-RESEARCH.md (section "RISK-04 — `ForgerProps.component` tightening decision" — the definitive `React.ElementType` decision and why NOT to use a ForgerSlotProps generic constraint)
  </read_first>
  <action>
    Apply these exact retypes in src/types.ts, changing NO runtime behavior (this file is types only):
    - Line ~2: change `import { Accept } from "react-dropzone";` to `import type { Accept } from "react-dropzone";` so it is erased at runtime (fixes module-not-found for consumers without react-dropzone). This is the first `import type` in the repo — that is expected.
    - KEEP the `ForgeControl<T, TFieldProps>` augmentation shape at :17-32 exactly as-is (success criterion #5 locks it: `Control<T, any> &` with `fields?`, `hasFields`, `isWizard?`, `currentStep?`, `totalSteps?`, `isFirstStep?`, `isLastStep?`, `handleNext?`, `handlePrevious?`, `handleWizardSubmit?`). Do NOT split Forge state into a separate object — that is a Deferred Idea, out of scope.
    - Line ~43: `ForgerProps.component: any;` to `component: React.ElementType;` — do NOT introduce a `ForgerSlotProps`-constrained generic (RESEARCH RISK-04: produces false positives on valid cross-platform inputs).
    - Line ~44: `label?: string | JSX.Element;` to `label?: string | React.ReactElement;` (no global JSX namespace under `jsx: react-jsx`).
    - Line ~64: `ForgerControllerProps.component: Component<ForgerSlotProps>;` to `component: React.ElementType;` (the current type is WRONG — it types an instance, not a component type).
    - Line ~82: `typeof Component<ForgerSlotProps> | any` to `React.ElementType`.
    - Lines ~49 and ~68: `& Record<string, any>` to `& Record<string, unknown>` ONLY where it does not cascade type errors into valid consumer usage; if a change cascades errors, leave it as `Record<string, unknown>` is preferred but revert that single site to `any` only if strictly necessary and note it in the SUMMARY.
    - Remove the now-unused `Component` import if `Component<ForgerSlotProps>` was its only use.
  </action>
  <verify>
    <automated>npx tsc --noEmit (exits 0)</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -n "as any" src/types.ts` returns 0 matches
    - `grep -n ": any" src/types.ts` returns 0 matches for `component: any`
    - `grep -n "JSX.Element" src/types.ts` returns 0 matches
    - `grep -n "import type { Accept }" src/types.ts` returns 1 match
    - The `ForgeControl` type still declares `hasFields`, `fields`, `isWizard`, `currentStep`, `totalSteps`, `isFirstStep`, `isLastStep`, `handleNext`, `handlePrevious`, `handleWizardSubmit` (grep `ForgeControl` block confirms shape preserved)
  </acceptance_criteria>
  <done>types.ts public surface is `as any`-free, `component` is `React.ElementType`, react-dropzone is `import type`, `JSX.Element` is gone, ForgeControl shape preserved, tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies only TypeScript type declarations. No runtime code, no new data flow, no new external input path.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.01-01 | (none) | src/types.ts | accept | No new attack surface — this is a type-only sweep. `import type` is erased at runtime; no value-level import is added. No threat introduced. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `src/types.ts` contains no `as any`, no `JSX.Element`, no bare `component: any`
- `import type { Accept }` present; ForgeControl augmentation shape intact
</verification>

<success_criteria>
Public type surface in `types.ts` is `as any`-free and `tsc --noEmit` clean; `ForgeControl<T>` shape preserved; react-dropzone import erased at runtime. Unblocks Plan 05 (useForge ForgeControl cast, Forger component typing).
</success_criteria>

<output>
After completion, create `.planning/phases/02-stability/02-01-SUMMARY.md`
</output>
