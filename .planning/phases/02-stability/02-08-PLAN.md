---
phase: 02-stability
plan: 08
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-05", "02-06", "02-07"]
files_modified:
  - package.json
autonomous: false
requirements: [STAB-03, STAB-04, STAB-05]
must_haves:
  truths:
    - "lodash and @types/lodash are gone from package.json; @hookform/devtools is gone from dependencies"
    - "@hookform/devtools appears under devDependencies AND peerDependenciesMeta as optional (mirroring react-dropzone)"
    - "The react-hook-form peer range floor is lowered to ^7.34.0"
    - "After npm install + rollup -c, the runtime tree and built bundles contain no lodash and no inlined @hookform/devtools"
    - "A packed-tarball consumer install (without @hookform/devtools) has zero @hookform/devtools in its dependency graph (scriptable)"
    - "tsc --noEmit and both bundle builds are clean"
  artifacts:
    - path: "package.json"
      provides: "Cleaned dependency manifest: no lodash, devtools demoted to optional peer + devDep, RHF ^7.34.0"
      contains: "\"@hookform/devtools\": { \"optional\": true }"
  key_links:
    - from: "package.json peerDependenciesMeta"
      to: "@hookform/devtools optional peer"
      via: "mirrors react-dropzone optional-peer block"
      pattern: "peerDependenciesMeta"
---

<objective>
Finalize the dependency manifest (D-08/D-13): remove `lodash` + `@types/lodash`, demote `@hookform/devtools` from runtime `dependencies` to a `devDependency` + optional peer (mirroring `react-dropzone`), and lower the `react-hook-form` peer floor to `^7.34.0`. Then refresh `node_modules`/lockfile and rebuild so the removed packages leave the runtime tree and bundles. This plan runs LAST because it depends on every source file having already dropped its lodash and top-level-devtools imports.

Purpose: STAB-03 (lodash out of deps) + STAB-04 (devtools dev-only) + STAB-05 (supported RHF ^7 range).
Output: A cleaned `package.json`, a refreshed lockfile, and green type+build with no lodash/devtools in the runtime graph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stability/02-CONTEXT.md
@.planning/phases/02-stability/02-RESEARCH.md
@.planning/phases/02-stability/02-PATTERNS.md
@.planning/phases/02-stability/02-VALIDATION.md
@.planning/phases/02-stability/02-02-SUMMARY.md
@.planning/phases/02-stability/02-03-SUMMARY.md
@.planning/phases/02-stability/02-06-SUMMARY.md
@.planning/phases/02-stability/02-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit package.json dependency manifest (D-08/D-13)</name>
  <files>package.json</files>
  <read_first>
    - package.json (current: `dependencies` :42-45 has `@hookform/devtools` ^4.3.1 + `lodash` ^4.17.21; `peerDependencies` :46-49 has react-hook-form ^7.50.1; `peerDependenciesMeta` :50-54 has the react-dropzone optional block; `devDependencies` :55-69 has `@types/lodash`)
    - .planning/phases/02-stability/02-PATTERNS.md (section "package.json (config, dependency move D-08/13)" — the exact changes and the react-dropzone analog to mirror)
    - .planning/phases/02-stability/02-RESEARCH.md (the "Package.json changes" jsonc block, "Minimum RHF ^7 floor" justifying ^7.34.0, and "Runtime State Inventory" requiring npm install + rebuild)
  </read_first>
  <action>
    Edit package.json:
    - In `dependencies`: remove `"@hookform/devtools": "^4.3.1"` and `"lodash": "^4.17.21"`. The `dependencies` object becomes empty `{}` (or remove the key entirely).
    - In `peerDependencies`: change `"react-hook-form": "^7.50.1"` to `"react-hook-form": "^7.34.0"` (RESEARCH: 7.34.0 is the useFieldArray `rules` floor, the binding constraint). Keep `"react": ">=18"`.
    - In `peerDependenciesMeta`: ADD `"@hookform/devtools": { "optional": true }` alongside the existing `react-dropzone` optional block (copy that exact shape).
    - In `devDependencies`: ADD `"@hookform/devtools": "^4.3.1"`; REMOVE `"@types/lodash": "^4.14.202"`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d=p.dependencies||{}; if(d.lodash||d['@hookform/devtools']) process.exit(1); if(!(p.devDependencies&&p.devDependencies['@hookform/devtools'])) process.exit(1); if(p.devDependencies&&p.devDependencies['@types/lodash']) process.exit(1); if(!(p.peerDependenciesMeta&&p.peerDependenciesMeta['@hookform/devtools']&&p.peerDependenciesMeta['@hookform/devtools'].optional)) process.exit(1); if(p.peerDependencies['react-hook-form']!=='^7.34.0') process.exit(1); console.log('ok')" (prints ok, exits 0)</automated>
  </verify>
  <acceptance_criteria>
    - `dependencies` has no `lodash` and no `@hookform/devtools` key
    - `devDependencies` has `@hookform/devtools` and no `@types/lodash`
    - `peerDependenciesMeta["@hookform/devtools"].optional === true`
    - `peerDependencies["react-hook-form"] === "^7.34.0"`
    - the node -e manifest assertion above prints `ok`
  </acceptance_criteria>
  <done>package.json: lodash + @types/lodash removed, devtools demoted to devDep + optional peer, RHF floor ^7.34.0.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh lockfile + rebuild; confirm clean runtime tree, bundles, and consumer graph (STAB-03/04/05)</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/02-stability/02-RESEARCH.md (Runtime State Inventory: npm install + rebuild flush lodash/devtools from the runtime tree; Validation Architecture "Bundle grep checks")
    - .planning/phases/02-stability/02-VALIDATION.md (Static-assertion patterns + Manual-Only devtools rows)
  </read_first>
  <action>
    - Run `npm install` to refresh `node_modules` and `package-lock.json` so `lodash` leaves the runtime tree and `@hookform/devtools` moves to dev-only.
    - Run `npx tsc --noEmit` (must be clean) and `npx rollup -c` (must build `dist/index.cjs.js`, `dist/index.esm.js`, `dist/index.d.ts`).
    - Confirm no source file still imports lodash anywhere (phase-wide gate): `grep -rn "from \"lodash\"" src/` and `grep -rn "from 'lodash'" src/` both return 0. This proves all SEVEN lodash files are clean — five rewired in Plan 02 (utils.ts, validateField.ts, logic/getDirtyFields.ts, logic/getFieldValueAs.ts, logic/hasPromiseValidation.ts), one in Plan 05 (Forger.tsx isEqual), one collapsed in Plan 07 (useForgeValues.tsx).
    - Confirm the built bundles contain no lodash MODULE BODY (not merely no path string). Use a deterministic lodash-internal marker rather than the substring "lodash": assert `grep -nE "function lodash\(|runInContext|lodash\.templateSettings" dist/index.cjs.js dist/index.esm.js` returns 0, AND assert no `require("lodash")` / `from "lodash"` survives in the CJS/ESM output. (The bare substring "lodash" can appear only in sourcemap path comments, which are inert.)
    - Confirm `@hookform/devtools` appears in dist only as an external require/import (never the inlined DevTool implementation).
    - SCRIPTABLE STAB-04 consumer-graph gate: run `npm pack` to produce the tarball; in a throwaway temp dir, `npm init -y`, install ONLY `react react-dom react-hook-form` + the packed tarball (do NOT install `@hookform/devtools`), then assert `@hookform/devtools` is ABSENT from `npm ls --all` output and absent from the temp dir's `node_modules` (`test ! -d node_modules/@hookform/devtools`). This proves a non-debug consumer never pulls devtools into the production graph without relying on manual narrative. Clean up the temp dir + the `.tgz` afterward.
  </action>
  <verify>
    <automated>npx tsc --noEmit (exits 0) AND npx rollup -c (exits 0) AND grep -rn "from \"lodash\"" src/ returns 0 AND grep -nE "function lodash\(|runInContext" dist/index.cjs.js dist/index.esm.js returns 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from \"lodash\"" src/` and `grep -rn "from 'lodash'" src/` both return 0 matches (all seven lodash sites across the phase are clean)
    - `npx tsc --noEmit` exits 0
    - `npx rollup -c` exits 0 and produces all three dist artifacts
    - `grep -nE "function lodash\(|runInContext|lodash\.templateSettings" dist/index.cjs.js dist/index.esm.js` returns 0 (no lodash module body inlined — deterministic marker, not the bare "lodash" substring)
    - `grep -nE "require\([\"']lodash[\"']\)|from [\"']lodash[\"']" dist/index.cjs.js dist/index.esm.js` returns 0 (no surviving lodash require/import in the bundle)
    - `@hookform/devtools` appears in dist only as an external `require`/import, never as inlined DevTool implementation
    - SCRIPTABLE (STAB-04): after `npm pack` + installing the tarball into a throwaway dir WITHOUT `@hookform/devtools`, `npm ls --all` shows no `@hookform/devtools` and `node_modules/@hookform/devtools` does not exist (`test ! -d` exits 0)
    - MANDATORY MANUAL (STAB-04 behavior): with `debug={true}` and the package uninstalled, Forge throws the Forge-named error (the runtime lazy-load behavior from Plan 03)
  </acceptance_criteria>
  <done>lockfile refreshed; tsc + both bundles green; no lodash in src or bundle body; devtools external-only and provably absent from a packed non-debug consumer graph via npm pack + npm ls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits the dependency manifest and refreshes the install. The supply-chain surface (which packages a consumer pulls in) is the relevant boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.08-01 | Information Disclosure | @hookform/devtools in production graph | mitigate | Demoting devtools to optional peer + devDependency ensures non-debug consumers never pull the dev inspector into production — closes the same exposure as Plan 03 at the manifest level. Scriptable npm-pack + npm-ls gate confirms absence from a fresh consumer graph (no longer manual-only). |
| T-02.08-02 | Elevation of Privilege / supply chain | removing lodash | mitigate | Dropping `lodash` + `@types/lodash` reduces transitive supply-chain surface (a large, frequently-targeted package) with native replacements already landed in Plans 02/05/07. |
| T-02.08-03 | (none) | RHF peer floor ^7.34.0 | accept | Widening the supported peer range; no new attack surface. |
</threat_model>

<verification>
- node -e manifest assertion prints `ok` (lodash/devtools out of deps, devtools optional peer + devDep, RHF ^7.34.0)
- `grep -rn "from \"lodash\"" src/` -> 0 (phase-wide lodash gate, all seven files)
- `npx tsc --noEmit` exits 0; `npx rollup -c` exits 0 (all three artifacts)
- dist has no inlined lodash module body (deterministic marker grep) and no surviving lodash require/import; devtools external-only
- SCRIPTABLE: npm pack -> install tarball without devtools -> `npm ls --all` + `test ! -d node_modules/@hookform/devtools` confirms zero devtools in the consumer graph
- MANUAL: debug+missing -> Forge-named throw
</verification>

<success_criteria>
The published manifest carries no lodash and no runtime devtools; devtools is an optional peer + devDependency; the RHF peer floor is `^7.34.0`; type + build are green; the runtime graph/bundles are clean; and a packed-tarball consumer install provably contains zero devtools. Satisfies STAB-03/STAB-04 and the build half of STAB-05.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stability/02-08-SUMMARY.md`
</output>
