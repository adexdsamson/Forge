---
phase: 02-stability
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/usePersist/usePersist.tsx
autonomous: true
requirements: [STAB-02]
must_haves:
  truths:
    - "usePersist no longer accesses control._subjects.state (or any control._* member)"
    - "usePersist subscribes to values via public useWatch and to isDirty/isValid via scoped useFormState"
    - "The handler receives (values, { isDirty, isValid }) — the new lighter signature (documented D-01 break)"
    - "The handlerRef ref-stability idiom is preserved so the effect does not re-bind on every handler identity change"
  artifacts:
    - path: "src/usePersist/usePersist.tsx"
      provides: "Public-API autosave/draft subscription"
      contains: "useWatch"
  key_links:
    - from: "src/usePersist/usePersist.tsx"
      to: "RHF useWatch + useFormState"
      via: "public reactive subscription"
      pattern: "useWatch|useFormState"
---

<objective>
Rewrite `usePersist` onto public RHF APIs (D-12): replace the `control._subjects.state` firehose subscription with `useWatch` (reactive values) + a scoped `useFormState` (`isDirty`/`isValid`), and deliver the new lighter handler signature `(values, { isDirty, isValid })`.

Purpose: STAB-02 — remove `control._subjects`/`control._formValues` access from the autosave/draft subscription path.
Output: A `usePersist.tsx` with zero `_*` access, public reactive hooks, and a documented lighter handler contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stability/02-CONTEXT.md
@.planning/phases/02-stability/02-RESEARCH.md
@.planning/phases/02-stability/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite usePersist onto useWatch + useFormState (D-12)</name>
  <files>src/usePersist/usePersist.tsx</files>
  <read_first>
    - src/usePersist/usePersist.tsx (the full current file: the `ForgePersist` handler type at :5-15, the `handlerRef` idiom at :21-22, and the `useSubscribe({ subject: (control as any)._subjects.state, ... })` block at :24-30)
    - .planning/phases/02-stability/02-PATTERNS.md (section "src/usePersist/usePersist.tsx (hook, public subscription D-12)" — KEEPER idiom, anti-pattern removed, Replacement pattern)
    - .planning/phases/02-stability/02-RESEARCH.md (sections "Pattern 3: Reactive observe via public hooks (D-12)", "D-12 usePersist rewrite confirmation", and "Pitfall 5: useWatch returns a new object reference each render")
  </read_first>
  <action>
    Rewrite src/usePersist/usePersist.tsx:
    - Change the `ForgePersist` handler type (:5-15) so the handler signature becomes `(values: T, state: { isDirty: boolean; isValid: boolean }) => void` (documented D-01 break — drop the old `{ name, type, values }` firehose shape). Keep `control: Control<TFieldValues>` (public RHF type, not ForgeControl).
    - Remove the `useSubscribe({ subject: (control as any)._subjects.state, ... })` block entirely. Remove the `useSubscribe` import if it is no longer used here.
    - Subscribe via public hooks: `const values = useWatch({ control });` for reactive values; `const { isDirty, isValid } = useFormState({ control });` for scoped state.
    - PRESERVE the `handlerRef` idiom (:21-22): `const handlerRef = React.useRef(handler); handlerRef.current = handler;`.
    - Fire in an effect: `React.useEffect(() => { handlerRef.current(values, { isDirty, isValid }); }, [values, isDirty, isValid]);`.
    - Import `useWatch` and `useFormState` from `react-hook-form`. Document (in a code comment) the firing contract: useWatch returns a fresh object each tick so the handler fires on value changes — intended for autosave (RESEARCH Pitfall 5).
  </action>
  <verify>
    <automated>npx tsc --noEmit (exits 0) AND grep -c "control\._" src/usePersist/usePersist.tsx returns 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "control\._" src/usePersist/usePersist.tsx` returns 0
    - `grep -n "_subjects" src/usePersist/usePersist.tsx` returns 0 matches
    - `grep -n "as any" src/usePersist/usePersist.tsx` returns 0 matches
    - `grep -n "useWatch\|useFormState" src/usePersist/usePersist.tsx` confirms both public hooks are used
    - `npx tsc --noEmit` exits 0
    - Manual (STAB-02): typing into a field calls the handler with current values + `{ isDirty, isValid }`
  </acceptance_criteria>
  <done>usePersist uses useWatch + useFormState, zero `_*`/`as any`, handlerRef preserved, new handler signature in place, tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

`usePersist` hands current form values to a consumer-supplied callback (autosave/draft). The values originate inside RHF; no new external input crosses a boundary in this rewrite.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.04-01 | Information Disclosure | values passed to consumer handler | accept | The handler is consumer-supplied; Forge does not persist or transmit values itself (RESEARCH Runtime State Inventory: no stored data). Same data exposure as before; the new signature delivers strictly LESS state (values + two flags vs the full firehose). No new threat. |
| T-02.04-02 | (none) | _subjects removal | accept | Removing private-API access reduces coupling; no new attack surface. |
</threat_model>

<verification>
- `grep -c "control\._" src/usePersist/usePersist.tsx` -> 0; no `_subjects`, no `as any`
- `useWatch` + `useFormState` both present
- `npx tsc --noEmit` exits 0
- Manual: handler fires with `(values, { isDirty, isValid })` on field change
</verification>

<success_criteria>
`usePersist` obtains values/state via public RHF hooks only; zero `_*` access; lighter documented handler signature. Contributes to STAB-02.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stability/02-04-SUMMARY.md`
</output>
