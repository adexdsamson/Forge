---
phase: 02-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils.ts
  - src/validateField.ts
  - src/logic/getDirtyFields.ts
  - src/logic/getFieldValueAs.ts
  - src/logic/hasPromiseValidation.ts
autonomous: true
requirements: [STAB-03]
must_haves:
  truths:
    - "No `from \"lodash\"` import remains in utils.ts, validateField.ts, logic/getDirtyFields.ts, logic/getFieldValueAs.ts, or logic/hasPromiseValidation.ts"
    - "A single shared isObject helper with lodash semantics (function -> true, null -> false) is centralized in utils.ts and exported"
    - "isString, isUndefined, isBoolean are exported from utils.ts (alongside the already-exported isFunction) so validateField.ts and the two logic/ files can import them; existing isFunction and deepEqual are reused"
    - "tsc --noEmit passes cleanly"
  artifacts:
    - path: "src/utils.ts"
      provides: "Native isUndefined/isString/isNumber inline checks (isUndefined+isString exported) + exported isObject (lodash semantics) + exported isBoolean"
      contains: "export const isObject"
    - path: "src/validateField.ts"
      provides: "Native predicates imported from ../utils instead of lodash"
    - path: "src/logic/getDirtyFields.ts"
      provides: "isObject imported from ../utils (lodash semantics preserved)"
    - path: "src/logic/getFieldValueAs.ts"
      provides: "isString/isUndefined imported from ../utils instead of lodash"
    - path: "src/logic/hasPromiseValidation.ts"
      provides: "isFunction/isObject imported from ../utils instead of lodash"
  key_links:
    - from: "src/logic/getDirtyFields.ts"
      to: "src/utils.ts isObject"
      via: "import"
      pattern: "isObject.*from.*utils"
    - from: "src/logic/getFieldValueAs.ts"
      to: "src/utils.ts isString/isUndefined"
      via: "import"
      pattern: "isString.*from.*utils"
    - from: "src/logic/hasPromiseValidation.ts"
      to: "src/utils.ts isFunction/isObject"
      via: "import"
      pattern: "isObject.*from.*utils"
---

<objective>
Remove the `lodash` runtime dependency from five of the seven lodash call-site files by replacing the lodash predicate utilities with native checks, reusing the local helpers that already exist in `utils.ts`. (`Forger.tsx`'s `isEqual` and `useForgeValues.tsx`'s lodash imports are handled in their own rewrite plans — 05 and 07 — to avoid file-ownership conflicts.) The two `logic/` files `getFieldValueAs.ts` and `hasPromiseValidation.ts` are imported ONLY by `useForgeValues.tsx`, which Plan 07 collapses to a thin wrapper that no longer imports them; their lodash imports must still be rewired here so the phase-wide lodash gate (Plan 08) reaches zero.

Purpose: STAB-03 — shrink the published bundle by dropping lodash.
Output: `utils.ts` centralizes a lodash-semantics `isObject` and exports `isObject`/`isBoolean`/`isString`/`isUndefined`; `validateField.ts`, `logic/getDirtyFields.ts`, `logic/getFieldValueAs.ts`, and `logic/hasPromiseValidation.ts` import native predicates from `../utils`; zero `from "lodash"` in these five files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stability/02-CONTEXT.md
@.planning/phases/02-stability/02-RESEARCH.md
@.planning/phases/02-stability/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize native predicates in utils.ts (D-13)</name>
  <files>src/utils.ts</files>
  <read_first>
    - src/utils.ts (the file being modified — read the lodash import at :22 and the existing local helpers: isNullOrUndefined :24, isBoolean :37, isFunction :137, deepEqual default export :448)
    - .planning/phases/02-stability/02-PATTERNS.md (section "src/utils.ts (utility, lodash swap D-13)" — confirms which helpers are already local vs net-new)
    - .planning/phases/02-stability/02-RESEARCH.md (section "lodash -> native replacement (D-13)" table AND "Pitfall 4: Lodash isObject vs native typeof divergence" — the CRITICAL isObject semantics)
  </read_first>
  <action>
    In src/utils.ts:
    - Remove the lodash import at :22 `import { isUndefined, isObject, isString, isNumber } from "lodash";`.
    - Add local native predicates: `isUndefined(v) => v === undefined` (does NOT match null — preserves the distinction from the separate local `isNullOrUndefined`), `isString(v) => typeof v === "string"`, `isNumber(v) => typeof v === "number"`.
    - EXPORT `isUndefined` and `isString` (in addition to defining them) so `validateField.ts` and the two `logic/` files (`getFieldValueAs.ts`, `hasPromiseValidation.ts`) can import them. `isNumber` may stay local unless an external importer needs it.
    - Add and EXPORT a single shared `isObject(v): v is object => v !== null && (typeof v === "object" || typeof v === "function")`. CRITICAL: lodash semantics — true for functions and arrays, false for null. Do NOT use bare `typeof v === "object"` (that is true for null, false for functions and breaks getDirtyFields/deepEqual/isPlainObject/hasPromiseValidation).
    - EXPORT the existing local `isBoolean` (:37) so validateField.ts can import it (it is currently local-only). Leave `isFunction` (:137, already exported) and `deepEqual` (:448, default export) untouched.
    - Verify every former-lodash call site inside utils.ts now resolves to the local predicate with identical behavior.
  </action>
  <verify>
    <automated>npx tsc --noEmit (exits 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from \"lodash\"" src/utils.ts` returns 0 matches
    - `grep -n "export const isObject" src/utils.ts` returns 1 match
    - `grep -n "export" src/utils.ts | grep isBoolean` confirms isBoolean is exported
    - `grep -n "export" src/utils.ts | grep -E "isString|isUndefined"` confirms isString and isUndefined are exported
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>utils.ts has zero lodash imports; isObject (lodash semantics), isBoolean, isString, and isUndefined are exported; isFunction/deepEqual reused; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire validateField.ts and getDirtyFields.ts off lodash (D-13)</name>
  <files>src/validateField.ts, src/logic/getDirtyFields.ts</files>
  <read_first>
    - src/validateField.ts (lodash import at :33 `import { isBoolean, isFunction, isObject, isString, isUndefined } from "lodash";` and call sites listed in PATTERNS)
    - src/logic/getDirtyFields.ts (lodash import at :1 `import { isObject, isUndefined } from "lodash";`; note :2 ALREADY imports deepEqual/isNullOrUndefined/isPrimitive/objectHasFunction from ../utils)
    - .planning/phases/02-stability/02-PATTERNS.md (sections "src/validateField.ts" and "src/logic/getDirtyFields.ts" — pure import-rewire, behavior identical)
    - src/utils.ts (the centralized exports from Task 1 — only after Task 1 lands)
  </read_first>
  <action>
    Pure import-rewire, no call-site logic changes:
    - In src/validateField.ts: remove the lodash import at :33; import `isBoolean`, `isFunction`, `isObject`, `isString`, `isUndefined` from `../utils` (now all exported/added in Task 1). All call sites (:102, :118-119, :157, :162, :173-174, :205, :252, :260, :287, :311, :329, :346) stay byte-for-byte identical.
    - In src/logic/getDirtyFields.ts: remove the lodash import at :1; extend the existing `../utils` import (at :2) to also pull `isObject` (and `isUndefined` from ../utils). Call sites (:7, :11, :34, :38, :41) unchanged. CRITICAL: the imported `isObject` MUST keep lodash semantics (function -> true, null -> false) or markFieldsDirty/getDirtyFieldsFromDefaultValues misclassify.
  </action>
  <verify>
    <automated>npx tsc --noEmit (exits 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from \"lodash\"" src/validateField.ts src/logic/getDirtyFields.ts` returns 0 matches
    - `grep -n "from \"../utils\"" src/logic/getDirtyFields.ts` shows isObject imported
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>validateField.ts and getDirtyFields.ts import native predicates from ../utils; zero lodash imports; tsc clean; behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire logic/getFieldValueAs.ts and logic/hasPromiseValidation.ts off lodash (D-13)</name>
  <files>src/logic/getFieldValueAs.ts, src/logic/hasPromiseValidation.ts</files>
  <read_first>
    - src/logic/getFieldValueAs.ts (lodash import at :1 `import { isString, isUndefined } from "lodash";`; call sites: `isUndefined(value)` :8, `isString(value)` :16 — pure predicate calls, no logic to change)
    - src/logic/hasPromiseValidation.ts (lodash import at :2 `import { isFunction, isObject } from 'lodash';`; call sites: `isFunction(fieldReference.validate)` :11, `isObject(fieldReference.validate)` :13 — pure predicate calls)
    - src/utils.ts (the centralized exports from Task 1: `isString`, `isUndefined`, `isObject` newly exported; `isFunction` already exported — import these. The relative path from `src/logic/` is `../utils`, exactly as `getDirtyFields.ts` already does)
    - .planning/phases/02-stability/02-PATTERNS.md (section "src/logic/getDirtyFields.ts" — the analog: a `logic/` file rewired from `lodash` to `../utils` with identical call-site behavior; getFieldValueAs.ts and hasPromiseValidation.ts follow the same pattern)
    - .planning/phases/02-stability/02-RESEARCH.md (section "lodash -> native replacement (D-13)" table — `isString`/`isUndefined`/`isFunction` are trivial typeof swaps; `isObject` MUST keep lodash semantics, which Task 1 centralizes)
  </read_first>
  <action>
    Pure import-rewire, no call-site logic changes (mirror exactly how getDirtyFields.ts is rewired in Task 2):
    - In src/logic/getFieldValueAs.ts: remove the lodash import at :1 `import { isString, isUndefined } from "lodash";`; replace with `import { isString, isUndefined } from "../utils";` (both exported in Task 1). Call sites at :8 (`isUndefined(value)`) and :16 (`isString(value)`) stay byte-for-byte identical. CRITICAL: native `isUndefined` does NOT match null (preserves the `valueAsDate && isString(value)` branch behavior); the table in RESEARCH confirms equivalence.
    - In src/logic/hasPromiseValidation.ts: remove the lodash import at :2 `import { isFunction, isObject } from 'lodash';`; replace with `import { isFunction, isObject } from "../utils";` (`isFunction` already exported, `isObject` exported in Task 1). Call sites at :11 (`isFunction(fieldReference.validate)`) and :13 (`isObject(fieldReference.validate)`) stay byte-for-byte identical. CRITICAL: `isObject` MUST keep lodash semantics (the `Object.values(fieldReference.validate)` branch only runs when validate is a record of validators — a function would be caught by the `isFunction` branch first, but `isObject` must still return false for null and true for object maps).
  </action>
  <verify>
    <automated>npx tsc --noEmit (exits 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "lodash" src/logic/getFieldValueAs.ts` returns 0 matches
    - `grep -rn "lodash" src/logic/hasPromiseValidation.ts` returns 0 matches
    - `grep -n "from \"../utils\"" src/logic/getFieldValueAs.ts` shows isString and isUndefined imported
    - `grep -n "from \"../utils\"" src/logic/hasPromiseValidation.ts` shows isFunction and isObject imported
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>getFieldValueAs.ts and hasPromiseValidation.ts import native predicates from ../utils; zero lodash imports; tsc clean; behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan swaps lodash predicate utilities for native equivalents in internal validation/dirty-tracking helpers. No new external input path; the predicates operate on values RHF already holds.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.02-01 | Tampering | isObject semantics in getDirtyFields/deepEqual/hasPromiseValidation | mitigate | Use the exact lodash-equivalent `isObject` (function -> true, null -> false). A bare `typeof === "object"` would misclassify and could corrupt dirty-field detection or async-validation detection. Acceptance criteria pin the centralized helper; RESEARCH Pitfall 4 documents the divergence. |
| T-02.02-02 | (none) | lodash removal | accept | Removing a dependency reduces supply-chain surface; introduces no new threat. |
</threat_model>

<verification>
- `grep -rn "from \"lodash\"" src/utils.ts src/validateField.ts src/logic/getDirtyFields.ts src/logic/getFieldValueAs.ts src/logic/hasPromiseValidation.ts` returns 0
- `npx tsc --noEmit` exits 0
- isObject centralized + exported with lodash semantics; isBoolean/isString/isUndefined exported
- Manual (deferred to Plan 08 build gate): no behavior change in validation/dirty detection
</verification>

<success_criteria>
Five of seven lodash call-site files are lodash-free with native predicates centralized in `utils.ts`. Unblocks Plan 05 (Forger isEqual->deepEqual) and Plan 08 (drop lodash from package.json once ALL files are clean). The remaining two lodash files (Forger.tsx, useForgeValues.tsx) are handled in Plans 05 and 07.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stability/02-02-SUMMARY.md`
</output>
