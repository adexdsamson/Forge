---
phase: 02-stability
plan: 06
type: execute
wave: 3
depends_on: ["02-01", "02-05"]
files_modified:
  - src/useFieldArray/useFieldArray.tsx
autonomous: true
requirements: [STAB-01]
must_haves:
  truths:
    - "useFieldArray calls RHF's public useFieldArray for append/remove/insert/swap/update + fields/id; zero control._* access remains"
    - "Forge's per-item inputProps decoration (D-05 keeper) survives — each returned field still carries inputProps"
    - "The unstable [fields, name, control] useEffect (D-07) is deleted entirely"
    - "rules and shouldUnregister still pass through to RHF's useFieldArray"
    - "Post-mutation validation re-homes to RHF rules (built-in, 7.34.0+) or explicit public trigger — no manual validateField/_subjects path"
  artifacts:
    - path: "src/useFieldArray/useFieldArray.tsx"
      provides: "Decorate-on-top field array with per-item input attributes"
      contains: "useFieldArray"
  key_links:
    - from: "src/useFieldArray/useFieldArray.tsx"
      to: "react-hook-form public useFieldArray"
      via: "decorate-on-top: map rhf.fields -> { ...field, inputProps }"
      pattern: "\\.map\\("
---

<objective>
Rewrite `useFieldArray` as a decorate-on-top wrapper over RHF's public `useFieldArray` (D-05/D-06/D-07): RHF owns all mutation (`append`/`remove`/`insert`/`swap`/`update`/`replace`), id generation, focus, and `rules`-based validation; Forge layers its per-item `inputProps` onto the returned `fields` via a `useMemo` map. Every one of the ~36 `control._*` sites is deleted, including the unstable `[fields, name, control]` effect.

Purpose: STAB-01 — remove all RHF private-API access from the field-array hook while keeping the D-05 per-item input-attribute capability.
Output: A `useFieldArray.tsx` with zero `_*` access, RHF-owned mutations, and the `inputProps` keeper intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stability/02-CONTEXT.md
@.planning/phases/02-stability/02-RESEARCH.md
@.planning/phases/02-stability/02-PATTERNS.md
@.planning/phases/02-stability/02-VALIDATION.md
@.planning/phases/02-stability/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decorate-on-top rewrite of useFieldArray (D-05/06/07)</name>
  <files>src/useFieldArray/useFieldArray.tsx</files>
  <read_first>
    - src/useFieldArray/useFieldArray.tsx (full current file — note every `_*` site to delete: `_getFieldArray` :62/:137/:153/:167/:184/:202, `_names.array.add` :73, `register(rules)` :75-79, `_subjects.array` subscribe :86-96, `_setFieldArray`/`_formValues` :99, `_fields`/`_f.mount` :101-112, `(control as any)._updateFieldArray` :124, `_state.action` :213, `_subjects.state.next` :215-218/:247-254, manual `validateField` block :228-256, `_subjects.values.next` :259-262, `_names.focus`/`iterateFieldsByAction` :264-277, the unstable effect dep :280; and the KEEPER per-item map :288-296)
    - .planning/phases/02-stability/02-PATTERNS.md (section "src/useFieldArray/useFieldArray.tsx" — KEEPER pattern, the full DELETE list, Replacement pattern)
    - .planning/phases/02-stability/02-RESEARCH.md (sections "Pattern 1: Decorate-on-top hook (D-06)", "RISK-02 — full `_*` trace for useFieldArray -> public mapping" table, "Open Questions #1" on validate-on-mutate, and "Minimum RHF ^7 floor" confirming `rules` at 7.34.0)
    - .planning/phases/02-stability/02-VALIDATION.md (Manual-Only row for useFieldArray ops)
  </read_first>
  <action>
    Rewrite src/useFieldArray/useFieldArray.tsx to decorate-on-top with ZERO `control._*`:
    - Import RHF's public `useFieldArray` (alias it, e.g. `useFieldArray as useRHFFieldArray`) from `react-hook-form`. Keep the `ForgeFieldArray` props type (`UseFieldArrayProps<...> & { inputProps }`).
    - Call `const rhf = useRHFFieldArray<TFieldValues, TFieldArrayName, TKeyName>({ control, name, keyName, rules: props.rules, shouldUnregister: props.shouldUnregister });` — RHF performs internally everything the deleted `_*` sites did (array read, registration, subject sync, focus, unmount cleanup, mutation commit). DELETE the local `useState(control._getFieldArray)`, the `ids` ref, the `_subjects.array` `useSubscribe`, both `useEffect` blocks (incl. the unstable `[fields, name, control]` effect at :280 — D-07), `updateValues`, and the hand-rolled `append`/`remove`/`insert`/`swap`/`update` bodies. Use RHF's `append`/`remove`/`insert`/`swap`/`update`/`replace` directly.
    - PRESERVE the D-05 keeper: `const fields = React.useMemo(() => rhf.fields.map((field) => ({ ...field, inputProps })), [rhf.fields, inputProps]);` and `return { ...rhf, fields };`.
    - Post-mutation validation: rely on RHF's built-in `rules` validation (auto, 7.34.0+). If parity for validate-on-mutate WITHOUT rules is needed (RESEARCH Open Q #1), wrap the mutation methods to also call public `methods.trigger(name)` — choose the `rules`-first approach and note the choice in SUMMARY. Do NOT re-introduce manual `validateField`/`_subjects`/`_formValues`.
    - Remove now-unused imports (`useSubscribe`, `validateField`, `updateFieldArrayRootError`, `get`, `cloneObject`, `appendAt`, `removeArrayAt`, internal `_*`-only helpers) if no longer referenced.
  </action>
  <verify>
    <automated>npx tsc --noEmit (exits 0) AND grep -c "control\._" src/useFieldArray/useFieldArray.tsx returns 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "control\._" src/useFieldArray/useFieldArray.tsx` returns 0
    - `grep -n "_subjects\|_getFieldArray\|_updateFieldArray\|_setFieldArray\|_names\|_formValues\|_state\.action\|_fields" src/useFieldArray/useFieldArray.tsx` returns 0 matches
    - `grep -n "as any" src/useFieldArray/useFieldArray.tsx` returns 0 matches
    - `grep -n "inputProps" src/useFieldArray/useFieldArray.tsx` confirms the per-item keeper map survives
    - `npx tsc --noEmit` exits 0; `npx rollup -c` exits 0
    - MANDATORY MANUAL (STAB-01, no automated test): append/remove/insert/swap/update each mutate the array, the UI updates, ids stay stable, per-item inputProps are present on each row, and validation re-runs after a mutation (error clears on valid append/remove). Confirm no re-render storm (D-07 effect gone).
  </acceptance_criteria>
  <done>useFieldArray is decorate-on-top with zero `_*`/`as any`, the inputProps keeper intact, the unstable effect deleted; all five mutation ops verified manually; tsc + build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan replaces internal RHF-state orchestration with calls to RHF's own public `useFieldArray`. The data (field-array values) already lives in RHF; no new external input boundary is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.06-01 | Tampering | post-mutation validation path | mitigate | Validation re-homes to RHF's official `rules`/`trigger` path rather than a hand-rolled `validateField` + manual error injection; this reduces the chance of validation drift on RHF updates. Manual gate confirms error-clears-after-mutation. |
| T-02.06-02 | (none) | decorate-on-top rewrite | accept | Delegating to RHF's public hook reduces private-API coupling; no new attack surface. |
</threat_model>

<verification>
- `grep -c "control\._" src/useFieldArray/useFieldArray.tsx` -> 0; no `_subjects`/`_getFieldArray`/etc.; no `as any`
- `inputProps` keeper map present
- `npx tsc --noEmit` exits 0; `npx rollup -c` exits 0
- MANUAL: all five mutations work, inputProps persist, ids stable, validation re-runs, no re-render storm
</verification>

<success_criteria>
`useFieldArray` is fully decorate-on-top with zero `control._*` access, the D-05 per-item input-attribute capability preserved, and the D-07 unstable effect removed. Satisfies STAB-01. Contributes to the Plan 08 package.json `rules`-driven RHF floor (^7.34.0).
</success_criteria>

<output>
After completion, create `.planning/phases/02-stability/02-06-SUMMARY.md`
</output>
