---
phase: 05-docs-ci
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/API.md
  - examples/ReactNativeExample.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "D-01: docs/API.md exists with prop/return tables for useForge, Forge, Forger, useFieldArray, useForgeValues, usePersist, validateField, and platform-detection utilities"
    - "D-02: README.md is a full rewrite — hero description, badges, install, quickstart, RN variant, key concepts, links to docs/API.md and examples/"
    - "D-03: README.md opens with npm-version, CI-status, and license badges (shields.io)"
    - "D-04: Quickstart shows a signup-style form (text + select + checkbox) for both web and RN using useForge -> <Forge control={...}> -> <Forger>-wraps-custom-component with validation and real submit"
    - "D-05: examples/ReactNativeExample.md refreshed to current API with @adexdsamson/forge imports; linked from README"
    - "All import examples use @adexdsamson/forge (not ./lib/forge or ../index)"
    - "README install section shows peer deps: react >=18 and react-hook-form ^7.x"
    - "npm run typecheck exits 0 after README and docs authoring (no new TS errors introduced)"
  artifacts:
    - path: "README.md"
      provides: "OSS landing page with install + quickstart + RN variant (D-02/D-03/D-04)"
      contains: "@adexdsamson/forge"
    - path: "docs/API.md"
      provides: "API reference tables for all public exports (D-01)"
      contains: "useForge"
    - path: "examples/ReactNativeExample.md"
      provides: "Refreshed RN example with correct imports (D-05)"
      contains: "@adexdsamson/forge"
  key_links:
    - from: "README.md"
      to: "docs/API.md"
      via: "## API Reference section with link"
      pattern: "docs/API.md"
    - from: "README.md"
      to: "examples/ReactNativeExample.md"
      via: "## Examples section with link"
      pattern: "ReactNativeExample"
---

<objective>
Write the README from scratch as a polished OSS landing page, create docs/API.md with prop/return reference tables for every public export, and refresh examples/ReactNativeExample.md to the current API.

Purpose: DOCS-01 requires that following the README on a fresh project (web OR React Native) produces a working validated form. DOCS-02 requires a complete API reference covering all exported symbols. Both are acceptance bars for Phase 5.

Output: README.md (full rewrite), docs/API.md (new file), examples/ReactNativeExample.md (refreshed).
</objective>

<execution_context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-RESEARCH.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-PATTERNS.md
</execution_context>

<context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\PROJECT.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\ROADMAP.md

<interfaces>
<!-- Extracted from src/index.ts — the authoritative public export list. -->
<!-- Every symbol below MUST appear in docs/API.md. -->

From src/index.ts (public exports):
  Components:    Forge, Forger
  Hooks:         useForge, usePersist, useFieldArray, useForgeValues
  Types (from src/types.ts):
    ForgeControl, ForgeProps, ForgerProps, ForgerControllerProps, ForgerSlotProps,
    TForgerProps, FieldProps, UseForgeProps, UseForgeResult, ReactNativeInputProps,
    PlatformSpecificProps, CrossPlatformForgerProps, FormPropsRef
  From src/reactNative.ts:
    REACT_NATIVE_COMPONENTS, getEventHandlerName, getValuePropertyName,
    setReactNativeError, getComponentType, mergePlatformProps,
    REACT_NATIVE_VALIDATION_RULES, handleReactNativeFile, getPlatform,
    isValidReactNativeComponent
  From src/utils.ts:
    isWeb, isReactNative, isMobile, isTextInput, isCheckBoxInput,
    isRadioInput, isPicker, isSwitch, isSlider

From src/types.ts key shapes (executor must read file to get exact signatures):
  UseForgeProps<TFieldProps, TFieldValues> — see RESEARCH.md §API.md Documentation Surface
  useForge returns UseForgeResult<TFieldValues> = RHF UseFormReturn + ForgeControl augmentation
  ForgeProps<TFieldValues, TFieldProps> includes: control, onSubmit, className, noValidate,
    debug, isWizard, platform, ref, children, fieldProps
  ForgerProps<TFieldValues> includes: name, component, rules, transform, handler,
    dependencies, plus spread HTML input attrs
  usePersist handler signature: (values, { isDirty, isValid }) — see Phase 2 D-12 in STATE.md

From package.json (peer dependencies):
  "react": ">=18", "react-hook-form": "^7.34.0"

Package scope: @adexdsamson/forge
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/API.md — prop/return reference tables</name>
  <files>docs/API.md</files>

  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\src\index.ts — the authoritative export list; document only what is exported here
    - C:\Users\HomePC\Documents\GitHub\Forge\src\types.ts — UseForgeProps, UseForgeResult, ForgeProps, ForgerProps, FieldProps, transform types, RN types
    - C:\Users\HomePC\Documents\GitHub\Forge\src\useForge\useForge.tsx — useForge signature and augmented control shape
    - C:\Users\HomePC\Documents\GitHub\Forge\src\Forge\Forge.tsx — Forge component props (incl. wizard, fieldProps, debug, platform)
    - C:\Users\HomePC\Documents\GitHub\Forge\src\Forger\Forger.tsx — Forger props, transform.input/output, event wiring
    - C:\Users\HomePC\Documents\GitHub\Forge\src\useFieldArray\useFieldArray.tsx — hook signature + return shape
    - C:\Users\HomePC\Documents\GitHub\Forge\src\useForgeValues\useForgeValues.tsx — getValue/setValue/getValues signatures
    - C:\Users\HomePC\Documents\GitHub\Forge\src\usePersist\usePersist.tsx — handler signature (values, { isDirty, isValid })
    - C:\Users\HomePC\Documents\GitHub\Forge\src\validateField.ts — signature + supported rules
    - C:\Users\HomePC\Documents\GitHub\Forge\src\reactNative.ts — exported platform helpers
    - C:\Users\HomePC\Documents\GitHub\Forge\src\utils.ts — exported platform booleans
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-RESEARCH.md §API.md Documentation Surface — complete list of symbols and their source files
  </read_first>

  <action>
    Create the directory docs/ at the repo root (C:\Users\HomePC\Documents\GitHub\Forge\docs\), then create docs/API.md.

    The file must contain one section per public API group. Read each source file listed in read_first to extract accurate prop names, types, and descriptions before writing. Do NOT document symbols that are not re-exported from src/index.ts.

    Required sections (D-01):
    1. useForge — Props table (UseForgeProps columns: prop name, type, required, default, description) and Returns table (UseForgeResult members + augmented ForgeControl fields like hasFields, fields[], wizard navigation)
    2. Forge — Props table (ForgeProps columns: prop, type, required, default, description)
    3. Forger — Props table (ForgerProps columns including name, component, rules, transform, handler, dependencies) and transform subsection (input/output types)
    4. useFieldArray — Parameters table + Returns table (append, remove, insert, swap, update + fields array)
    5. useForgeValues — Parameters + Returns table (getValue, setValue, getValues)
    6. usePersist — Parameters + Returns table; note the handler signature (values, { isDirty, isValid })
    7. validateField — Parameters + Returns table; list all supported validation rules (required, min, max, maxLength, minLength, pattern, validate)
    8. Platform Detection Utilities — two sub-tables: (a) Platform booleans from utils.ts (isWeb, isReactNative, isMobile, isTextInput, isCheckBoxInput, isRadioInput, isPicker, isSwitch, isSlider), (b) RN helpers from reactNative.ts (getEventHandlerName, getValuePropertyName, getComponentType, mergePlatformProps, getPlatform, isValidReactNativeComponent, setReactNativeError, handleReactNativeFile, REACT_NATIVE_COMPONENTS constant, REACT_NATIVE_VALIDATION_RULES constant)

    Table format (use standard markdown tables):
      | Prop | Type | Required | Default | Description |

    At the top of docs/API.md, add a link back to the README and a note that the canonical install is @adexdsamson/forge.

    Do NOT add TypeScript code blocks containing incorrect types — if a type is unclear from source, use the actual type from the source file or use a descriptive string like "See UseForgeProps".
  </action>

  <verify>
    <automated>cd "C:/Users/HomePC/Documents/GitHub/Forge" && node -e "const fs = require('fs'); const content = fs.readFileSync('docs/API.md', 'utf8'); const symbols = ['useForge','Forge','Forger','useFieldArray','useForgeValues','usePersist','validateField','isWeb','isReactNative','getEventHandlerName']; const missing = symbols.filter(s => !content.includes(s)); console.log('missing:', missing.length === 0 ? 'none' : missing)"</automated>
  </verify>

  <acceptance_criteria>
    - docs/API.md exists
    - docs/API.md contains sections for all 8 required API groups from D-01: useForge, Forge, Forger, useFieldArray, useForgeValues, usePersist, validateField, platform-detection utilities
    - Every symbol exported from src/index.ts appears in at least one table row in docs/API.md (cross-check against src/index.ts export list)
    - The file contains markdown tables with pipe syntax (not just prose)
    - No symbol documented that is NOT in src/index.ts exports
    - usePersist handler signature shows (values, { isDirty, isValid }) — the Phase 2 breaking change from D-12 in STATE.md
    - validateField section lists at minimum: required, min, max, maxLength, minLength, pattern, validate rules
  </acceptance_criteria>

  <done>docs/API.md exists with a section and prop/return table for every public export group. Cross-check against src/index.ts confirms full coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md and refresh examples/ReactNativeExample.md</name>
  <files>README.md, examples/ReactNativeExample.md</files>

  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\README.md — read the full current file to understand what stale content to eliminate (Swifter blurb ~L363, placeholder install ~L17-19, ./lib/forge imports throughout, raw input instead of Forger ~L43-45)
    - C:\Users\HomePC\Documents\GitHub\Forge\examples\ReactNativeExample.md — read fully; find the stale '../index' import (line 19); audit all prop names against current API
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-PATTERNS.md §README.md, §examples/ReactNativeExample.md — exact change lists
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-RESEARCH.md §Housekeeping Cleanups — exact strings to verify gone after this task
    - C:\Users\HomePC\Documents\GitHub\Forge\src\types.ts — to confirm current ForgerProps/UseForgeProps shape for quickstart code accuracy
    - C:\Users\HomePC\Documents\GitHub\Forge\package.json — for exact version badge URL construction (version field, repository.url)
  </read_first>

  <action>
    PART A — Rewrite README.md (full replacement, per D-02):

    Structure (in order, per PATTERNS.md §README.md target structure):
    1. Hero: "# Forge" heading + one-paragraph description of what Forge is (cross-platform React form library wrapping react-hook-form, web + RN, useForge + Forge + Forger API)
    2. Badges row (D-03): three shields.io badges using img tags or markdown badge syntax:
       - npm version: https://img.shields.io/npm/v/@adexdsamson/forge.svg linking to https://www.npmjs.com/package/@adexdsamson/forge
       - CI status: https://img.shields.io/github/actions/workflow/status/adexdsamson/Forge/ci.yml.svg linking to the Actions tab
       - License: https://img.shields.io/npm/l/@adexdsamson/forge.svg
    3. ## Installation section:
       npm install @adexdsamson/forge
       Show peer deps separately: npm install react react-hook-form
       Note react-dropzone as optional peer for file inputs
    4. ## Quickstart (Web) section (D-04):
       A realistic signup form with text (name), select (role), and checkbox (acceptTerms) fields.
       Must show: useForge({ defaultValues, resolver? }) call, <Forge control={...} onSubmit={onSubmit}> wrapper,
       three <Forger name="..." component={CustomInput} rules={{...}}> usages wrapping custom components,
       and an onSubmit handler receiving typed form data.
       All imports from '@adexdsamson/forge'. Custom component is a simple wrapper (can be inline example).
       The code MUST compile against the current API — types must be real (no made-up props).
    5. ## React Native section (D-04):
       Same signup form adapted for RN: same useForge call, same <Forge control> wrapper,
       but the custom components are RN components (TextInput from react-native, Picker, Switch or CheckBox equivalent).
       Show that the import is still 'from "@adexdsamson/forge"' — the same surface works cross-platform.
       A note explaining Forge auto-detects the platform and wires the right event handlers.
    6. ## Key Concepts section:
       Three bullets: useForge (configures + returns control), Forge (renders form + wires submit/wizard),
       Forger (connects one field to RHF state via useController + applies transforms).
    7. ## API Reference section:
       "Full prop/return tables: [docs/API.md](docs/API.md)"
    8. ## Examples section:
       Link to examples/ReactNativeExample.md for a complete runnable RN example.
    9. ## License section:
       "MIT © adexdsamson" — NO "part of the Swifter project" text.

    Stale content that MUST NOT appear anywhere in the new README:
    - "part of the Swifter project" (any variation)
    - "The forge library is included in your project"
    - "'./lib/forge'" imports
    - "__tests__/" references
    - raw <input> elements in place of <Forger> wrappers

    PART B — Refresh examples/ReactNativeExample.md (targeted fixes, per PATTERNS.md):
    1. Replace the stale import on line 19: "from '../index'" → "from '@adexdsamson/forge'"
    2. Audit all prop names in the example against the current API in src/types.ts — fix any that have changed (e.g., handler, transform, dependencies)
    3. Verify there are no references to './lib/forge' or '../index' remaining in the file
    4. Do NOT fold this file into README — keep it as a standalone example (D-05)
  </action>

  <verify>
    <automated>cd "C:/Users/HomePC/Documents/GitHub/Forge" && node -e "const fs = require('fs'); const readme = fs.readFileSync('README.md', 'utf8'); const rn = fs.readFileSync('examples/ReactNativeExample.md', 'utf8'); const checks = {'readme no Swifter': !readme.includes('Swifter'), 'readme no lib/forge': !readme.includes('./lib/forge'), 'readme no placeholder install': !readme.includes('is included in your project'), 'readme has scope': readme.includes('@adexdsamson/forge'), 'readme has API.md link': readme.includes('docs/API.md'), 'readme has badges': readme.includes('shields.io'), 'rn no stale import': !rn.includes('../index'), 'rn has scope': rn.includes('@adexdsamson/forge')}; const failures = Object.entries(checks).filter(([k,v]) => !v).map(([k]) => k); console.log(failures.length === 0 ? 'ALL PASS' : 'FAILURES: ' + failures.join(', '))"</automated>
  </verify>

  <acceptance_criteria>
    - README.md does NOT contain: "Swifter", "./lib/forge", "is included in your project", "__tests__/", raw "<input" elements in form examples
    - README.md contains: "@adexdsamson/forge", "shields.io" badge URLs, a "## Installation" section showing npm install with peer deps, a quickstart with text + select + checkbox fields, "## React Native" section, link to "docs/API.md", link to "ReactNativeExample"
    - README.md quickstart imports use "from '@adexdsamson/forge'"
    - README.md shows <Forge control={...}> wrapper and <Forger name="..." component={...}> pattern (not raw <input>)
    - examples/ReactNativeExample.md does NOT contain "../index"
    - examples/ReactNativeExample.md contains "@adexdsamson/forge"
    - npm run typecheck exits 0 (docs changes do not affect TS compilation)
  </acceptance_criteria>

  <done>README.md is a complete OSS landing page with web + RN quickstart examples, badges, correct imports, link to docs/API.md and examples/. examples/ReactNativeExample.md imports from @adexdsamson/forge. No stale content remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc authoring → repo files | Static markdown documentation; no runtime code, no secrets, no network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | README.md badges | accept | Shields.io badge URLs are public metadata; no secrets embedded |
| T-05-02-02 | Tampering | docs/API.md inaccuracy | accept | Incorrect docs are a usability problem, not a security threat; verified against src/index.ts |

No new attack surface introduced — this plan produces only static markdown documentation files. No secrets, no runtime code, no configuration that affects security posture.
</threat_model>

<verification>
End-of-plan checks:

1. Stale content gone: `grep -rn "Swifter\|lib/forge\|is included in your project\|\.\./index" . --include="*.md"` — expect 0 results
2. docs/API.md coverage: manually cross-check every symbol in src/index.ts exports against docs/API.md
3. `npm run typecheck` exits 0
4. README badges render correctly: verify badge URLs contain valid shields.io paths for @adexdsamson/forge
5. README quickstart code uses real current API props (cross-check against src/types.ts UseForgeProps/ForgerProps shapes)
</verification>

<success_criteria>
- Following the README install + quickstart on a fresh project (web path) produces a working form using the real useForge + Forge + Forger API (DOCS-01)
- The React Native quickstart section uses the same useForge/Forge/Forger surface with RN components and a note about cross-platform runtime detection (DOCS-01)
- docs/API.md contains prop/return tables for all 8 API groups from D-01 (DOCS-02)
- No file in the changed set references Swifter, placeholder install text, ./lib/forge, or ../index
</success_criteria>

<output>
After completion, create C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-02-SUMMARY.md
</output>
