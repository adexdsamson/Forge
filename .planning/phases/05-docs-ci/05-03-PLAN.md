---
phase: 05-docs-ci
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - MIGRATION.md
  - CHANGELOG.md
  - LICENSE
  - .versionrc.json
autonomous: true
requirements:
  - DOCS-03

must_haves:
  truths:
    - "D-15: CHANGELOG.md exists and was generated with commit-and-tag-version; a changelog script exists in package.json"
    - "D-16: CHANGELOG.md initial entry is under ## [Unreleased] (not a version number + date header); content is curated to user-facing changes only"
    - "D-17: MIGRATION.md is repurposed as 'migrating from raw react-hook-form to Forge'; dead __tests__/ReactNativeForm.test.tsx reference at line 346 is removed"
    - "No file in the repo contains the string 'Swifter' in any .md file"
    - "No .md file contains '__tests__/' as a path reference"
    - "LICENSE reflects correct MIT text for @adexdsamson/forge (verified, no changes needed per RESEARCH.md)"
    - ".versionrc.json exists with skip bump/tag/commit flags so Phase 5 changelog generation does not touch package.json version (Phase 6 owns version)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Conventional-changelog history under [Unreleased] (D-15/D-16)"
      contains: "[Unreleased]"
    - path: "MIGRATION.md"
      provides: "RHF-to-Forge migration guide for new public users (D-17)"
      contains: "@adexdsamson/forge"
    - path: ".versionrc.json"
      provides: "commit-and-tag-version skip config for Phase 5 (D-16)"
      contains: "bump"
  key_links:
    - from: "CHANGELOG.md"
      to: "conventional commits in git history"
      via: "commit-and-tag-version reads git log"
      pattern: "[Unreleased]"
    - from: "MIGRATION.md"
      to: "src/index.ts exports"
      via: "before/after examples must use real API"
      pattern: "@adexdsamson/forge"
---

<objective>
Correct the three housekeeping files: repurpose MIGRATION.md for the public RHF-to-Forge audience, generate CHANGELOG.md with commit-and-tag-version and curate the initial [Unreleased] entry, and verify LICENSE is correct. Also create .versionrc.json so the changelog script does not accidentally bump the package version.

Purpose: DOCS-03 requires that no file references "Swifter project", placeholder install text, or a dead __tests__/ path; that CHANGELOG.md exists with at least one entry; and that LICENSE is correct. These are acceptance-bar items in the ROADMAP Phase 5 success criteria.

Output: MIGRATION.md (repurposed), CHANGELOG.md (generated + curated), .versionrc.json (Phase 5 skip config).
</objective>

<execution_context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-RESEARCH.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-PATTERNS.md
</execution_context>

<context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\PROJECT.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\ROADMAP.md

<interfaces>
<!-- Key facts extracted from source artifacts. -->

From RESEARCH.md §Housekeeping Cleanups:
  Exact strings verified to exist in current files:
  - README.md L363: "This library is part of the Swifter project and follows the same licensing terms."
    (Plan 02 removes this via full README rewrite — Plan 03 grep checks all OTHER .md files)
  - MIGRATION.md L346: dead __tests__/ReactNativeForm.test.tsx reference in a code block
  - examples/ReactNativeExample.md L19: "../index" import (Plan 02 fixes)

  LICENSE: already correct MIT text "Copyright (c) 2026 adexdsamson" — no changes needed [VERIFIED per RESEARCH.md]

From RESEARCH.md §Pattern 6 (commit-and-tag-version CHANGELOG generation):
  Command: npx commit-and-tag-version --skip.bump --skip.tag --skip.commit
  Curation wrinkle: no prior git tags → tool sweeps ALL history; generated header will be
    "## [1.0.3] - 2026-XX-XX" — must be renamed to "## [Unreleased]"
  The generated list will include 30+ internal planning/chore commits — must be pruned

From RESEARCH.md .versionrc.json:
  { "skip": { "bump": true, "tag": true, "commit": true } }
  If this file exists, the "changelog" npm script simplifies to just "commit-and-tag-version"

From PATTERNS.md §MIGRATION.md target framing (D-17):
  Title: "Migration Guide: From react-hook-form to Forge"
  Audience: developer already using raw RHF who wants to adopt Forge
  Content: side-by-side before/after for useForm→useForge, Controller→Forger, handleSubmit, field arrays
  All imports: '@adexdsamson/forge'
  No __tests__/ references anywhere

Package scope: @adexdsamson/forge
Current package.json version: 1.0.3 (Phase 6 will bump — do NOT touch version here)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .versionrc.json and generate + curate CHANGELOG.md</name>
  <files>CHANGELOG.md, .versionrc.json</files>

  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\package.json — confirm commit-and-tag-version is in devDependencies (installed by Plan 01 Task 1); if Plan 01 has not run yet, install it manually with npm install --save-dev commit-and-tag-version@^12.7.3
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-RESEARCH.md §Pattern 6 — exact commit-and-tag-version invocation + curation steps
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-PATTERNS.md §CHANGELOG.md — generation + curation two-step
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-RESEARCH.md §Pitfall 2 — first-run sweeps ALL commits; curation is mandatory
  </read_first>

  <action>
    STEP 1 — Create .versionrc.json at repo root (C:\Users\HomePC\Documents\GitHub\Forge):
    Content: { "skip": { "bump": true, "tag": true, "commit": true } }
    This makes the "changelog" npm script (added in Plan 01) safe to run without touching the version.
    IMPORTANT: .versionrc.json must be removed or updated in Phase 6 before running the real release.
    Add a comment line at the top of the JSON — wait, JSON does not support comments. Instead, document in the file only that this is Phase-5 specific by keeping it minimal (just the skip object above).

    STEP 2 — Generate CHANGELOG.md:
    Run from repo root: npx commit-and-tag-version --skip.bump --skip.tag --skip.commit
    (Or: npm run changelog if Plan 01 has added that script)
    This creates CHANGELOG.md. Do NOT run without the skip flags — that would bump package.json version.

    STEP 3 — Curate CHANGELOG.md:
    Open the generated CHANGELOG.md and make these manual edits:
    a) The generated section header will be something like "## [1.0.3] - 2026-05-31" — rename it to "## [Unreleased]" (remove version number and date entirely per D-16)
    b) Review the full commit list. Keep entries that are user-facing (type: feat, fix, perf, refactor with user impact). Remove entries that are internal noise (type: docs, chore, ci, test, build, internal migration commits, planning workflow commits like "docs(state):", "docs(phase-XX):")
    c) Ensure at least one entry remains under ## [Unreleased]. If all commits were internal and would be pruned, keep a single curated summary entry such as "* **feat:** initial open-source release — useForge, Forge, Forger, useFieldArray, useForgeValues, usePersist, validateField, cross-platform web + React Native"
    d) Add a brief header at the top of the file: "# Changelog\n\nAll notable changes will be documented here.\n\nFormat based on [Keep a Changelog](https://keepachangelog.com/).\n"
    e) The file must end with a newline

    IMPORTANT: Do NOT let commit-and-tag-version create a git commit or tag (the skip flags prevent this). Do NOT run git add / git commit for CHANGELOG.md independently — the GSD workflow handles committing.
    IMPORTANT: package.json version must remain 1.0.3 after this task. Check with: node -e "console.log(require('./package.json').version)"
  </action>

  <verify>
    <automated>cd "C:/Users/HomePC/Documents/GitHub/Forge" && node -e "const fs = require('fs'); const cl = fs.readFileSync('CHANGELOG.md', 'utf8'); const vr = JSON.parse(fs.readFileSync('.versionrc.json', 'utf8')); const version = require('./package.json').version; console.log({has_unreleased: cl.includes('[Unreleased]'), no_version_header: !cl.includes('[1.0.3]'), versionrc_skip_bump: vr.skip && vr.skip.bump === true, package_version_unchanged: version === '1.0.3'})"</automated>
  </verify>

  <acceptance_criteria>
    - CHANGELOG.md exists at repo root
    - CHANGELOG.md contains "## [Unreleased]" (NOT "## [1.0.3]" or any other semver header)
    - CHANGELOG.md has at least one entry under ## [Unreleased]
    - CHANGELOG.md begins with a "# Changelog" header
    - .versionrc.json exists with skip.bump = true, skip.tag = true, skip.commit = true
    - package.json version field is still "1.0.3" (commit-and-tag-version did NOT bump it)
    - No git tags were created (run: git tag -l — must return empty or same as before)
  </acceptance_criteria>

  <done>CHANGELOG.md exists with curated [Unreleased] entry. .versionrc.json prevents accidental version bump. package.json version unchanged at 1.0.3.</done>
</task>

<task type="auto">
  <name>Task 2: Repurpose MIGRATION.md for RHF-to-Forge public audience</name>
  <files>MIGRATION.md</files>

  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\MIGRATION.md — read the FULL current file to understand existing content; find the dead __tests__/ reference (around line 346); note current framing to understand what must be replaced
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-PATTERNS.md §MIGRATION.md — exact target framing (D-17), content requirements, dead reference to remove
    - C:\Users\HomePC\Documents\GitHub\Forge\src\types.ts — for accurate prop names in before/after examples
    - C:\Users\HomePC\Documents\GitHub\Forge\src\useForge\useForge.tsx — for useForge signature in the "after" examples
    - C:\Users\HomePC\Documents\GitHub\Forge\src\Forger\Forger.tsx — for Forger props in the "after" examples
  </read_first>

  <action>
    Rewrite MIGRATION.md with the framing from D-17: "Migration Guide: From react-hook-form to Forge".

    Target audience: An external developer who already uses react-hook-form directly and wants to understand the migration path to Forge. This is NOT an internal Forge-to-RN guide.

    Required sections:
    1. Title: "# Migration Guide: From react-hook-form to Forge"
    2. Brief intro: what Forge is and why you'd migrate (thinner API, cross-platform, same RHF under the hood)
    3. ## Installation
       npm install @adexdsamson/forge
       peer deps: react react-hook-form (already in your project)
    4. ## useForm → useForge
       Side-by-side before (useForm) and after (useForge) code blocks.
       Show: useForm({ defaultValues }) → useForge({ defaultValues }); control augmented with hasFields etc.
       Note: useForge returns the full RHF UseFormReturn plus the augmented ForgeControl.
    5. ## Rendering Fields: Controller → Forger
       Before: <Controller name="email" control={control} render={({ field }) => <input {...field} />} />
       After: <Forge control={control}><Forger name="email" component={TextInput} /></Forge>
       Explain: Forger wraps useController internally; no render prop needed; component receives value/onChange.
    6. ## Form Submit: handleSubmit → Forge onSubmit
       Before: <form onSubmit={handleSubmit(onSubmit)}> ...
       After: <Forge control={control} onSubmit={onSubmit}> ...
       Explain: Forge renders a real <form> on web and wires submit automatically.
    7. ## Field Arrays
       Brief section showing useFieldArray from Forge (which wraps RHF's useFieldArray, adds per-item inputProps preservation).
    8. ## Transform (optional)
       Brief: Forger transform.input / transform.output for value coercion.
    9. ## React Native
       Note: the same useForge / Forge / Forger surface works cross-platform via runtime detection.
       No hard 'react-native' import in Forge itself.
    10. ## What Does NOT Change
        List: resolver configuration (Zod/Yup pass through useForge), formState (same as RHF), watch/setValue/getValues (available on control).

    Absolute requirements:
    - NO reference to __tests__/ anywhere in the file (the dead code block around line 346 in the original must NOT appear)
    - NO reference to "Swifter project" or internal orbipayx framing
    - All import examples use '@adexdsamson/forge'
    - All code examples use the real current API surface (read src/types.ts for accurate prop names)
    - No __tests__/, no '../index', no './lib/forge' path references
  </action>

  <verify>
    <automated>cd "C:/Users/HomePC/Documents/GitHub/Forge" && node -e "const fs = require('fs'); const m = fs.readFileSync('MIGRATION.md', 'utf8'); const checks = {'no __tests__': !m.includes('__tests__'), 'no Swifter': !m.includes('Swifter'), 'has scope import': m.includes('@adexdsamson/forge'), 'correct title': m.includes('From react-hook-form to Forge'), 'has Controller section': m.includes('Forger'), 'no stale import': !m.includes('../index') && !m.includes('./lib/forge')}; const failures = Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k); console.log(failures.length===0 ? 'ALL PASS' : 'FAIL: '+failures.join(', '))"</automated>
  </verify>

  <acceptance_criteria>
    - MIGRATION.md title is "# Migration Guide: From react-hook-form to Forge" (or equivalent public-audience framing)
    - MIGRATION.md does NOT contain "__tests__/" anywhere (the dead reference at original line 346 is gone)
    - MIGRATION.md does NOT contain "Swifter"
    - MIGRATION.md contains "@adexdsamson/forge" in import examples
    - MIGRATION.md contains a side-by-side Controller → Forger migration example
    - MIGRATION.md does NOT contain "../index" or "./lib/forge"
    - Repo-wide grep for "__tests__/" in .md files returns 0 results: grep -rn "__tests__" . --include="*.md" (expect 0)
    - Repo-wide grep for "Swifter" in .md files returns 0 results: grep -rn "Swifter" . --include="*.md" (expect 0)
  </acceptance_criteria>

  <done>MIGRATION.md repurposed for public RHF-to-Forge migration audience. All dead references removed. Repo-wide grep confirms no remaining Swifter or __tests__/ references in any .md file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → CHANGELOG.md | commit-and-tag-version reads local git log; no external network calls |
| .versionrc.json → npm lifecycle | config file controls skip flags; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Elevation of Privilege | commit-and-tag-version version bump | mitigate | .versionrc.json skip.bump=true prevents version change; verify package.json version = 1.0.3 after run |
| T-05-03-02 | Tampering | .versionrc.json skip flags removed in Phase 6 | accept | Documented in task action; Phase 6 planner must address; low risk — Phase 6 explicitly owns version bump |
| T-05-03-03 | Information Disclosure | CHANGELOG.md exposes git commit messages | accept | All commits are OSS-public work; no PII or secrets in commit messages |

No new attack surface introduced — static docs + dev-only CHANGELOG tooling config. No secrets handled.
</threat_model>

<verification>
End-of-plan checks:

1. Swifter strings gone repo-wide: `grep -rn "Swifter" . --include="*.md"` — expect 0 results
2. Dead __tests__/ refs gone: `grep -rn "__tests__" . --include="*.md"` — expect 0 results  
3. CHANGELOG.md header check: `grep -n "Unreleased\|^\#\# \[" CHANGELOG.md` — first entry must be [Unreleased]
4. Package version unchanged: `node -e "console.log(require('./package.json').version)"` — must print 1.0.3
5. .versionrc.json skip flags: `node -e "console.log(require('./.versionrc.json').skip)"` — must show {bump:true, tag:true, commit:true}
6. No git tags created: `git tag -l` — must return empty
</verification>

<success_criteria>
- CHANGELOG.md exists with ## [Unreleased] as the first entry (D-15/D-16)
- MIGRATION.md repurposed for public RHF-to-Forge audience with no dead __tests__/ reference (D-17)
- LICENSE exists and is correct MIT text (verified correct by RESEARCH.md — no edit needed; confirm it exists)
- Repo-wide: zero .md files contain "Swifter", "is included in your project", or "__tests__/" path references (DOCS-03)
- package.json version is still 1.0.3 (Phase 6 owns the version bump)
</success_criteria>

<output>
After completion, create C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\05-docs-ci\05-03-SUMMARY.md
</output>
