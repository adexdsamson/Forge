---
phase: 03-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - package.json
autonomous: true
requirements:
  - TEST-01
  - TEST-04

must_haves:
  truths:
    - "npm test (vitest run --coverage) runs without missing-dependency errors"
    - "vitest.config.ts contains a coverage block with provider v8 and thresholds set to 0 (placeholder for Wave 3 measurement)"
    - "package.json devDependencies contains @vitest/coverage-v8 ^4.1.7"
    - "package.json scripts.test is vitest run --coverage"
    - "npm install completes successfully with updated lockfile"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Coverage config block (provider, thresholds placeholder, exclude list)"
      contains: "coverage"
    - path: "package.json"
      provides: "New devDependency @vitest/coverage-v8, updated test script"
      contains: "@vitest/coverage-v8"
  key_links:
    - from: "package.json scripts.test"
      to: "vitest.config.ts coverage block"
      via: "--coverage flag"
      pattern: "vitest run --coverage"
    - from: "vitest.config.ts coverage.provider"
      to: "@vitest/coverage-v8 package"
      via: "npm devDependency"
      pattern: "v8"
---

<objective>
Install @vitest/coverage-v8, wire the --coverage flag into the test script, and add the coverage config block to vitest.config.ts with placeholder thresholds (0). This unlocks coverage reporting for the tests written in Plans 02 and 03. The real thresholds are set in Plan 04 after measuring.

Purpose: TEST-04 requires npm test to exit non-zero when coverage falls below threshold. Vitest 4.x exits with code 1 on threshold violation but requires @vitest/coverage-v8 to be installed and the --coverage flag present in the test script. Neither currently exists.
Output: Updated vitest.config.ts (coverage block), updated package.json (devDep + script), updated package-lock.json.
</objective>

<execution_context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-RESEARCH.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-PATTERNS.md
</execution_context>

<context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\PROJECT.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\ROADMAP.md

<interfaces>
From vitest.config.ts (current — lines 1-9):
The file exports a single defineConfig call with test.environment="jsdom", test.globals=true, test.setupFiles=["./vitest.setup.ts"]. A coverage block must be added inside test: {}.

From package.json (current — lines 15-21):
scripts.test = "vitest run" → must become "vitest run --coverage".
devDependencies does not contain @vitest/coverage-v8 → must be added at "^4.1.7".

Coverage config shape (from RESEARCH.md Coverage Configuration section):
  provider: "v8"
  thresholds: { lines: 0, functions: 0, statements: 0, branches: 0 }  (placeholder; Plan 04 sets real values)
  exclude: ["**/index.ts", "src/types.ts", "**/*.test.*", "**/*.test.tsx", "rollup.config.mjs", "src/test-utils.tsx"]

Note: src/test-utils.tsx is added to exclude because Plan 02 creates it as a shared test helper — it contains no runtime code and must not inflate coverage numbers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @vitest/coverage-v8 and wire --coverage into test script</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\package.json (read the full devDependencies and scripts blocks before editing)
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-RESEARCH.md §"New Dependency" and §"Scripts Update" for exact version and script value
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-PATTERNS.md §package.json for current scripts block (lines 551-561)
  </read_first>
  <action>
    Add "@vitest/coverage-v8": "^4.1.7" to the devDependencies object in package.json (must match installed vitest version "^4.1.7" exactly — RESEARCH.md §New Dependency confirms this is required; mismatched versions print a peer-dep warning).

    Update scripts.test from "vitest run" to "vitest run --coverage". Leave scripts.test:watch as "vitest" (no --coverage on watch — avoid watch-mode threshold blocks).

    Then run: npm install

    This updates package-lock.json with the new devDependency. Confirm the install exits 0.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const ok=p.devDependencies['@vitest/coverage-v8'] && p.scripts.test==='vitest run --coverage'; process.exit(ok?0:1)" && npm ls @vitest/coverage-v8 --depth=0</automated>
  </verify>
  <done>
    package.json devDependencies contains "@vitest/coverage-v8": "^4.1.7"; scripts.test equals "vitest run --coverage"; npm install exits 0; npm ls @vitest/coverage-v8 shows it installed at 4.x.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add coverage block to vitest.config.ts (placeholder thresholds)</name>
  <files>vitest.config.ts</files>
  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\vitest.config.ts (read current 9-line file before editing)
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-PATTERNS.md §vitest.config.ts for the target shape
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-RESEARCH.md §"Config Shape (Vitest 4.x)" for the exact coverage block keys
  </read_first>
  <action>
    Add a coverage block inside the test: {} object. The block must specify:
      provider: "v8"
      thresholds: { lines: 0, functions: 0, statements: 0, branches: 0 }
    Set all threshold values to 0 as a placeholder. RISK-T2 requires measuring actual coverage after all tests are written (Plan 04) before setting real values. Setting non-zero values now would cause npm test to fail during Plans 02-03 while tests are being written.

    The exclude list must contain: "**/index.ts", "src/types.ts", "**/*.test.*", "**/*.test.tsx", "rollup.config.mjs", "src/test-utils.tsx".

    Add a comment above the thresholds block: "// Thresholds set in Plan 04 after measuring achieved coverage (RISK-T2 / TEST-04 / D-05)."

    Do NOT change environment, globals, or setupFiles — they are correct as-is from Phase 2.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | head -20</automated>
  </verify>
  <done>
    vitest.config.ts contains a coverage block with provider "v8", thresholds all 0, and the exclude list. Running npx vitest run completes without configuration errors and the 3 existing tests in Forge.submit.test.tsx still pass (green). The --coverage flag now works: npx vitest run --coverage exits 0 (thresholds are 0 so no violation).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → test tooling | package.json scripts are developer-invoked in local + CI environments; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | vitest.config.ts coverage.thresholds | accept | Thresholds are set to 0 in this plan intentionally (placeholder); Plan 04 locks real values. A malicious actor lowering thresholds in config could mask regressions — accepted for this plan; the final non-zero thresholds in Plan 04 constitute the mitigation. |
| T-03-01-02 | Information Disclosure | coverage output (lcov, json) | accept | Coverage reports are dev-only artifacts; no PII; not published via package.json files field (which lists only "dist"). No action needed. |

Note: This plan touches only devDependencies and test configuration. It introduces no new runtime attack surface. The published package artifact (dist/) is unaffected.
</threat_model>

<verification>
After both tasks complete:
1. npm test (vitest run --coverage) exits 0 with thresholds all 0.
2. package.json devDependencies has @vitest/coverage-v8 ^4.1.7.
3. package.json scripts.test equals "vitest run --coverage".
4. vitest.config.ts coverage.provider equals "v8".
5. The 3 existing Forge.submit.test.tsx tests still pass (green).
6. No new test files created in this plan — that is Wave 1 Plans 02 + 03.
</verification>

<success_criteria>
npm test runs without MISSING DEPENDENCY error; all 3 existing tests pass; coverage report generates (even if only for already-tested files); thresholds are 0 (placeholder, no violations). Foundation is in place for Plans 02 and 03 to add test files.
</success_criteria>

<output>
After completion, create .planning/phases/03-testing/03-01-SUMMARY.md with: what changed (files, devDep added, script updated), what the coverage baseline looks like after 3 existing tests, and any observations about the coverage output that inform Plan 04's threshold-setting task.
</output>
