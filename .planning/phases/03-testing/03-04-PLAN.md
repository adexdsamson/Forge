---
phase: 03-testing
plan: 04
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - vitest.config.ts
autonomous: true
requirements:
  - TEST-04

must_haves:
  truths:
    - "npm test exits with code 1 when a coverage threshold is artificially raised above the achieved coverage level"
    - "npm test exits with code 0 when all thresholds are at or below the achieved coverage level"
    - "vitest.config.ts documents the source of the threshold numbers (measured from the full test suite)"
    - "lines, functions, statements thresholds are in the 60-70% band; branches threshold is in the 40-50% band (D-05)"
    - "The threshold-setting step comes AFTER measuring — not before (RISK-T2 resolved)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Final non-zero coverage thresholds derived from measured coverage"
      contains: "thresholds"
  key_links:
    - from: "vitest.config.ts coverage.thresholds"
      to: "npm test exit code"
      via: "vitest run --coverage threshold enforcement"
      pattern: "lines.*[6-9][0-9]"
---

<objective>
Measure actual coverage from the full test suite (all files from Plans 01-03), then set real non-zero thresholds in vitest.config.ts conservatively below achieved coverage within the D-05 band (lines/functions/statements 60-70%, branches 40-50%). Verify npm test enforces them.

Purpose: Resolves RISK-T2 — thresholds set from measured data, not aspirationally. Satisfies TEST-04 success criterion 4: "Running yarn test fails with a non-zero exit code when coverage falls below the configured threshold; the threshold is documented in the test config."
Output: Updated vitest.config.ts with real threshold numbers derived from measured coverage. npm test enforces the thresholds.
</objective>

<execution_context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-RESEARCH.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-PATTERNS.md
</execution_context>

<context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\PROJECT.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\ROADMAP.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-03-SUMMARY.md

<interfaces>
From vitest.config.ts (current state after Plan 01 Task 2):
  coverage block exists with thresholds: { lines: 0, functions: 0, statements: 0, branches: 0 }
  Comment above thresholds: "// Thresholds set in Plan 04 after measuring achieved coverage (RISK-T2 / TEST-04 / D-05)."

Baseline coverage (1 test file, from RESEARCH.md §Baseline Coverage):
  lines: 44.81%, branches: 27.13%, functions: 28.57%, statements: 42.15%
  Note: useFieldArray, usePersist, useForgeValues, validateField were at 0% in baseline.
  Expected after Plans 02-03: lines ~65-75%, branches ~40-55% (to be measured).

D-05 band: lines/functions/statements 60-70%; branches 40-50% (lower, per RESEARCH.md recommendation).
Threshold rule: set each metric to floor(measured - 3), clamped to the D-05 band.
  Example: if measured lines=71%, set threshold to 68% (within band). If measured=58%, set to 57% (below band floor — flag to user but proceed with 57%).

Exit code behavior (RESEARCH.md §Threshold Exit Code, VERIFIED live):
  Vitest 4.x exits code 1 when any threshold is violated.
  Vitest exits code 0 when all thresholds are met.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure coverage and set final thresholds in vitest.config.ts</name>
  <files>vitest.config.ts</files>
  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\vitest.config.ts (read current state — thresholds should be 0 from Plan 01)
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-RESEARCH.md §"D-05 Modest threshold", §"Baseline Coverage", §"Common Pitfalls: Pitfall 5"
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-03-SUMMARY.md (coverage delta from RN tests — critical for threshold calculation)
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-02-SUMMARY.md (coverage delta from web tests)
  </read_first>
  <action>
    Step 1 — MEASURE: Run npm test (vitest run --coverage) with thresholds still at 0. Capture the coverage summary output. Record the actual percentages for lines, functions, statements, and branches across all source files combined (the "All files" row).

    Step 2 — CALCULATE thresholds using this formula:
      threshold_lines = max(60, floor(measured_lines - 3))
      threshold_functions = max(60, floor(measured_functions - 3))
      threshold_statements = max(60, floor(measured_statements - 3))
      threshold_branches = max(40, floor(measured_branches - 3))
    If any metric's measured value is below the band floor (lines < 60, branches < 40), set the threshold to floor(measured - 2) to avoid immediate failure — and note the shortfall in a comment.

    Step 3 — UPDATE vitest.config.ts: Replace the 0 placeholder values with the calculated thresholds. Update the comment above the thresholds block to document the measured values and the calculation date:
      "// Thresholds set 2026-05-31 from measured coverage after full test suite (Plans 01-03)."
      "// Measured: lines=X%, functions=Y%, statements=Z%, branches=W%."
      "// Set at measured minus 3pp within D-05 band (lines/fns/stmts 60-70%, branches 40-50%)."

    Step 4 — VERIFY: Run npm test again. It must exit 0 (thresholds are at or below achieved). Then artificially raise one threshold above measured (e.g., add +20 to lines) and run npm test — must exit 1. Then restore the correct values.

    Do NOT set thresholds aspirationally. If lines coverage after Plans 02-03 is 58%, set the threshold to 55 (not 65). The goal is enforcing a floor, not inflating a number.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 with real thresholds.
    - vitest.config.ts thresholds block has no zeros (all values are from the measured run).
    - The comment above thresholds documents the measured values and date.
    - Lines threshold is in range [55, 70]; branches threshold is in range [40, 55].
    - Validation: temporarily set lines threshold to 99 and confirm npm test exits 1; restore original value.
  </acceptance_criteria>
  <done>
    vitest.config.ts contains non-zero thresholds derived from the measured coverage. npm test exits 0. A comment in vitest.config.ts documents the measurement date and the measured-vs-threshold values. TEST-04 is satisfied: npm test enforces coverage and exits non-zero below threshold.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Coverage infrastructure (Plan 01), all web-mode tests (Plan 02), all RN-branch tests (Plan 03), and enforced coverage thresholds (Plan 04 Task 1). The full test suite now runs with coverage enforcement.
  </what-built>
  <how-to-verify>
    Run: npm test
    Expected output:
    1. All tests pass (green) — count should be ~20 tests across all test files.
    2. Coverage report appears in the terminal (lines/functions/statements/branches % for each file).
    3. Exit code 0 (no threshold violations).
    4. The coverage summary shows files that were previously at 0% (useFieldArray, usePersist, useForgeValues, validateField) now have meaningful coverage.

    Optional deeper check:
    - Open the HTML coverage report: npx vitest run --coverage --coverage.reporter=html (generates coverage/ dir) and inspect src/Forger/Forger.tsx and src/validateField.ts to confirm RN branches are marked as covered.

    Verify TEST-04 enforcement: temporarily edit vitest.config.ts to set thresholds.lines to 99, run npm test, confirm it exits non-zero with a coverage error message, then restore the correct value.
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass and coverage threshold enforcement works. Or describe specific failures to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coverage threshold → CI gate | Coverage thresholds are the security net that prevents regressions from being merged. If thresholds are too low, the gate is ineffective |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | coverage.thresholds in vitest.config.ts | mitigate | A developer lowering thresholds to make a failing build pass is the primary risk. Mitigated by: (1) thresholds are committed to git with a comment documenting the measured baseline — any reduction is visible in diff; (2) Phase 5 CI workflow (CICD-02) will run npm test on every PR, making threshold bypasses visible to reviewers. |
| T-03-04-02 | Repudiation | threshold measurement | accept | Thresholds are set from a specific measured run. If the run was flawed (e.g., mocking caused false-positive coverage), the threshold may be set too low. Mitigated by: the comment documenting the specific measured values — future reviewers can assess whether the numbers are plausible. |

Note: This plan touches only vitest.config.ts (threshold values). No new files. No runtime attack surface.
</threat_model>

<verification>
After Task 1 completes:
1. npm test exits 0 with non-zero thresholds.
2. npm test exits 1 when lines threshold is artificially set to 99.
3. vitest.config.ts thresholds contains only non-zero values derived from measurement.
4. The comment in vitest.config.ts documents the measured percentages and date.

After the human-verify checkpoint:
5. User has confirmed the full suite passes visually.
6. User has confirmed the threshold enforcement works (npm test exits 1 with inflated threshold).
</verification>

<success_criteria>
npm test enforces coverage thresholds and exits non-zero when they are violated. Thresholds are documented in vitest.config.ts with the measured values they were derived from. All 4 phase requirements are satisfied: TEST-01 (runner works), TEST-02 (submit test exists), TEST-03 (hook/validator/wizard/RN tests exist), TEST-04 (coverage threshold enforced).
</success_criteria>

<output>
After completion, create .planning/phases/03-testing/03-04-SUMMARY.md with:
- Final threshold values set (lines, functions, statements, branches)
- Measured coverage values that thresholds were derived from
- Total test count and pass/fail status
- Phase 3 completion declaration: TEST-01, TEST-02, TEST-03, TEST-04 all satisfied
</output>
