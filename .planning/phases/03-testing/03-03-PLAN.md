---
phase: 03-testing
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/Forger/Forger.rn.test.tsx
  - src/validateField.rn.test.ts
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "In RN mode, Forger injects onChangeText (not onChange) onto a TextInput-displayName component"
    - "In RN mode, Forger injects onValueChange onto a Switch/Picker-displayName component"
    - "In RN mode, validateField required rule calls setNativeProps({ error: message }) on the ref when shouldUseNativeValidation=true"
    - "The vi.mock on ../utils (from Forger) and ./utils (from validateField) correctly overrides isReactNative=true before the SUT module is imported"
    - "Existing web-mode tests are unaffected (platform mock is file-scoped)"
  artifacts:
    - path: "src/Forger/Forger.rn.test.tsx"
      provides: "RN event-handler wiring tests for onChangeText and onValueChange"
      min_lines: 80
    - path: "src/validateField.rn.test.ts"
      provides: "validateField RN branch: setNativeProps called for required rule"
      min_lines: 40
  key_links:
    - from: "src/Forger/Forger.rn.test.tsx vi.mock"
      to: "src/utils.ts isReactNative constant"
      via: "hoisted vi.mock factory with importOriginal spread"
      pattern: "isReactNative.*true"
    - from: "src/Forger/Forger.rn.test.tsx assertions"
      to: "src/Forger/Forger.tsx lines 50-55"
      via: "captured props showing onChangeText defined, onChange undefined"
      pattern: "onChangeText"
    - from: "src/validateField.rn.test.ts assertions"
      to: "src/validateField.ts lines 159-163"
      via: "mockSetNativeProps.toHaveBeenCalledWith({ error: message })"
      pattern: "setNativeProps"
---

<objective>
Write the React Native branch test files using the hoisted vi.mock seam on src/utils.ts. These files force isReactNative=true at import time (RISK-T1 seam — confirmed in RESEARCH.md) so Forger and validateField execute their RN code paths under jsdom. This satisfies D-04 (TEST-03 cross-platform requirement).

Purpose: Completes TEST-03 by covering RN event-handler wiring (Forger) and the setNativeProps validation path (validateField). These branches are permanently in web mode under jsdom without the vi.mock override.
Output: 2 new RN-mode test files. All existing web-mode tests continue to pass (mocks are file-scoped).
</objective>

<execution_context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-RESEARCH.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-PATTERNS.md
</execution_context>

<context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\PROJECT.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\ROADMAP.md
@C:\Users\HomePC\Documents\GitHub\Forge\src\Forge\Forge.submit.test.tsx

<interfaces>
RN mock seam (RESEARCH.md Pattern 2, confirmed HIGH confidence — seam traced in all 5 consumer files):
  vi.mock("../utils", async (importOriginal) => {
    const original = await importOriginal<typeof import("../utils")>();
    return { ...original, isReactNative: true, isWeb: false, isMobile: true };
  });
  Path from src/Forger/Forger.rn.test.tsx: "../utils"
  Path from src/validateField.rn.test.ts: "./utils"

CRITICAL: The vi.mock call MUST be the very first thing in the file (before all imports in source code). Vitest hoists it above static imports at transform time. A vi.mock inside a describe/it block is NOT hoisted and will NOT flip module-level constants (RISK-T1 / RESEARCH.md Pitfall 1).

From src/Forger/Forger.tsx RN branch (PATTERNS.md §Forger.rn.test.tsx, RESEARCH.md Open Question 2):
  - When isReactNative=true and displayName="TextInput" (isTextInput check at line 50): ForgerController injects onChangeText (not onChange) and sets onChange to a no-op.
  - When isReactNative=true and displayName="Switch" (isSwitch check): ForgerController injects onValueChange.
  - Forger.tsx line 47: if isReactNative, handlers.onChange = () => {} (no-op); handlers.onChangeText = field.onChange
  - Because jsdom does not fire onChangeText as a DOM event, assert handler presence via captured props (not via userEvent). Use act() + direct prop call to simulate.
  - RESEARCH.md Open Question 2 resolution: Use a capturing component pattern — RN component saves its props to a closure ref; after render, assert capturedProps.onChangeText is defined.

From src/validateField.ts RN branch (RESEARCH.md Pattern 3, PATTERNS.md §validateField.test.ts RN section):
  - Lines 153-163: inputRef = refs ? refs[0] : ref; if shouldUseNativeValidation is true, calls inputRef.setNativeProps({ error: message })
  - RN ref must have setNativeProps: vi.fn()
  - shouldUseNativeValidation (4th arg) must be true for the setNativeProps path to execute
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forger RN event-handler wiring test (Forger.rn.test.tsx)</name>
  <files>src/Forger/Forger.rn.test.tsx</files>
  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\src\Forger\Forger.tsx (lines 40-75: ForgerController handler logic; isTextInput check at line 50; the RN handler injection)
    - C:\Users\HomePC\Documents\GitHub\Forge\src\utils.ts (lines 99-104: isWeb/isReactNative/isMobile constants; isTextInput/isSwitch/isPicker/isSlider definitions)
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-PATTERNS.md §"Forger.rn.test.tsx", §"Pattern B: Hoisted vi.mock for RN Mode"
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-RESEARCH.md §"Pattern 2: React Native Branch Mock", §"Common Pitfalls: Pitfall 1 (module-level not mocked)", §"Open Questions: Forger.rn render strategy"
    - C:\Users\HomePC\Documents\GitHub\Forge\src\Forge\Forge.submit.test.tsx (import style reference)
  </read_first>
  <behavior>
    The vi.mock call is the first statement in the file source (Vitest hoists it above static imports). Factory uses importOriginal to spread all other exports from ../utils unchanged.

    Test 1: Forger injects onChangeText for components with displayName="TextInput" when isReactNative=true.
    - CapturingRNInput: a React.forwardRef component that captures its props into a closure variable. displayName="TextInput" (triggers the isTextInput branch in Forger.tsx line 50).
    - After render(<TestForm />), capturedProps.onChangeText must be defined (type: function).
    - After act(() => capturedProps.onChangeText("hello")), no error thrown (basic smoke that the fn is callable and updates RHF state without crash).

    Test 2: Forger injects onValueChange for components with displayName="Switch" when isReactNative=true.
    - A second CapturingSwitchInput with displayName="Switch".
    - After render, capturedProps.onValueChange must be defined.

    Both tests use: useForge + Forge + Forger wrapping the capturing component.
    Import: Forge from "../Forge/Forge", Forger from "./Forger", useForge from "../useForge/useForge".
    No TextInput from test-utils needed (RN components don't use HTMLInputElement).
  </behavior>
  <action>
    Create src/Forger/Forger.rn.test.tsx. Place the vi.mock call as the first top-level statement (Pattern B from PATTERNS.md). The path is "../utils" (relative from src/Forger/).

    The file structure:
    1. vi.mock("../utils", async (importOriginal) => { ... isReactNative: true, isWeb: false, isMobile: true })
    2. Triple-slash reference comment
    3. Static imports: React, describe/it/expect/vi/beforeEach/act from vitest, render from @testing-library/react, Forge, Forger, useForge
    4. Two capturing component definitions (CapturingRNInput with displayName="TextInput", CapturingSwitchInput with displayName="Switch") — each stores received props into a closure let variable
    5. describe block with beforeEach vi.clearAllMocks()
    6. Test 1 + Test 2 as described in behavior

    For each test: capturedProps is a let variable in module scope (or describe scope) reset in beforeEach. The capturing component assigns its props to it on each render call.

    After render, access capturedProps to assert handler presence. Use await act(async () => capturedProps.onChangeText("hello")) to verify no crash on invocation.

    Read src/Forger/Forger.tsx lines 40-75 first to confirm: (a) the exact prop name used for onChangeText vs onChange, (b) whether onChange is undefined or set to no-op (affects the assertion: use toBeUndefined OR check it is a no-op via toEqual(expect.any(Function))).
  </action>
  <verify>
    <automated>npx vitest run src/Forger/Forger.rn.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/Forger/Forger.rn.test.tsx` exists; `npx vitest run src/Forger/Forger.rn.test.tsx` exits 0 with 2 tests passing.
    - The captured props in Test 1 include `onChangeText` defined as a function (verified via `expect(capturedProps.onChangeText).toEqual(expect.any(Function))`).
    - The captured props in Test 2 include `onValueChange` defined as a function.
    - No hard `react-native` import is present in the file (platform is mocked via vi.mock on utils, not via a native import).
    - `npx vitest run` (full suite) exits 0 — web-mode tests in Plans 01 and 02 are unaffected by the file-scoped RN mock.
  </acceptance_criteria>
  <done>
    src/Forger/Forger.rn.test.tsx exists with 2 passing tests. Test 1 asserts onChangeText is defined on the captured props. Test 2 asserts onValueChange is defined. Both tests pass under the hoisted vi.mock (isReactNative=true). All previously passing tests still pass when running the full suite (npx vitest run).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validateField RN branch test (validateField.rn.test.ts)</name>
  <files>src/validateField.rn.test.ts</files>
  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\src\validateField.ts (lines 126-165: function signature, inputRef resolution at line 153, setNativeProps call at lines 159-163, shouldUseNativeValidation guard)
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-PATTERNS.md §"validateField.test.ts: RN-branch test file strategy" and §"Pattern B"
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-RESEARCH.md §"Pattern 3: validateField Direct Function Test" (RN section), §"RISK-T3 confirmed"
  </read_first>
  <behavior>
    The vi.mock call is the first statement in the file source. Path is "./utils" (relative from src/ where validateField.ts lives).

    Test 1: required rule calls setNativeProps with error message when shouldUseNativeValidation=true.
    - rnRef = { setNativeProps: vi.fn() }
    - field._f = { ref: rnRef, refs: undefined, name: "username", required: "Required", mount: true, disabled: false }
    - Call: await validateField(field, { username: "" }, false, true)
    - Assert: result has "username" key with type "required"
    - Assert: rnRef.setNativeProps called with object containing { error: "Required" }

    Test 2 (smoke): required rule does NOT call setNativeProps when shouldUseNativeValidation is falsy (omitted).
    - Same setup but call without 4th arg: await validateField(field, { username: "" }, false)
    - Assert: rnRef.setNativeProps was NOT called (validates the shouldUseNativeValidation guard)
  </behavior>
  <action>
    Create src/validateField.rn.test.ts (a .ts file — no JSX). Place vi.mock("./utils", async (importOriginal) => { ... isReactNative: true, isWeb: false, isMobile: true }) as the first statement.

    Import validateField from "./validateField" (default import). Import describe/it/expect/vi/beforeEach from vitest.

    beforeEach resets rnRef.setNativeProps mock with vi.clearAllMocks() (or recreate rnRef per test).

    Test 1 and Test 2 as described in behavior section.

    Confirm the exact call site in validateField.ts lines 159-163 by reading those lines before writing the assertion. The exact object shape passed to setNativeProps may include only { error } or additional keys — match what the source actually passes.
  </action>
  <verify>
    <automated>npx vitest run src/validateField.rn.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/validateField.rn.test.ts` exists; `npx vitest run src/validateField.rn.test.ts` exits 0 with 2 tests passing.
    - In Test 1, `rnRef.setNativeProps` is asserted as called (`toHaveBeenCalled`) with an object containing `{ error: "Required" }` when `shouldUseNativeValidation=true`.
    - In Test 2, `rnRef.setNativeProps` is asserted as NOT called (`not.toHaveBeenCalled`) when `shouldUseNativeValidation` is omitted.
    - `npx vitest run` (full suite) exits 0 — all web-mode tests from Plans 01 and 02 still pass.
  </acceptance_criteria>
  <done>
    src/validateField.rn.test.ts exists with 2 passing tests. Test 1 confirms setNativeProps called with error message. Test 2 confirms setNativeProps NOT called without shouldUseNativeValidation. Full suite (npx vitest run) still passes all previously-passing tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vi.mock scope | The hoisted mock applies only to this test file's module registry; it does not affect other test files or the runtime bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Spoofing | vi.mock on utils.ts | accept | The mock replaces isReactNative with true only in test files. The production utils.ts is unmodified. Risk: a test mock that diverges from the real runtime behavior gives false confidence in RN branches. Mitigated by: the mock uses importOriginal spread — only isReactNative/isWeb/isMobile are overridden; all other utils exports (deepEqual, Slot, cloneObject, etc.) are real. |
| T-03-03-02 | Tampering | shouldUseNativeValidation guard | accept | Test 2 in validateField.rn.test.ts explicitly verifies the guard is respected (setNativeProps NOT called when flag omitted). This prevents a regression where the guard is removed and the RN path executes on web. |

Note: This plan adds test files only. No new runtime code. No attack surface added to the published package.
</threat_model>

<verification>
After both tasks complete:
1. npx vitest run (full suite, no filter) runs all tests and all pass: all web-mode tests from Plans 01-02 plus 2 Forger RN + 2 validateField RN = ~20 total tests.
2. npx vitest run src/Forger/Forger.rn.test.tsx passes 2 tests.
3. npx vitest run src/validateField.rn.test.ts passes 2 tests.
4. Coverage report (npx vitest run --coverage) shows isReactNative=true branches in Forger.tsx and validateField.ts now covered.
5. All web-mode test files show no leakage of the RN mock (file-scoped).
</verification>

<success_criteria>
Both RN-mode test files exist and pass. The vi.mock hoisting pattern correctly overrides isReactNative at module evaluation time. Forger's onChangeText and onValueChange injection is asserted. validateField's setNativeProps path is asserted with and without the shouldUseNativeValidation guard. Full suite is green.
</success_criteria>

<output>
After completion, create .planning/phases/03-testing/03-03-SUMMARY.md with: RN branch coverage delta (which lines/branches in Forger.tsx and validateField.ts are now hit), any hoisting gotchas encountered, and the total test count. Note which branches remain uncovered (if any) so Plan 04 can accurately set thresholds.
</output>
