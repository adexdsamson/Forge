---
phase: 03-testing
plan: 02
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - src/test-utils.tsx
  - src/Forge/Forge.errors.test.tsx
  - src/validateField.test.ts
  - src/useFieldArray/useFieldArray.test.tsx
  - src/usePersist/usePersist.test.tsx
  - src/useForgeValues/useForgeValues.test.tsx
autonomous: true
requirements:
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "CORR-02 regression: rendering Forger with multiple children throws an error containing both 'Forger' and the field name"
    - "validateField required rule returns an error object for empty value (web path)"
    - "validateField minLength, maxLength, pattern, and custom validate rules return errors for violations"
    - "validateField passing validation returns empty error object"
    - "useFieldArray append adds a new field; remove deletes a field by index"
    - "usePersist handler fires after field value change with updated values and isDirty: true"
    - "useForgeValues getValue returns the registered field value"
    - "useForgeValues getValue throws for an unregistered field"
  artifacts:
    - path: "src/test-utils.tsx"
      provides: "Shared TextInput forwardRef helper for all web-mode test files"
      exports: ["TextInput"]
    - path: "src/Forge/Forge.errors.test.tsx"
      provides: "CORR-02 regression test (D-06)"
      min_lines: 40
    - path: "src/validateField.test.ts"
      provides: "validateField web-path rule tests (required, minLength, maxLength, pattern, custom)"
      min_lines: 80
    - path: "src/useFieldArray/useFieldArray.test.tsx"
      provides: "append and remove integration tests"
      min_lines: 80
    - path: "src/usePersist/usePersist.test.tsx"
      provides: "subscription / handler-fires test"
      min_lines: 50
    - path: "src/useForgeValues/useForgeValues.test.tsx"
      provides: "getValue, setValue, and throws-on-unknown tests"
      min_lines: 70
  key_links:
    - from: "src/Forge/Forge.errors.test.tsx"
      to: "src/Forger/Forger.tsx lines 119-132"
      via: "expect().toThrow() catching Forger-named error"
      pattern: "Forger.*myField"
    - from: "src/validateField.test.ts"
      to: "src/validateField.ts"
      via: "direct async function call"
      pattern: "validateField\\(field"
    - from: "src/useFieldArray/useFieldArray.test.tsx"
      to: "src/useFieldArray/useFieldArray.tsx"
      via: "useFieldArray hook inside Forge render tree"
      pattern: "useFieldArray"
    - from: "src/usePersist/usePersist.test.tsx"
      to: "src/usePersist/usePersist.tsx"
      via: "usePersist hook inside Forge render tree"
      pattern: "usePersist"
    - from: "src/useForgeValues/useForgeValues.test.tsx"
      to: "src/useForgeValues/useForgeValues.tsx lines 71-74"
      via: "capturedGetValue() throwing on unknown field"
      pattern: "not registered"
---

<objective>
Write all web-mode test files (no RN mocking in this plan). Creates the shared TextInput test helper, the CORR-02 regression test, validateField rule tests (web path only), and integration tests for useFieldArray, usePersist, and useForgeValues. All tests run under jsdom's default web mode — no vi.mock on utils.ts needed.

Purpose: Satisfies TEST-02 (extends existing submit coverage with error regression) and the majority of TEST-03. Provides the bulk of integration coverage that will be measured in Plan 04.
Output: 6 new source files (1 shared helper + 5 co-located test files). All 3 existing tests continue to pass alongside new tests.
</objective>

<execution_context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-RESEARCH.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-PATTERNS.md
</execution_context>

<context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\PROJECT.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\ROADMAP.md
@C:\Users\HomePC\Documents\GitHub\Forge\src\Forge\Forge.submit.test.tsx

<interfaces>
From src/Forge/Forge.submit.test.tsx (the canonical template):
- TextInput forwardRef helper at lines 23-46 — copy verbatim into src/test-utils.tsx and import from there in all 5 test files.
- Integration pattern: useForge + Forge + Forger + userEvent + waitFor.
- beforeEach(() => vi.clearAllMocks()) at lines 52-54.

From src/Forger/Forger.tsx (CORR-02 error source, PATTERNS.md §Forge.errors.test.tsx):
- Error message template at lines 119-123: `Forger: field "${props.name}" expects exactly one valid React element as its child`
- Throws when children count > 1 or child is not a valid React element.

From src/validateField.ts (RESEARCH.md Pattern 3, PATTERNS.md §validateField.test.ts):
- Signature: async (field, formValues, validateAllFieldCriteria, shouldUseNativeValidation?, isFieldArray?, disabledFieldNames?) => Promise<InternalFieldErrors>
- Default export.
- Web path: inputRef = refs ? refs[0] : ref; calls inputRef.setCustomValidity(msg) + inputRef.reportValidity()
- Rules in field._f: required (string), minLength (number), maxLength (number), pattern (RegExp), validate (fn or map)
- Returns: { [fieldName]: { type, message } } on error; {} on pass.

From src/useFieldArray/useFieldArray.tsx (PATTERNS.md §useFieldArray.test.tsx):
- Requires FormProvider context (line 27: useFormContext).
- Signature: useFieldArray({ control, name, inputProps }) => { fields, append, remove, insert, swap, update }
- fields[n].id is the React key; fields[n].inputProps carries through inputProps from config.

From src/usePersist/usePersist.tsx (PATTERNS.md §usePersist.test.tsx, RESEARCH.md Pattern 5):
- Signature: usePersist({ control, handler })
- Handler fires on mount (initial useWatch emission) and on every field value change.
- Handler signature per Phase 2 D-12: (values, { isDirty, isValid }) => void
- PITFALL 4: drain initial mount emission with waitFor, then mockClear, then assert post-action.

From src/useForgeValues/useForgeValues.tsx (PATTERNS.md §useForgeValues.test.tsx, RESEARCH.md Pattern 6):
- Requires FormProvider context (line 63: useFormContext).
- Signature: useForgeValues({ control }) => { getValue, setValue, getValues }
- getValue(name): returns value from ctx.getValues(name); throws "useForgeValues.getValue: field \"${name}\" is not registered" when field absent from getValues() at lines 71-74.
- Use captured-ref pattern: capture getValue/setValue inside the render function, use after render.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared test helper + Forge.errors (CORR-02) + validateField web tests</name>
  <files>src/test-utils.tsx, src/Forge/Forge.errors.test.tsx, src/validateField.test.ts</files>
  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\src\Forge\Forge.submit.test.tsx (TextInput helper at lines 23-46; import style at lines 10-17)
    - C:\Users\HomePC\Documents\GitHub\Forge\src\Forger\Forger.tsx (error message at lines 119-132; understand what triggers it)
    - C:\Users\HomePC\Documents\GitHub\Forge\src\validateField.ts (default export, _f field shape, web path lines 154-158)
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-PATTERNS.md §"Forge.errors.test.tsx", §"validateField.test.ts", §"Pattern C: React 18 Throwing Component Assertion"
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-RESEARCH.md §"Pattern 3: validateField Direct Function Test", §"Pattern 4: CORR-02 Error Assertion", §"Pitfall 3"
  </read_first>
  <behavior>
    src/test-utils.tsx exports TextInput (React.forwardRef<HTMLInputElement, ...>) matching lines 23-46 of Forge.submit.test.tsx exactly. No default export — named export only so test files can: import { TextInput } from "../test-utils".

    src/Forge/Forge.errors.test.tsx:
    - Test: CORR-02 — rendering Forger with two children inside Forge throws error matching /Forger/ AND /myField/.
    - Test must spy on console.error, suppress it during render, restore after.
    - Uses ThrowingForm helper component (not inline JSX — avoids React 18 render reconciler edge case).
    - Single describe block with beforeEach vi.clearAllMocks().

    src/validateField.test.ts:
    - Import validateField from "./validateField" (default import).
    - describe "validateField — web rules": beforeEach creates fresh jsdom HTMLInputElement ref with vi.fn() setCustomValidity and reportValidity.
    - Tests (each is a separate it()):
      1. required rule returns error for empty value: field._f.required="Email is required", formValues={ email: "" } → result.email.type="required", result.email.message="Email is required"
      2. minLength rule returns error when value too short: field._f.minLength=5, formValues={ field: "hi" } → result has type "minLength"
      3. maxLength rule returns error when value too long: field._f.maxLength=3, formValues={ field: "toolong" } → result has type "maxLength"
      4. pattern rule returns error on mismatch: field._f.pattern={ value: /^\d+$/, message: "Digits only" }, formValues={ field: "abc" } → result has type "pattern"
      5. custom validate fn returning a string is an error: field._f.validate=(v) => v === "bad" ? "Invalid" : true, formValues={ field: "bad" } → result has type "validate", message "Invalid"
      6. passing required validation returns empty error: field._f.required="Required", formValues={ field: "hello" } → result is empty object {}
  </behavior>
  <action>
    Create src/test-utils.tsx: copy the TextInput forwardRef component from Forge.submit.test.tsx lines 23-46 verbatim. Add named export. Add the triple-slash reference comment: /// <reference types="vitest/globals" /> at the top. This file is excluded from coverage via vitest.config.ts coverage.exclude ("src/test-utils.tsx" added in Plan 01 Task 2).

    Create src/Forge/Forge.errors.test.tsx following Pattern A (PATTERNS.md §Shared Patterns) and Pattern C (CORR-02 throw assertion). Import TextInput from "../test-utils" (relative from src/Forge/). Use the ThrowingForm pattern from PATTERNS.md §Forge.errors.test.tsx — a React component that renders Forger with two span children. The test must verify BOTH /Forger/ and /myField/ patterns match the thrown error to satisfy D-06.

    Create src/validateField.test.ts following PATTERNS.md §validateField.test.ts and RESEARCH.md Pattern 3. This is a .ts (not .tsx) file — no JSX needed; validateField is a pure async function. Each rule test builds a minimal field._f object (ref, refs: undefined, name, mount: true, disabled: false, and the specific rule property). No React render or @testing-library needed. Ref is document.createElement("input") with vi.fn() setCustomValidity/reportValidity.

    Do NOT include RN-branch tests (vi.mock on utils) in this file — those go in validateField.rn.test.ts in Plan 03.
  </action>
  <verify>
    <automated>npx vitest run src/Forge/Forge.errors.test.tsx src/validateField.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    src/test-utils.tsx exists and exports TextInput. Forge.errors.test.tsx has at least 1 test that passes asserting both /Forger/ and /myField/ in the thrown error. validateField.test.ts has 6 tests all passing: required (error + pass), minLength, maxLength, pattern, and custom validate. No console errors from the CORR-02 test (console.error is suppressed).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useFieldArray, usePersist, and useForgeValues integration tests</name>
  <files>src/useFieldArray/useFieldArray.test.tsx, src/usePersist/usePersist.test.tsx, src/useForgeValues/useForgeValues.test.tsx</files>
  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\src\Forge\Forge.submit.test.tsx (integration pattern, import style, beforeEach)
    - C:\Users\HomePC\Documents\GitHub\Forge\src\useFieldArray\useFieldArray.tsx (hook signature, fields shape, useFormContext requirement at line 27)
    - C:\Users\HomePC\Documents\GitHub\Forge\src\usePersist\usePersist.tsx (handler signature, useWatch emission on mount)
    - C:\Users\HomePC\Documents\GitHub\Forge\src\useForgeValues\useForgeValues.tsx (getValue/setValue/getValues, throws at lines 71-74, useFormContext at line 63)
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-PATTERNS.md §"useFieldArray.test.tsx", §"usePersist.test.tsx", §"useForgeValues.test.tsx", §"Pattern D: usePersist Initial-Mount Mock Clear"
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\03-testing\03-RESEARCH.md §"Pattern 5: usePersist Subscription Test", §"Pattern 6: useForgeValues getValue-throws Test", §"Pitfall 4", §"Pitfall 6"
  </read_first>
  <behavior>
    useFieldArray.test.tsx:
    - Test 1: append adds a field — render DynamicForm with 1 initial item; click "Add" button; waitFor confirms 2 input fields (or 2 remove buttons) visible.
    - Test 2: remove deletes a field — render DynamicForm with 2 initial items; click "Remove" for index 0; waitFor confirms only 1 item remains.
    - DynamicForm renders Forge + useFieldArray with name="items", maps fields to Forger + remove button; has "Add" button calling append({value:""}).
    - Import TextInput from "../test-utils".

    usePersist.test.tsx:
    - Test: handler fires with updated values and isDirty:true after userEvent.type.
    - Apply Pattern D (PATTERNS.md): waitFor initial mount emission → handler.mockClear() → userEvent.type → waitFor expects handler called with { name: stringContaining("a") } and { isDirty: true }.
    - Import TextInput from "../test-utils".

    useForgeValues.test.tsx:
    - Test 1: getValue("email") returns "test@example.com" when defaultValue is set.
    - Test 2: getValue("nonexistent") throws matching /useForgeValues\.getValue.*nonexistent.*not registered/.
    - Test 3: setValue("email", "new@example.com") updates the field; subsequent getValue returns new value (or verify via render update).
    - Capture hook return values via closure ref inside TestForm component (PATTERNS.md §useForgeValues captured ref pattern).
    - Import TextInput from "../test-utils".
  </behavior>
  <action>
    Create src/useFieldArray/useFieldArray.test.tsx following the DynamicForm pattern in PATTERNS.md §useFieldArray.test.tsx. The form renders: fields.map → div with Forger (name="items.N.value") + remove button (data-testid="remove-N"), plus an add button (data-testid="add-btn") calling append({ value: "" }). Import TextInput from "../test-utils" (path: "../../test-utils" from src/useFieldArray/). Test 1 clicks add-btn, waits for 2 remove buttons visible. Test 2 starts with 2 items in defaultValues, clicks remove-0, waits for 1 remove button.

    Create src/usePersist/usePersist.test.tsx following PATTERNS.md §usePersist.test.tsx and Pattern D. Import usePersist from "./usePersist". Import TextInput from "../../test-utils". Handler mock must be a vi.fn(). The usePersist call goes inside TestForm: usePersist({ control, handler }). After initial-mount drain and mockClear, type "alice" into the name input and assert handler called with values+state shape matching Phase 2 D-12 signature: (values, { isDirty, isValid }).

    Create src/useForgeValues/useForgeValues.test.tsx following PATTERNS.md §useForgeValues.test.tsx and RESEARCH.md Pattern 6. Import useForgeValues from "./useForgeValues". Import TextInput from "../../test-utils". For the throws test: capturedGetValue is assigned inside TestForm render; after render+waitFor(defined), calling capturedGetValue("nonexistent") must throw. The regex must match the actual error string from useForgeValues.tsx lines 71-74 — verify the exact string by reading those lines before writing the assertion.

    All three files follow Pattern A (PATTERNS.md §Shared Patterns): triple-slash reference, React import, vitest imports, @testing-library imports, SUT imports, TextInput import from test-utils, describe block, beforeEach vi.clearAllMocks().
  </action>
  <verify>
    <automated>npx vitest run src/useFieldArray/useFieldArray.test.tsx src/usePersist/usePersist.test.tsx src/useForgeValues/useForgeValues.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    All three files exist and all tests pass: useFieldArray (2 tests), usePersist (1 test), useForgeValues (3 tests). Running npx vitest run with no filter shows all previously existing tests (Forge.submit, Forge.errors, validateField) still pass alongside the new tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → source under test | Tests import from src/; no network or filesystem outside the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | test assertions | accept | Tests assert observable behavior (D-03), not internal state. If a test is weakened (matcher too broad), it could mask regressions. Mitigated by: specific matcher assertions (exact error messages, specific field values, not just toHaveBeenCalled). |
| T-03-02-02 | Information Disclosure | test-utils.tsx in coverage | accept | src/test-utils.tsx is excluded from coverage via Plan 01's exclude list. No production code; no data at risk. |

Note: This plan adds test files only. No new public API surface. No runtime code modified. No authentication, network, or user input handling introduced.
</threat_model>

<verification>
After both tasks complete:
1. npx vitest run (no filter) runs all tests and all pass: 3 existing + 1 CORR-02 + 6 validateField + 2 useFieldArray + 1 usePersist + 3 useForgeValues = ~16 tests.
2. No console errors from the CORR-02 test (suppressed).
3. src/test-utils.tsx exports TextInput.
4. All test files are co-located next to their source (matching Forge.submit.test.tsx pattern).
5. No __tests__/ directory created.
</verification>

<success_criteria>
All 6 new files exist and all new tests pass alongside the existing 3 tests. validateField required, minLength, maxLength, pattern, and custom validate rules are covered. useFieldArray append and remove are covered. usePersist subscription fires with correct values. useForgeValues getValue returns and throws correctly.
</success_criteria>

<output>
After completion, create .planning/phases/03-testing/03-02-SUMMARY.md with: list of files created, test counts (passing/failing), any implementation notes (e.g., exact error message format discovered in useForgeValues), and any fields or branches that proved difficult to reach.
</output>
