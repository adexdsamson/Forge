---
phase: 06-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - c:\Temp\forge-smoke-test\smoke.ts
  - c:\Temp\forge-smoke-test\package.json
autonomous: true
requirements: [PUB-01]
must_haves:
  truths:
    - "smoke.ts imports all 6 exports: useForge, Forge, Forger, useFieldArray, useForgeValues, usePersist (per D-07)"
    - "smoke package.json references @adexdsamson/forge WITHOUT a file: tarball path — pointing at the live registry so post-publish SC1 proves a real fresh install"
    - "npx tsc --noEmit in the smoke dir exits non-zero NOW (TS2307 — cannot find module) because the package is not yet published; this confirms the scaffold is wired to the registry and not the stale tarball"
  artifacts:
    - path: "c:\\Temp\\forge-smoke-test\\smoke.ts"
      provides: "6-export import check for SC2 type-resolution proof"
      contains: "useFieldArray, useForgeValues, usePersist"
    - path: "c:\\Temp\\forge-smoke-test\\package.json"
      provides: "live-registry dependency reference for SC1 install proof"
      contains: "@adexdsamson/forge"
  key_links:
    - from: "c:\\Temp\\forge-smoke-test\\smoke.ts"
      to: "@adexdsamson/forge dist/index.d.ts"
      via: "npm install + tsc resolution"
      pattern: "useFieldArray.*useForgeValues.*usePersist"
---

<objective>
Prepare the post-publish smoke scaffold so it is ready to prove SC1, SC2, and SC3 the moment CI publishes the package. The existing scaffold at c:\Temp\forge-smoke-test currently imports only 3 of 6 exports and points at a local tarball — both must be fixed now so Wave-3 verification is a simple `npm install` + `npx tsc --noEmit`, not a debugging session.

Purpose: Wave 0 preparation ensures the post-publish gate (Plan 06-04) can execute rapidly and deterministically. The smoke scaffold must be tested against the registry path before the release is created, so any tsconfig or module-resolution issues surface here, not after the irreversible publish.

Output: Updated smoke.ts (6 exports), updated smoke package.json (registry reference, not tarball). The `@adexdsamson/forge` package does not yet exist on the registry, so `tsc --noEmit` will exit with TS2307 after this plan — that is the expected state confirming the scaffold is correctly wired.
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\PROJECT.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\ROADMAP.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\STATE.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\06-publish\06-CONTEXT.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\06-publish\06-RESEARCH.md

<interfaces>
<!-- Current smoke scaffold state — executor must read these before editing -->

From c:\Temp\forge-smoke-test\smoke.ts (CURRENT — 3 exports only):
  import { useForge, Forge, Forger } from "@adexdsamson/forge";
  const _check = { useForge, Forge, Forger }

From c:\Temp\forge-smoke-test\package.json (CURRENT — tarball reference):
  "@adexdsamson/forge": "file:../../Users/HomePC/Documents/GitHub/Forge/adexdsamson-forge-1.0.3.tgz"

From c:\Temp\forge-smoke-test\tsconfig.json (CURRENT — note verbatimModuleSyntax: false already set):
  "module": "nodenext", "verbatimModuleSyntax": false, "skipLibCheck": true
  (TS1295 false-alarm is already neutralized; real failure indicator is TS2307)

Target smoke.ts (6 exports — write this exactly):
  import { useForge, Forge, Forger, useFieldArray, useForgeValues, usePersist } from "@adexdsamson/forge";
  const _check = { useForge, Forge, Forger, useFieldArray, useForgeValues, usePersist };
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update smoke.ts to import all 6 named exports</name>
  <files>c:\Temp\forge-smoke-test\smoke.ts</files>
  <read_first>
    - c:\Temp\forge-smoke-test\smoke.ts — current 3-export content to replace
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\06-publish\06-RESEARCH.md §Q5 — exact target smoke.ts content
    - C:\Users\HomePC\Documents\GitHub\Forge\src\index.ts — confirm these 6 names are exported (read only the export lines)
  </read_first>
  <action>
    Overwrite c:\Temp\forge-smoke-test\smoke.ts with exactly two lines:
    Line 1: import { useForge, Forge, Forger, useFieldArray, useForgeValues, usePersist } from "@adexdsamson/forge";
    Line 2: const _check = { useForge, Forge, Forger, useFieldArray, useForgeValues, usePersist };
    Do not add comments, type annotations, or any other content. The file is a type-check-only smoke probe — minimal is correct.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'C:\Temp\forge-smoke-test\smoke.ts' -Pattern 'useFieldArray' -Quiet; exit ($LASTEXITCODE -eq 0 ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - smoke.ts line 1 contains exactly: import { useForge, Forge, Forger, useFieldArray, useForgeValues, usePersist } from "@adexdsamson/forge";
    - smoke.ts line 2 contains: const _check = { useForge, Forge, Forger, useFieldArray, useForgeValues, usePersist };
    - File has no other content (no comments, no extra blank lines beyond a trailing newline)
    - All 6 names are present: useForge, Forge, Forger, useFieldArray, useForgeValues, usePersist
  </acceptance_criteria>
  <done>smoke.ts imports all 6 exports per D-07/SC2 requirement</done>
</task>

<task type="auto">
  <name>Task 2: Repoint smoke package.json to live registry and reinstall</name>
  <files>c:\Temp\forge-smoke-test\package.json</files>
  <read_first>
    - c:\Temp\forge-smoke-test\package.json — current content showing file: tarball path
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\06-publish\06-RESEARCH.md §Q5 — exact reinstall commands and expected TS2307 outcome
  </read_first>
  <action>
    Edit c:\Temp\forge-smoke-test\package.json: change the "@adexdsamson/forge" dependency value from "file:../../Users/HomePC/Documents/GitHub/Forge/adexdsamson-forge-1.0.3.tgz" to "@adexdsamson/forge" (no version pin — allows `npm install` to pull latest, or pin to "1.0.0" explicitly once published). Use "1.0.0" as the version specifier since that is the exact target version per D-01.

    Then reinstall: from c:\Temp\forge-smoke-test, run:
      Remove-Item node_modules -Recurse -Force -ErrorAction SilentlyContinue
      Remove-Item package-lock.json -ErrorAction SilentlyContinue
      npm install

    npm install will fail with a 404 because @adexdsamson/forge@1.0.0 does not yet exist on the registry — that is the EXPECTED outcome confirming the scaffold is wired to the live registry and not the stale tarball. The failure message will say "404 Not Found - GET https://registry.npmjs.org/@adexdsamson/forge" which proves the registry path is active.

    After the expected 404 failure, run npx tsc --noEmit anyway (it will also fail with TS2307 since the package could not install). Capture both failure codes as confirmation of correct scaffold wiring.

    Do NOT install the tarball again. Do NOT add a "file:" reference back. The 404 is the proof of correct setup.
  </action>
  <verify>
    <automated>powershell -Command "$pkg = Get-Content 'C:\Temp\forge-smoke-test\package.json' | ConvertFrom-Json; if ($pkg.dependencies.'@adexdsamson/forge' -like 'file:*') { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - c:\Temp\forge-smoke-test\package.json "dependencies.@adexdsamson/forge" does NOT contain "file:" — it is "1.0.0" or a registry-compatible specifier
    - npm install exits with a 404 error referencing "registry.npmjs.org/@adexdsamson%2fforge" (confirms registry path is active, not a local tarball path)
    - The 404 error message does NOT reference a local file path — it references the npmjs.org registry URL
    - Note: TS2307 from npx tsc --noEmit is also expected and correct at this stage; it is NOT a failure of this plan
  </acceptance_criteria>
  <done>Smoke scaffold points at live registry; 404 confirms it will resolve correctly once the package is published; tarball reference is gone</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local smoke dir → npm registry | npm install in smoke project contacts registry.npmjs.org over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | smoke package.json | accept | Smoke dir is a throwaway local scaffold, not a published artifact; no attack surface |
| T-06-01-02 | Spoofing | npm registry resolution | mitigate | npm install uses HTTPS to registry.npmjs.org; npm verifies package integrity via lockfile SHA; no custom registry |
</threat_model>

<verification>
After both tasks complete:
1. c:\Temp\forge-smoke-test\smoke.ts contains all 6 export names — confirm with: Select-String -Path C:\Temp\forge-smoke-test\smoke.ts -Pattern "usePersist"
2. c:\Temp\forge-smoke-test\package.json has no "file:" in the @adexdsamson/forge dependency value
3. npm install in the smoke dir produces a 404 referencing registry.npmjs.org (not a file path error) — scaffold correctly wired to live registry
</verification>

<success_criteria>
- smoke.ts imports all 6 named exports per D-07 / SC2
- smoke package.json dependency for @adexdsamson/forge uses a registry version specifier (not file:), proving SC1 will be tested against the live registry
- The 404 from npm install confirms registry wiring is correct; this plan is complete even with a 404 because the package does not yet exist
</success_criteria>

<output>
After completion, create C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\06-publish\06-01-SUMMARY.md
</output>
