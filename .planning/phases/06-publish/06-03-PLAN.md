---
phase: 06-publish
plan: 03
type: execute
wave: 2
depends_on: [06-02]
files_modified:
  - CHANGELOG.md
  - package.json
  - package-lock.json
autonomous: false
requirements: [PUB-01]
must_haves:
  truths:
    - "CHANGELOG.md first entry heading is '## [1.0.0] - 2026-06-01' with no '## [Unreleased]' above it; the curated Features and Bug Fixes bullets are UNCHANGED (per D-03)"
    - "package.json version field is '1.0.0' (per D-01)"
    - "package-lock.json version field is '1.0.0' (updated atomically by commit-and-tag-version)"
    - "Commit 'chore(release): 1.0.0' exists on branch release/v1.0.0 and is pushed to origin"
    - "A release PR from release/v1.0.0 to main is open on GitHub and the human has merged it to main"
  artifacts:
    - path: "C:\\Users\\HomePC\\Documents\\GitHub\\Forge\\CHANGELOG.md"
      provides: "Finalized changelog with 1.0.0 release heading"
      contains: "## [1.0.0] - 2026-06-01"
    - path: "C:\\Users\\HomePC\\Documents\\GitHub\\Forge\\package.json"
      provides: "Version bumped to 1.0.0"
      contains: "\"version\": \"1.0.0\""
  key_links:
    - from: "package.json version"
      to: "git tag v1.0.0"
      via: "commit-and-tag-version --release-as 1.0.0 --skip.changelog"
      pattern: "chore\\(release\\): 1\\.0\\.0"
    - from: "release/v1.0.0 branch"
      to: "main branch"
      via: "PR merge (D-05)"
      pattern: "PR merged to main"
---

<objective>
Finalize the CHANGELOG heading for the 1.0.0 release, run commit-and-tag-version to bump package.json to 1.0.0 and create the release commit, push the release branch, open a PR to main, and wait for the human to merge it. This plan produces the release commit that will become the anchor for the git tag in Plan 06-04.

Purpose: D-01 mandates 1.0.0 as the first public release version. D-03 mandates using commit-and-tag-version with --skip.changelog to preserve the curated CHANGELOG body. D-05 mandates the PR-then-tag flow regardless of branch protection status. The release commit must land on main BEFORE the GitHub Release is created — the GitHub Release creates the tag on main HEAD, and that tag fires publish.yml.

Output: release/v1.0.0 branch with the version bump commit merged to main. package.json = 1.0.0. CHANGELOG.md heading = [1.0.0] - 2026-06-01. No git tag yet — that comes from gh release create in Plan 06-04.
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\PROJECT.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\ROADMAP.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\06-publish\06-CONTEXT.md
@C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\06-publish\06-RESEARCH.md

<interfaces>
<!-- Existing CHANGELOG.md top section — executor must read before editing -->

From CHANGELOG.md (current top of file — DO NOT regenerate, only rename the heading):
  ## [Unreleased]
  ### Features
  * **useForge / Forge / Forger:** initial open-source release ...
  * **Forge:** renders a real <form> on web ...
  (... 7 more Features bullets ...)
  ### Bug Fixes
  * **field arrays:** preserve index alignment ...
  (... 3 more Bug Fix bullets ...)

Target heading (one-line edit only):
  ## [Unreleased]  →  ## [1.0.0] - 2026-06-01

.versionrc.json current state (IMPORTANT — must NOT be modified):
  { "skip": { "bump": true, "tag": true, "commit": true } }
  This was set in Phase 5 to support changelog-only generation.
  The --skip.changelog CLI flag overrides the changelog behavior at invocation time.
  Do NOT edit .versionrc.json — the CLI flag is the correct override for Phase 6.

commit-and-tag-version invocation (EXACT — use these flags):
  npx commit-and-tag-version --release-as 1.0.0 --skip.changelog
  (from C:\Users\HomePC\Documents\GitHub\Forge — this overrides the .versionrc.json skip config)
  Expected output lines:
    √ bumping version in package.json from 1.0.3 to 1.0.0
    √ bumping version in package-lock.json from 1.0.3 to 1.0.0
    √ tagging release v1.0.0
    i Run `git push --follow-tags origin main` to publish
  Do NOT follow the "git push --follow-tags" suggestion — push the branch only (not the tag) per D-05.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit CHANGELOG.md heading and run version bump</name>
  <files>CHANGELOG.md, package.json, package-lock.json</files>
  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\CHANGELOG.md — confirm current heading is "## [Unreleased]" and curated body is present; read the full file to see what MUST be preserved
    - C:\Users\HomePC\Documents\GitHub\Forge\package.json — confirm current version is "1.0.3" before running the tool
    - C:\Users\HomePC\Documents\GitHub\Forge\.versionrc.json — read to confirm skip config; do NOT edit it
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\06-publish\06-RESEARCH.md §Q1 — exact invocation flags and expected output
  </read_first>
  <action>
    Step 1 — Create the release branch (run from C:\Users\HomePC\Documents\GitHub\Forge):
      git checkout -b release/v1.0.0

    Step 2 — Edit CHANGELOG.md: change ONLY the heading on the line that reads "## [Unreleased]" to "## [1.0.0] - 2026-06-01". Do not touch any other line — not the Features bullets, not the Bug Fixes bullets, not any other content. This is a single-line edit.

    Step 3 — Run the version bump (from C:\Users\HomePC\Documents\GitHub\Forge):
      npx commit-and-tag-version --release-as 1.0.0 --skip.changelog

    This tool will:
    - Bump "version" in package.json from "1.0.3" to "1.0.0"
    - Bump the version in package-lock.json to match
    - Create a git commit with message "chore(release): 1.0.0"
    - Create a local git tag v1.0.0

    CRITICAL: --skip.changelog prevents the tool from regenerating the CHANGELOG body. The manual heading edit in Step 2 is staged and committed as part of the tool's commit.

    Wait — the tool's commit includes staged files. After Step 2 (manual CHANGELOG edit), the CHANGELOG edit is NOT yet staged. The tool stages and commits package.json, package-lock.json, and any staged files. You must stage the CHANGELOG edit before running the tool:
      git add CHANGELOG.md
    Then run the tool. It will include CHANGELOG.md in the release commit.

    Step 4 — Verify the commit and tag:
      git log --oneline -3
      git tag -l v1.0.0
      (confirms: commit "chore(release): 1.0.0" exists, tag v1.0.0 exists locally)

    Step 5 — Run npm test to confirm no regression from the version/CHANGELOG edits:
      npm test

    Step 6 — Push the branch (NOT the tag — tag will be created by gh release create in Plan 06-04):
      git push origin release/v1.0.0
      (Do NOT run: git push --follow-tags. Do NOT push the tag yet.)
  </action>
  <verify>
    <automated>cd C:\Users\HomePC\Documents\GitHub\Forge && node -e "const p=require('./package.json'); process.exit(p.version === '1.0.0' ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - C:\Users\HomePC\Documents\GitHub\Forge\package.json "version" field is exactly "1.0.0"
    - C:\Users\HomePC\Documents\GitHub\Forge\package-lock.json version is "1.0.0"
    - C:\Users\HomePC\Documents\GitHub\Forge\CHANGELOG.md first versioned heading is "## [1.0.0] - 2026-06-01" — no "## [Unreleased]" heading remains above it
    - CHANGELOG.md Features and Bug Fixes bullets are UNCHANGED from their pre-edit content (the curated body is preserved verbatim)
    - git log --oneline shows commit message "chore(release): 1.0.0" as the most recent commit on release/v1.0.0
    - git tag -l v1.0.0 returns "v1.0.0" (local tag exists)
    - npm test exits 0 (existing suite is green after version bump)
    - git push origin release/v1.0.0 exits 0 (branch is on origin)
    - The local tag v1.0.0 is NOT pushed to origin yet (push.followTags was not used)
  </acceptance_criteria>
  <done>Version bump commit is on release/v1.0.0 branch, pushed to origin; CHANGELOG heading is finalized; package.json = 1.0.0; tests green; ready for PR</done>
</task>

<task type="auto">
  <name>Task 2: Open release PR to main</name>
  <files></files>
  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\06-publish\06-RESEARCH.md §Q3 — PR flow, squash-merge hazard, and the reason NOT to push the tag
    - C:\Users\HomePC\Documents\GitHub\Forge\CHANGELOG.md — read the finalized [1.0.0] section to use as the PR body
  </read_first>
  <action>
    From C:\Users\HomePC\Documents\GitHub\Forge, create the release PR:

      gh pr create --title "chore(release): v1.0.0" --base main --head release/v1.0.0 --body "$(cat <<'PRBODY'
    ## v1.0.0 — Initial Public Release

    This PR lands the version bump commit for the first public release of `@adexdsamson/forge`.

    ### What's changing
    - `package.json` version: `1.0.3` → `1.0.0` (per D-01: first public release resets extraction-era version)
    - `CHANGELOG.md`: `[Unreleased]` heading finalized to `[1.0.0] - 2026-06-01`
    - `package-lock.json`: version updated to match

    ### No code changes
    This commit contains only version metadata. All library code shipped in phases 1–5.

    ### After merge
    Do NOT push the local `v1.0.0` tag. The tag will be created by `gh release create v1.0.0 --target main` in the next step, which places the tag on the merged main commit (avoiding squash-merge SHA mismatch).

    ### Merge strategy
    Merge commit or squash both work — the tag is created AFTER merge via `gh release create --target main`.
    PRBODY
    )"

    Confirm the PR URL is returned by the command. Note the PR number.
  </action>
  <verify>
    <automated>cd C:\Users\HomePC\Documents\GitHub\Forge && gh pr list --head release/v1.0.0 --json number,title,state --jq '.[0].state'</automated>
  </verify>
  <acceptance_criteria>
    - gh pr create exits 0 and prints a GitHub PR URL
    - gh pr list --head release/v1.0.0 shows the PR with state "OPEN"
    - PR title is "chore(release): v1.0.0"
    - PR base is "main", head is "release/v1.0.0"
  </acceptance_criteria>
  <done>Release PR is open on GitHub and ready for human review and merge</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human merges the release PR to main</name>
  <read_first>
    - C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\06-publish\06-RESEARCH.md §Q3 — squash-merge SHA hazard; why the tag must be created AFTER merge via gh release create (not before)
  </read_first>
  <what-built>The release PR was opened in Task 2. The agent has pushed release/v1.0.0 to origin with the version bump commit. The PR is ready to merge.</what-built>
  <how-to-verify>
    1. Visit the PR on GitHub (URL printed by gh pr create in Task 2) or use: gh pr view --web
    2. Review the PR diff — it should show only CHANGELOG.md heading change + package.json/package-lock.json version bump (1.0.3 → 1.0.0). No source code changes.
    3. Merge the PR using any merge strategy (merge commit or squash — both work; the tag is created after merge in Plan 06-04 so SHA mismatch is avoided)
    4. After merge, confirm: gh pr view --head release/v1.0.0 --json state shows "MERGED"
    5. Confirm main has the bump: git fetch origin main && git log origin/main --oneline -3 — should show "chore(release): 1.0.0" in the log
  </how-to-verify>
  <acceptance_criteria>
    - The release PR is merged to main (state = MERGED)
    - git log origin/main shows the "chore(release): 1.0.0" commit
    - No other unexpected commits landed between the release PR merge and the current main HEAD
    - The local v1.0.0 tag has NOT been pushed to origin (it still points at the branch commit — that is correct; gh release create will create the tag on merged main HEAD in Plan 06-04)
  </acceptance_criteria>
  <resume-signal>Type "merged" once the PR is merged to main, or describe any issue found during review</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local commit → origin/release/v1.0.0 | git push over HTTPS; branch commit contains only version metadata |
| release/v1.0.0 → main | PR merge requires explicit human action (D-05); no automated merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | CHANGELOG.md body | mitigate | --skip.changelog prevents tool from regenerating body; manual edit is heading-only; acceptance criteria verifies curated bullets are unchanged |
| T-06-03-02 | Tampering | Tag on wrong commit (squash-merge SHA mismatch) | mitigate | Tag is NOT pushed from the branch; gh release create --target main in Plan 06-04 creates the tag on merged main HEAD (per research Q3 — confirmed approach) |
| T-06-03-03 | Repudiation | Version bump commit bypasses review | mitigate | D-05 mandates PR-then-tag; Task 3 is a human-blocking checkpoint before the irreversible GitHub Release creation in Plan 06-04 |
| T-06-03-04 | Elevation of Privilege | Unintended publish triggered by branch push | accept | publish.yml triggers on release: [published] only, NOT on branch push (confirmed from workflow YAML); pushing release/v1.0.0 does not fire CI publish |
</threat_model>

<verification>
After all three tasks (including checkpoint):
1. git log origin/main --oneline -3 includes commit "chore(release): 1.0.0"
2. package.json version at origin/main HEAD is "1.0.0": gh api repos/adexdsamson/Forge/contents/package.json --jq '.content' | base64 -d | python3 -c "import sys,json; print(json.load(sys.stdin)['version'])"
3. CHANGELOG.md at origin/main HEAD has "## [1.0.0] - 2026-06-01" as the first versioned heading
4. No git tag v1.0.0 exists on origin yet: git ls-remote --tags origin v1.0.0 returns empty
5. PR state is MERGED: gh pr list --head release/v1.0.0 --json state
</verification>

<success_criteria>
- package.json version is "1.0.0" on main (SC3 first component)
- CHANGELOG.md [Unreleased] is gone; [1.0.0] - 2026-06-01 heading with curated body is on main
- Release commit is on main via PR (D-05 satisfied)
- No git tag on origin yet (tag will be created by gh release create in Plan 06-04, anchored to correct main commit)
- Ready for Plan 06-04: GitHub Release creation and post-publish smoke
</success_criteria>

<output>
After completion, create C:\Users\HomePC\Documents\GitHub\Forge\.planning\phases\06-publish\06-03-SUMMARY.md
</output>
