---
phase: 04-packaging
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified: []
autonomous: false
requirements:
  - PKG-02
  - PKG-03

must_haves:
  truths:
    - "npm pack --dry-run lists dist/index.cjs.js, dist/index.esm.js, dist/index.d.ts plus their .map files"
    - "prepack runs rollup -c and produces a fresh dist/ (not stale artifacts)"
    - "dist/ is not tracked by git (git ls-files dist/ returns 0 lines)"
    - "A throwaway TypeScript project can import { useForge, Forge, Forger } from the packed tarball and tsc resolves types with no manual path config"
    - "Decisions covered by this plan: D-06: files:[dist] keeps sourcemaps in tarball; D-07: full fresh-project smoke test verifies type resolution; D-08: no .npmignore, files:[dist] is authoritative"
  artifacts:
    - path: "dist/index.cjs.js"
      provides: "CJS entry point in tarball"
    - path: "dist/index.esm.js"
      provides: "ESM entry point in tarball"
    - path: "dist/index.d.ts"
      provides: "TypeScript declarations in tarball"
  key_links:
    - from: "package.json scripts.prepack"
      to: "dist/ contents"
      via: "npm run build (rollup -c) rebuilds dist/ before pack"
      pattern: "prepack.*npm run build"
    - from: "npm pack tarball"
      to: "throwaway project tsc"
      via: "tarball installed as local dependency; types resolve from dist/index.d.ts"
      pattern: "@adexdsamson/forge"
---

<objective>
Verify all Phase 4 packaging changes are correct end-to-end: confirm dist hygiene, run
npm pack --dry-run, exercise the prepack build hook, and perform the manual smoke test
(install tarball into a throwaway TypeScript project, import the three core exports, run tsc).

Purpose: PKG-02 requires proof that types resolve without manual path config; PKG-03 requires
confirmation that dist/ is absent from git. This plan is the experiential gate before Phase 5.
Output: Human-verified pass/fail on each check; SUMMARY records results.
</objective>

<execution_context>
@C:/Users/HomePC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HomePC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-packaging/04-CONTEXT.md
@.planning/phases/04-packaging/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated checks — dist hygiene, prepack build, and pack dry-run</name>
  <files>.planning/phases/04-packaging/04-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-packaging/04-01-SUMMARY.md (confirm Task 1-3 of Plan 01 all passed before proceeding)
    - package.json (confirm prepack script and publishConfig are correct before running)
    - .planning/phases/04-packaging/04-CONTEXT.md (D-05, D-06, D-07 — what the dry-run must list)
  </read_first>
  <action>
    Run three automated checks in sequence. All three must pass before the smoke test checkpoint.

    CHECK 1 — dist hygiene (PKG-03 confirmation):
    Run: git ls-files dist/
    Expected: zero output (no lines). If any dist file is tracked, stop and investigate — do NOT commit any dist/ file to fix this. The .gitignore already lists "dist" per PATTERNS.md; this is a confirmation, not new work.

    CHECK 2 — prepack build hook (PKG-02):
    Delete the existing dist/ directory entirely, then run: npm run prepack
    This exercises the prepack hook end-to-end. After it completes, verify that dist/ was recreated and contains at minimum: dist/index.cjs.js, dist/index.esm.js, dist/index.d.ts (and their .map siblings). Use: ls dist/ to see what was produced.
    If prepack fails (non-zero exit), investigate rollup errors before continuing.

    CHECK 3 — npm pack dry-run (PKG-02/PKG-03 partial, per D-06 sourcemaps must appear, per D-08 no .npmignore):
    Run: npm pack --dry-run
    Capture the output. The tarball contents list MUST include:
      - package.json
      - README.md
      - LICENSE
      - dist/index.cjs.js
      - dist/index.esm.js
      - dist/index.d.ts
      - dist/index.cjs.js.map
      - dist/index.esm.js.map
      - dist/index.d.ts.map (may or may not be present depending on rollup dts plugin)
    The list must NOT include any file under src/, .env, any credential file, or any file outside dist/ (except the auto-included package.json / README.md / LICENSE).
    Also confirm that the listed "main", "module", and "types" fields in the output resolve to dist/index.cjs.js, dist/index.esm.js, dist/index.d.ts respectively.

    Record the exact output of all three checks in the SUMMARY.

    **Decisions covered:** D-06: files:["dist"] keeps sourcemaps (*.js.map) in the tarball — dry-run must list them; D-08: no .npmignore exists or should be added, files:["dist"] is the single authoritative include list — dry-run confirms npm honors it.
  </action>
  <verify>
    <automated>git ls-files dist/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - git ls-files dist/ returns 0 lines (exit 0, no output)
    - npm run prepack exits 0 and recreates dist/ from scratch
    - dist/index.cjs.js exists after prepack
    - dist/index.esm.js exists after prepack
    - dist/index.d.ts exists after prepack
    - npm pack --dry-run output lists dist/index.cjs.js, dist/index.esm.js, dist/index.d.ts
    - npm pack --dry-run output lists LICENSE (npm auto-include from repo root)
    - npm pack --dry-run output does NOT list any src/ file
    - npm pack --dry-run output does NOT list any .env or credential file
  </acceptance_criteria>
  <done>All three automated checks pass; results recorded in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke test — install tarball into throwaway TypeScript project</name>
  <what-built>
    Task 1 ran the automated hygiene and dry-run checks. Now the full PKG-02 experiential
    verification requires a human to install the actual tarball into a fresh TypeScript project
    and confirm types resolve — this cannot be automated by Claude because it requires an
    isolated project outside the repo.
  </what-built>
  <how-to-verify>
    Follow these exact steps (per D-07: full fresh-project smoke test):

    Step 1 — Pack the tarball:
    From the Forge repo root, run: npm pack
    This produces a file like adexdsamson-forge-1.0.3.tgz in the current directory.

    Step 2 — Create a throwaway TypeScript project:
    In a temporary directory OUTSIDE the Forge repo (e.g. C:\Temp\forge-smoke-test), run:
      mkdir forge-smoke-test
      cd forge-smoke-test
      npm init -y
      npm install typescript @types/react react react-hook-form
      npx tsc --init

    Step 3 — Install the tarball:
    Run: npm install C:\Users\HomePC\Documents\GitHub\Forge\adexdsamson-forge-1.0.3.tgz
    (adjust path to wherever npm pack created the .tgz)

    Step 4 — Create a test file:
    Create smoke.ts in the throwaway project with this content:
      import { useForge, Forge, Forger } from "@adexdsamson/forge";
      // If this file typechecks, types resolve correctly.
      const _check = { useForge, Forge, Forger };

    Step 5 — Run tsc:
    Run: npx tsc --noEmit
    Expected: exits 0 with NO errors. If tsc reports "Cannot find module '@adexdsamson/forge'
    or its corresponding type declarations" — that is a FAILURE. If it reports type errors
    from within Forge itself (not from your smoke.ts) those are acceptable and may be noted.

    Step 6 — Clean up:
    Delete the throwaway directory and the .tgz file from the Forge repo root.

    Report the exact tsc output (or "no output, exit 0") here.
  </how-to-verify>
  <resume-signal>
    Type "approved" if tsc exits 0 (types resolve, no module-not-found errors).
    Or describe the exact error output so it can be diagnosed.
  </resume-signal>
  <acceptance_criteria>
    - npm pack produces a .tgz file (non-zero size, contains dist/)
    - Installing the .tgz into a fresh project succeeds (npm install exits 0)
    - tsc --noEmit in the throwaway project exits 0 with no "Cannot find module" error for @adexdsamson/forge
    - The three imports (useForge, Forge, Forger) are resolved by tsc without manual tsconfig path mapping
  </acceptance_criteria>
  <done>Human reports "approved"; smoke test passes; PKG-02 fully verified. Decisions covered: D-07: full fresh-project smoke test confirms types resolve without manual path config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist/ tarball → npm registry (future) | The pack verification confirms tarball contents before any registry write occurs in this plan |
| Forge repo → throwaway project | The .tgz is installed from local filesystem; the throwaway project exercises the exact consumer experience |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | Stale dist/ shipped in tarball | mitigate | Check 2 explicitly deletes dist/ before running prepack; if prepack fails to rebuild, the dry-run in Check 3 would list an empty/incomplete dist/ — caught before publish. |
| T-04-08 | Information Disclosure | Unexpected files in tarball | mitigate | Check 3 (npm pack --dry-run) acceptance criteria require that src/ and .env files are absent from the tarball listing. files: ["dist"] is the allowlist; this check confirms npm honors it. |
| T-04-09 | Denial of Service | Throwaway project leftover .tgz | accept | The smoke test instructs cleanup of both the throwaway directory and the .tgz. Low-risk even if forgotten (local filesystem only, no registry impact). |
</threat_model>

<verification>
Consolidated pass criteria for Phase 4:

PKG-01: package.json has name "@adexdsamson/forge", populated description/keywords/repository/homepage/author(object)/license, no "private" key → verified in Plan 01 Task 1.

PKG-02: npm pack --dry-run lists main(CJS)/module(ESM)/types(.d.ts); prepack rebuilds artifacts; smoke test confirms types resolve → verified in Plan 02 Task 1 + Task 2.

PKG-03: dist/ in .gitignore + git ls-files dist/ returns 0 lines; files: ["dist"] limits tarball → verified in Plan 02 Task 1 + npm pack dry-run.

PKG-04: publishConfig.registry === "https://registry.npmjs.org", CI publish step runs from root (no working-directory), decision documented in PROJECT.md → verified in Plan 01 Task 1-3.
</verification>

<success_criteria>
Phase 4 verification is complete when:
1. git ls-files dist/ returns 0 lines (PKG-03 dist hygiene)
2. npm run prepack exits 0 and recreates dist/ from scratch (PKG-02 build hook)
3. npm pack --dry-run lists dist/index.cjs.js + dist/index.esm.js + dist/index.d.ts + LICENSE (PKG-02/PKG-03 tarball contents)
4. Human smoke test: tsc --noEmit in a fresh project importing from the tarball exits 0 with no module-not-found errors (PKG-02 type resolution)
5. Human types "approved" at the checkpoint gate
</success_criteria>

<output>
After completion, create .planning/phases/04-packaging/04-02-SUMMARY.md with:
- Exact output of git ls-files dist/ check
- Exact output of npm run prepack (exit code + dist/ listing)
- Exact output of npm pack --dry-run (full tarball file list)
- Smoke test result (tsc output or "no output, exit 0")
- Human gate result ("approved" / notes)
- Confirmation: Phase 4 all requirements PKG-01..PKG-04 satisfied
</output>
