---
phase: 01-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils.ts
  - src/Forger/Forger.tsx
  - src/useFieldArray/useFieldArray.tsx
  - src/useForgeValues/useForgeValues.tsx
autonomous: true
requirements: [CORR-02, CORR-03]

must_haves:
  truths:
    - "Passing multiple children to <Forger> throws an error naming Forger and the field name"
    - "Passing a non-element (string/number/false) child to <Forger> throws an error naming Forger and the field name (no silent null)"
    - "Slot, used independently, throws a clear 'Slot'-named message for >1 child"
    - "updateFieldArrayRootError exists only in src/logic/, not in src/utils.ts"
    - "All importers of updateFieldArrayRootError resolve from src/logic/, and zero references to the utils.ts copy remain"
  artifacts:
    - path: "src/Forger/Forger.tsx"
      provides: "Fail-fast guard naming Forger + field name for multiple/non-element children"
      contains: "Forger"
    - path: "src/utils.ts"
      provides: "Slot with a 'Slot'-named error message; duplicate updateFieldArrayRootError removed"
      contains: "Slot:"
    - path: "src/logic/updateFieldArrayRootError.ts"
      provides: "Canonical updateFieldArrayRootError (kept)"
      contains: "updateFieldArrayRootError"
  key_links:
    - from: "src/Forger/Forger.tsx"
      to: "Error naming Forger + field name"
      via: "guard before delegating to Slot"
      pattern: "Forger"
    - from: "src/useFieldArray/useFieldArray.tsx"
      to: "src/logic/updateFieldArrayRootError"
      via: "import repoint"
      pattern: "logic/updateFieldArrayRootError"
    - from: "src/useForgeValues/useForgeValues.tsx"
      to: "src/logic/updateFieldArrayRootError"
      via: "import repoint"
      pattern: "logic/updateFieldArrayRootError"
---

<objective>
Fix CORR-02 (component-named, fail-fast child errors layered across `Forger` and `Slot`) and the CORR-03 dedupe half (remove the duplicate `updateFieldArrayRootError` from `src/utils.ts`, keep the canonical `src/logic/` copy, repoint all imports). These are grouped because both are small, mechanical, and together own an exclusive file set (`utils.ts`, `Forger.tsx`, and the two `updateFieldArrayRootError` importers) — letting this plan run in parallel with Plan 01 with zero file overlap.

Purpose: Replace the generic, location-blind `"Only one child allowed"` throw and the silent `null` return with errors that name the component and field, and eliminate the duplicated helper so there is a single source of truth.

Output: Corrected `src/Forger/Forger.tsx`, `src/utils.ts`, and repointed imports in `src/useFieldArray/useFieldArray.tsx` and `src/useForgeValues/useForgeValues.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-correctness/01-CONTEXT.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Contracts the executor needs. Already in the codebase. -->

From src/utils.ts (current Slot — generic message + silent null at :406-423):
```typescript
export function Slot({ children, ...props }: SlotProps) {
  if (React.Children.count(children) > 1) {
    throw new Error("Only one child allowed");   // -> "Slot: only one child is allowed"
  }
  if (React.isValidElement(children)) { /* clone */ }
  return null;   // silent null on non-element child
}
```
NOTE on Slot's public-API status: `Slot` is NOT re-exported from `src/index.ts`. The package entrypoint exports only `./Forge`, `./Forger`, `./useForge`, `./usePersist`, `./useFieldArray`, `./useForgeValues`, `./types`, `./reactNative`, and a hand-picked list of platform predicates from `./utils` (`isWeb`, `isReactNative`, `isMobile`, `isTextInput`, `isCheckBoxInput`, `isRadioInput`, `isPicker`, `isSwitch`, `isSlider`). `Slot` is therefore an INTERNAL helper today — its named error message is defensive / forward-looking (it improves DX for anyone who later exports or reuses Slot, and it gives a clear message on the internal Slot path), NOT a change to public API surface.

From src/utils.ts (DUPLICATE to REMOVE, :312-321 — uses lodash-style `compact`):
```typescript
export const updateFieldArrayRootError = <T>(errors, error, name) => { ... };
```
NOTE: `compact` is still used elsewhere in utils.ts (:67, :110, :238) — do NOT remove `compact`, only the duplicate `updateFieldArrayRootError` export.

From src/logic/updateFieldArrayRootError.ts (CANONICAL — KEEP, default export, uses convertToArrayPayload):
```typescript
export default <T extends FieldValues = FieldValues>(errors, error, name): FieldErrors<T> => { ... };
```

IMPORTANT — the two copies are NOT behaviorally identical:
- utils.ts copy: `compact(get(errors, name))` — strips falsy values; `compact(undefined) → []`.
- logic/ copy (canonical): `convertToArrayPayload(get(errors, name))` — `Array.isArray(v) ? v : [v]`; does NOT strip falsy and wraps `undefined → [undefined]`.
Keeping `logic/` as canonical is locked by D-09. The dedupe therefore changes the payload semantics at both call sites (see Task 2).

Current importers (both import the NAMED export from "../utils"):
- src/useFieldArray/useFieldArray.tsx :31 — `updateFieldArrayRootError,` inside `from "../utils"`; used at :249.
- src/useForgeValues/useForgeValues.tsx :35 — `updateFieldArrayRootError,` inside `from "../utils"`; used at :306.

From src/Forger/Forger.tsx (current Forger has NO guard at :113-126; field name available as `props.name`):
```typescript
export const Forger = <T extends FieldValues>(props: ForgerProps<T>) => {
  const methods = useFormContext() ?? { control: props?.control };
  return (<Slot><MemorizeController {...props} name={props.name} methods={methods} component={props.component} /></Slot>);
};
```
The Forger ALWAYS passes exactly one child (MemorizeController) today, so its guard concerns the `children`-bearing form (Forger is invoked with field config props, not arbitrary children) — see action for the precise guard target.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Layered fail-fast child errors — Slot ('Slot'-named) + Forger (Forger + field name)</name>
  <read_first>
    - src/utils.ts (Slot ~:401-423, SlotProps type ~:401-404)
    - src/Forger/Forger.tsx (full file — Forger :113-126, MemorizeController :81-111, do NOT regress the comparator)
    - src/index.ts (confirm Slot is NOT exported — entrypoint only re-exports platform predicates from ./utils)
    - .planning/phases/01-correctness/01-CONTEXT.md (D-08, Specific Ideas illustrative wording)
    - .planning/codebase/CONCERNS.md (§"CORR-03: Slot Error Message and Forger Guard")
  </read_first>
  <action>
    In src/utils.ts `Slot` (:406-423), per D-08:
    - Replace the throw message `"Only one child allowed"` with `"Slot: only one child is allowed"` (name the component — Slot cannot know a field name).
    - Replace the silent `return null` for a non-element, non-empty child with a throw: when `children` is present but NOT a valid React element (e.g. string/number/false), throw `new Error("Slot: child must be a single valid React element")`. Allow `children` being null/undefined to still return null (an empty Slot is legitimate). Keep the existing valid-element clone path unchanged (web vs RN style-merge logic intact).
    - Rationale note (do not weaken the fix): `Slot` is currently an internal helper — it is NOT re-exported from `src/index.ts` (the entrypoint re-exports only platform predicates from `./utils`, not `Slot`). The 'Slot'-named message is therefore defensive / forward-looking rather than a public-API change today. The named-error requirement (D-08) still stands and MUST be implemented exactly as above.

    In src/Forger/Forger.tsx `Forger` (:113-126), per D-08 — add a rich, fail-fast guard BEFORE delegating to Slot that names `Forger` and the field `name`:
    - Forger renders exactly one child today (MemorizeController), so the guard validates the consumer-facing contract: Forger requires a usable `component` to render and a `name`. Throw a rich Error if the contract is violated for BOTH the multiple-children case AND the non-element-child case as framed by D-08. Because Forger constructs its own single child internally, implement the guard as: if `React.Children.count` of any incoming `props.children` is > 1, OR a provided child/component resolves to a non-element/non-renderable value, throw `new Error(\`Forger: field "${props.name}" expects exactly one valid React element as its child\`)` (illustrative wording per CONTEXT Specific Ideas — MUST name `Forger` and include the field `name`). The error must fire before Slot is reached so the developer sees the Forger+name message, not the generic Slot message.
    - Optionally set `Forger.displayName = "Forger"` for cleaner stack traces (discretionary per D-08 — recommended).
    - Do NOT change MemorizeController or its comparator (:81-111) — it is already correct.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/utils.ts Slot throws `"Slot: only one child is allowed"` for >1 child and no longer contains the string `"Only one child allowed"`.
    - Source assertion: src/utils.ts Slot throws (not returns null) for a non-null, non-element child; an empty Slot (null/undefined children) still returns null.
    - Rationale-accuracy assertion: the plan/rationale treats Slot as INTERNAL (not publicly exported) — `grep -n "Slot" src/index.ts` returns nothing, confirming Slot's named message is defensive/forward-looking, not a public-API change. (The D-08 fix itself is still required.)
    - Source assertion: src/Forger/Forger.tsx contains a guard that throws an Error whose message includes the literal `Forger` and interpolates the field `name` (e.g. matches `Forger: field "..."`), and the guard runs before `<Slot>` is rendered.
    - Source assertion: src/Forger/Forger.tsx MemorizeController comparator (:81-111) is unchanged (no regression).
    - Behavior assertion (manually verifiable): rendering `<Forger name="email" component={Input} />` works; passing two children or a string child to the Forger/Slot path throws a message naming Forger + "email" (or naming Slot when Slot is used directly).
    - Build command: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Slot throws a 'Slot'-named message for >1 child and a clear throw (not silent null) for non-element children (defensive — Slot is internal, not exported from index.ts); Forger throws a rich error naming Forger + the field name before delegating to Slot; MemorizeController untouched; tsc passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dedupe updateFieldArrayRootError — keep logic/ copy, remove utils.ts copy, repoint imports</name>
  <read_first>
    - src/utils.ts (duplicate updateFieldArrayRootError :312-321; confirm `compact` is used elsewhere at :67/:110/:238 and must remain)
    - src/logic/updateFieldArrayRootError.ts (canonical default export — keep; uses convertToArrayPayload)
    - src/useFieldArray/useFieldArray.tsx (import block :20-33, named import at :31; usage :249; stray dead comment at :92)
    - src/useForgeValues/useForgeValues.tsx (import block :28-39, named import at :35; usage :306)
    - .planning/phases/01-correctness/01-CONTEXT.md (D-09)
    - .planning/codebase/CONCERNS.md (§"CORR-04: Stale JSDoc" context — note: CONCERNS internal numbering differs from milestone IDs)
  </read_first>
  <action>
    Per D-09, canonical copy = `src/logic/updateFieldArrayRootError.ts` (default export). Eliminate the duplicate named export in `src/utils.ts`:
    - Repoint src/useFieldArray/useFieldArray.tsx: remove `updateFieldArrayRootError` from the `from "../utils"` named-import block (:31) and add `import updateFieldArrayRootError from "../logic/updateFieldArrayRootError";` (default import). The call site at :249 stays the same (same function signature: errors, error, name).
    - Repoint src/useForgeValues/useForgeValues.tsx: remove `updateFieldArrayRootError` from the `from "../utils"` named-import block (:35) and add `import updateFieldArrayRootError from "../logic/updateFieldArrayRootError";` (default import). Call site at :306 unchanged.
    - VERIFY zero remaining references to the utils.ts named export before deleting it: grep `updateFieldArrayRootError` across src/ must show only the logic/ definition + the two repointed default imports + their usages (NO `from "../utils"` association and NO export in utils.ts).
    - Delete the duplicate `export const updateFieldArrayRootError = ...` block from src/utils.ts (:312-321). Do NOT remove `compact` or any other utils export — only this one function.
    - SEMANTIC CHANGE — acknowledge, do not paper over: the deleted utils.ts copy used `compact(get(errors, name))` (strips falsy; `compact(undefined) → []`), while the canonical logic/ copy uses `convertToArrayPayload(get(errors, name))` (`Array.isArray(v) ? v : [v]` — does NOT strip falsy and wraps `undefined → [undefined]`). The signature is identical (errors, error, name) so no syntactic call-site change is needed, but the PAYLOAD shape the callers receive changes for the empty/undefined-error case. Confirm both call sites (useFieldArray :249, useForgeValues :306) tolerate a non-compacted payload — they merge the returned root error into the RHF errors object and do not iterate the payload assuming falsy values were stripped. Do NOT add a `compact` shim around the logic/ copy (that would re-diverge from D-09's canonical).
    - Dead-comment cleanup (ROADMAP criterion #4): while editing src/useFieldArray/useFieldArray.tsx for the import repoint, remove the stray commented-out line at :92 (`// ids.current = fieldValues.map(generateId);`) inside the `useSubscribe` next-handler. It is dead code referencing an abandoned id-tracking approach — delete the comment line entirely. (useFieldArray.tsx is already owned by this plan; do NOT add anything new to files_modified.)
  </action>
  <verify>
    <automated>npx tsc --noEmit; npx rollup -c</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/utils.ts` no longer contains `export const updateFieldArrayRootError`.
    - Source assertion: `src/useFieldArray/useFieldArray.tsx` imports `updateFieldArrayRootError` as a default import from `../logic/updateFieldArrayRootError` and NOT from `../utils`.
    - Source assertion: `src/useForgeValues/useForgeValues.tsx` imports `updateFieldArrayRootError` as a default import from `../logic/updateFieldArrayRootError` and NOT from `../utils`.
    - CLI assertion: a repo-wide search for `updateFieldArrayRootError` returns only the canonical definition in `src/logic/`, the two default imports, and their two call sites — zero references to a utils.ts export.
    - Semantic-change assertion (explicit, replaces any prior "behaviorally equivalent / no call-site change needed" claim): the executor MUST acknowledge in the SUMMARY that the dedupe swaps `compact` (falsy-stripping; `undefined→[]`) for `convertToArrayPayload` (non-stripping; `undefined→[undefined]`), and MUST assert that both call sites (useFieldArray :249, useForgeValues :306) tolerate the non-compacted payload. Verify via a concrete manual sanity check: in a field-array form, append one item, then remove it while a root-level array error is set — confirm the root error is attached/cleared correctly and no crash or stray-falsy iteration occurs at either call site. No `compact` shim is added around the canonical copy.
    - Source assertion: `compact` remains defined and used in `src/utils.ts` (not orphaned/removed).
    - Source assertion (dead-comment removal): `grep -n "ids.current = fieldValues.map(generateId)" src/useFieldArray/useFieldArray.tsx` returns nothing.
    - Build command: `npx tsc --noEmit` exits 0 AND `npx rollup -c` builds without errors.
  </acceptance_criteria>
  <done>Duplicate updateFieldArrayRootError removed from utils.ts; both importers repoint to the canonical logic/ default export; the compact→convertToArrayPayload semantic change is acknowledged and both call sites verified to tolerate a non-compacted payload (manual append/remove-with-root-error sanity check); zero remaining utils.ts references; compact untouched; stray `ids.current = ...` dead comment removed from useFieldArray.tsx:92; tsc + rollup clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer JSX → Forger/Slot | App-supplied children and field `name` cross into the guard logic |

Forge is a headless client-side form library with no network/auth/storage surface. This plan only changes error-throwing behavior and removes a duplicated pure function — no new external surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | new fail-fast error messages in Forger/Slot | mitigate | Error messages include only the field `name` (a developer-supplied identifier like `"email"`) and static component names ("Forger"/"Slot") — they MUST NOT interpolate the field's *value* or any form data. Action specifies naming the field `name` only, never `value`, preventing leakage of user-entered data into thrown errors/stack traces. |
| T-01-05 | Tampering | dedupe of updateFieldArrayRootError | accept | Pure error-shaping helper over in-memory RHF error objects; no trust boundary crossed. Behavior is NOT identical (compact→convertToArrayPayload changes the empty-error payload shape) but the canonical logic/ copy is the D-09-locked source of truth and both call sites tolerate the non-compacted payload (verified by manual sanity check). |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `npx rollup -c` builds CJS + ESM + dts cleanly.
- Repo-wide grep confirms single source of truth for `updateFieldArrayRootError` and absence of `"Only one child allowed"`.
- `grep -n "Slot" src/index.ts` returns nothing (Slot is internal, not public API — rationale accuracy).
- `grep -n "ids.current = fieldValues.map(generateId)" src/useFieldArray/useFieldArray.tsx` returns nothing (dead comment removed).
- Error messages name component + field `name`, never field value.
- compact→convertToArrayPayload semantic change acknowledged; both call sites verified to tolerate non-compacted payload.
</verification>

<success_criteria>
- Multiple or non-element children to `<Forger>` throw an error naming `Forger` + the field `name` (CORR-02).
- `Slot` (used directly) throws a clear `Slot`-named message and no longer silently returns null on a bad child — defensive, since Slot is internal/not exported (CORR-02).
- `updateFieldArrayRootError` exists only in `src/logic/`; all imports repoint there; zero utils.ts references remain; the compact→convertToArrayPayload semantic change is acknowledged and tolerated at both call sites (CORR-03 dedupe half).
</success_criteria>

<output>
After completion, create `.planning/phases/01-correctness/01-02-SUMMARY.md`
</output>
</content>
</invoke>
