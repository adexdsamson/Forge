---
phase: 01-correctness
plan: 03
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/Forge/Forge.tsx
  - src/logic/updateFieldArrayRootError.ts
  - src/utils.ts
  - src/Forger/Forger.tsx
  - src/types.ts
autonomous: true
requirements: [CORR-04, CORR-01, CORR-03]

must_haves:
  truths:
    - "In wizard mode on a non-final step, pressing Enter in a text input (implicit submit) does NOT call onSubmit and does NOT submit partial data — it advances via handleNext instead"
    - "On the LAST wizard step, the next/submit button (and Enter) fires onSubmit with the collected form data, gated by RHF whole-form validation"
    - "When handleWizardSubmit is absent on a hand-built control, the final wizard submit button still submits via control.handleSubmit (no silent no-op onClick=undefined)"
    - "The programmatic ref.onSubmit handle respects the wizard step model: it advances on intermediate steps and submits on the last step, agreeing with the in-tree wizard nav button behavior"
    - "updateFieldArrayRootError produces no leading `undefined` element when the field's error slot is undefined/non-array (compact semantics, not convertToArrayPayload)"
    - "src/utils.ts has no unused FieldErrors import"
    - "Switch/Slider/Picker and transform input/output value parameters are not falsely typed `string` (no masked coercion of boolean/number values)"
  artifacts:
    - path: "src/Forge/Forge.tsx"
      provides: "Wizard-aware onFormSubmit guard on the web <form>; handleWizardSubmit fallback to handleSubmit; wizard-aware imperative handle; memoized safeOnSubmit"
      contains: "onFormSubmit"
    - path: "src/logic/updateFieldArrayRootError.ts"
      provides: "compact-equivalent payload (no phantom leading undefined)"
      contains: "updateFieldArrayRootError"
    - path: "src/utils.ts"
      provides: "Import line without unused FieldErrors"
      contains: "FieldError"
    - path: "src/Forger/Forger.tsx"
      provides: "Non-string-typed event-handler value params"
      contains: "getEventHandlers"
    - path: "src/types.ts"
      provides: "transform.input/output value params widened from string"
      contains: "transform"
  key_links:
    - from: "src/Forge/Forge.tsx"
      to: "handleNext (intermediate step) / control.handleSubmit (last step)"
      via: "onFormSubmit handler wired to <form onSubmit>"
      pattern: "onSubmit=\\{onFormSubmit"
    - from: "src/Forge/Forge.tsx"
      to: "control.handleSubmit (fallback when handleWizardSubmit missing)"
      via: "ternary on control.handleWizardSubmit at last-step wizard button"
      pattern: "handleWizardSubmit"
    - from: "src/logic/updateFieldArrayRootError.ts"
      to: "compact (or falsy filter)"
      via: "replaces convertToArrayPayload on get(errors, name)"
      pattern: "compact|filter"
---

<objective>
Close the verification gaps left after Phase 01 was executed (verification scored 3/4 — one BLOCKER). Phase 01 plans 01-01 and 01-02 shipped the real `<form>` render, the Forger/Slot named errors, the JSDoc fix, and the `updateFieldArrayRootError` dedupe. This gap plan fixes the regressions and quality debt those changes introduced or exposed, restoring CORR-04 correctness and tightening CORR-01/CORR-03.

Purpose:
- Eliminate the BLOCKER (CR-01): the unconditional `<form onSubmit={control.handleSubmit(safeOnSubmit)}>` submits the whole form on Enter/implicit-submit during intermediate wizard steps, bypassing step progression. Introduce a wizard-aware `onFormSubmit` guard.
- Fix the related wizard wiring defects (WR-01 last-step fallback, WR-02 wizard-aware imperative handle + memoized safeOnSubmit).
- Fix the phantom-error payload regression introduced by the dedupe (WR-03).
- Remove the dead `FieldErrors` import exposed by the dedupe (WR-04).
- Remove the correctness-adjacent type lie on event-handler/transform value params (WR-05).

Output: corrected `src/Forge/Forge.tsx`, `src/logic/updateFieldArrayRootError.ts`, `src/utils.ts`, `src/Forger/Forger.tsx`, `src/types.ts`.

Out of scope (do NOT touch): WR-06 (submit-button descendant recursion), IN-02 (`hasFields` `?? false`), IN-03 (commented-out import lines). IN-01 (memoize safeOnSubmit) is folded into Task 1 because the wizard-aware imperative handle depends on it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-correctness/01-CONTEXT.md
@.planning/phases/01-correctness/01-VERIFICATION.md
@.planning/phases/01-correctness/01-REVIEW.md

<interfaces>
<!-- Contracts the executor needs. Already in the codebase as of 2026-05-31 — verified live, do not re-derive. -->

From src/Forge/Forge.tsx — current state (the file with the BLOCKER):
- Line 40: `const safeOnSubmit = onSubmit ?? (() => {});` — new identity every render (IN-01).
- Lines 54-63: wizard members destructured from `control`: `isWizard, currentStep = 0, totalSteps = 0, isFirstStep = true, isLastStep = true, handleNext, handlePrevious, handleWizardSubmit`.
- Line 121 (inside processChildrenRecursively, wizardNav === "next" + last step): `onClick = control.handleWizardSubmit?.(safeOnSubmit);` — optional-chained, yields `undefined` when handler absent (WR-01).
- Lines 236-248: `useImperativeHandle(ref, () => ({ onSubmit: () => { control.handleSubmit(safeOnSubmit)(); }, currentStep, totalSteps }), [safeOnSubmit, control, currentStep, totalSteps])` — ignores wizard path (WR-02).
- Lines 286-289: `<form className={className} noValidate={noValidate} onSubmit={control.handleSubmit(safeOnSubmit)}>` — UNCONDITIONAL, no wizard guard (CR-01 BLOCKER). The native branch (lines 276-281) is a Fragment with no submit element and is correct as-is.
- `useImperativeHandle` is imported from "react" (line 7); `useCallback` is NOT yet imported.

From src/logic/updateFieldArrayRootError.ts — current canonical copy (WR-03):
```
import { FieldError, FieldErrors, FieldValues, InternalFieldName } from "react-hook-form";
import { convertToArrayPayload, get, set } from "../utils";
export default <T extends FieldValues = FieldValues>(errors, error, name): FieldErrors<T> => {
  const fieldArrayErrors = convertToArrayPayload(get(errors, name));  // <- yields [undefined] when get() is undefined
  set(fieldArrayErrors, 'root', error[name]);
  set(errors, name, fieldArrayErrors);
  return errors;
};
```
The pre-dedupe utils.ts version used `compact(get(errors, name))` — `compact(undefined) → []`, no phantom element. `compact` is exported from src/utils.ts and is still in use (lines 67/110/238).

From src/utils.ts line 17 — `FieldErrors` is imported alongside `FieldError` but is now unused after the dedupe (only `FieldError` referenced, ~line 316). The import block spans lines 7-22; `FieldError` (line 18) must stay.

From src/Forger/Forger.tsx lines 42-64 — `getEventHandlers()` types every handler value param as `string`:
- `handlers[handler] = (value: string) => onChange(getTextTransform(value));`
- `handlers.onChangeText = (value: string) => ...` (RN TextInput)
- `handlers.onValueChange = (value: string) => ...` (RN Switch/Slider/Picker — boolean/number values, the type lie)
- web `handlers.onChange = (value: string) => ...`
- `getTextTransform = (text: string) => ...` and `getTransformedValue = (text: string) => ...` (lines 33-39) are also `string`-typed.

From src/types.ts lines 60-63 — the transform contract:
```
transform?: {
  input?: (value: string) => string;
  output?: (val: string) => string;
};
```
This is the public `ForgerControllerProps` transform shape (WR-05).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix wizard submit correctness in Forge.tsx — onFormSubmit guard (CR-01), handleWizardSubmit fallback (WR-01), wizard-aware imperative handle + memoized safeOnSubmit (WR-02/IN-01)</name>
  <read_first>
    - src/Forge/Forge.tsx (full file — line 40 safeOnSubmit, lines 54-63 wizard destructure, line 121 last-step onClick, lines 236-248 useImperativeHandle, lines 286-293 web <form>; line 7 react import for useImperativeHandle)
    - .planning/phases/01-correctness/01-VERIFICATION.md (the gaps block + Gap CR-01 section — primary source)
    - .planning/phases/01-correctness/01-REVIEW.md (CR-01, WR-01, WR-02, IN-01 — full fix descriptions)
    - .planning/phases/01-correctness/01-CONTEXT.md (D-04, D-10 — wizard last-step submit, deferred: NO per-step validation, NO error-step routing)
  </read_first>
  <action>
    All edits are in src/Forge/Forge.tsx. Honor the cross-platform constraint: the native branch (Fragment) has no `<form>` and must stay unchanged; all guards below apply only to the web `<form>` and the shared wizard/imperative logic. Do NOT add a react-native import. Do NOT introduce per-step validation or error-step auto-jump (both deferred per D-10).

    1. CR-01 (BLOCKER) — wizard-aware form submit guard:
       - Define a local handler `onFormSubmit` (a `(e: React.FormEvent) => void` / `unknown`) before the return. When `isWizard && !isLastStep`: call `e.preventDefault()`, call `handleNext?.()`, and return early (do NOT call safeOnSubmit). Otherwise delegate to `control.handleSubmit(safeOnSubmit)(e)`.
       - Change the web `<form>` element (currently `onSubmit={control.handleSubmit(safeOnSubmit)}` at line 289) to `onSubmit={onFormSubmit}`. Keep `className={className}` and `noValidate={noValidate}` exactly as they are. The native Fragment branch stays unchanged.
       - Rationale to preserve: this stops Enter / implicit submit on an intermediate wizard step from firing safeOnSubmit and submitting partial data; intermediate-step Enter now advances the wizard instead.

    2. WR-01 — last-step submit fallback:
       - At the wizard-nav "next" + last-step branch (line 121, currently `onClick = control.handleWizardSubmit?.(safeOnSubmit)`), replace the optional-chained call with a ternary fallback: when `control.handleWizardSubmit` exists, use `control.handleWizardSubmit(safeOnSubmit)`; otherwise fall back to `control.handleSubmit(safeOnSubmit)`. This prevents `onClick={undefined}` (a silent dead submit button) when a hand-built ForgeControl lacks `handleWizardSubmit`.

    3. WR-02 / IN-01 — wizard-aware imperative handle + memoized safeOnSubmit:
       - Memoize `safeOnSubmit` with `useCallback` keyed on `[onSubmit]` (add `useCallback` to the existing `react` import at line 7) so its identity is stable and the imperative-handle dependency array does not churn every render.
       - Make the `useImperativeHandle` `onSubmit` handle (lines 236-248) wizard-aware so programmatic submit agrees with the in-tree wizard nav: when `isWizard && !isLastStep`, call `handleNext?.()` and return; on the last step (or non-wizard), invoke the validated submit — prefer `control.handleWizardSubmit ? control.handleWizardSubmit(safeOnSubmit)() : control.handleSubmit(safeOnSubmit)()` so it mirrors the WR-01 fallback. Update the dependency array to include the wizard members it now reads (`isWizard`, `isLastStep`, `handleNext`, `control`, `safeOnSubmit`, `currentStep`, `totalSteps`).
       - Do NOT add `as any` casts to the public surface; the wizard members are already typed on ForgeControl.
  </action>
  <verify>
    <automated>npx tsc --noEmit; npx rollup -c</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/Forge/Forge.tsx defines an `onFormSubmit` handler that, on `isWizard && !isLastStep`, calls `e.preventDefault()` and `handleNext` and returns before any `safeOnSubmit`/`handleSubmit` call; otherwise delegates to `control.handleSubmit(safeOnSubmit)(e)`.
    - Source assertion: the web `<form>` element uses `onSubmit={onFormSubmit}` (NOT `onSubmit={control.handleSubmit(safeOnSubmit)}`); `className` and `noValidate` attributes are unchanged.
    - Source assertion: the last-step wizard "next" button onClick is a ternary that uses `control.handleWizardSubmit(safeOnSubmit)` when present and `control.handleSubmit(safeOnSubmit)` as fallback — no bare optional-chained `control.handleWizardSubmit?.(...)` that can resolve to undefined.
    - Source assertion: `safeOnSubmit` is wrapped in `useCallback` with `[onSubmit]` deps; `useCallback` is imported from "react".
    - Source assertion: the `useImperativeHandle` `onSubmit` handle branches on `isWizard && !isLastStep` (advances via handleNext) vs final/non-wizard (submits via the handleWizardSubmit-or-handleSubmit path); its dependency array includes the wizard members it reads.
    - Source assertion: no `react-native` import is added anywhere in the file; the native Fragment branch is unchanged.
    - Behavior assertion (manually verifiable, no test runner exists): in a multi-step wizard with a text input on a non-final step, simulating Enter triggers `onFormSubmit`, which calls `e.preventDefault()` + `handleNext` and does NOT call onSubmit — `currentStep` does not jump to a submitted state and partial data is not submitted. On the LAST step, clicking the next/submit button (or Enter) fires onSubmit with the collected data, gated by RHF whole-form validation. A hand-built control without `handleWizardSubmit` still submits on its last step (no dead button). The programmatic `ref.onSubmit()` advances on intermediate steps and submits on the last step.
    - Build command: `npx tsc --noEmit` exits 0 AND `npx rollup -c` builds CJS + ESM + dts without errors.
  </acceptance_criteria>
  <done>onFormSubmit guards intermediate-step implicit submit (CR-01 resolved); last-step button falls back to handleSubmit when handleWizardSubmit absent (WR-01); imperative handle is wizard-aware and safeOnSubmit is memoized (WR-02/IN-01); native Fragment unchanged; no react-native import; tsc + rollup clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix phantom field-array error (WR-03), remove dead FieldErrors import (WR-04), widen event-handler/transform value types (WR-05)</name>
  <read_first>
    - src/logic/updateFieldArrayRootError.ts (full file — line 9 convertToArrayPayload; line 2 imports compact-capable utils)
    - src/utils.ts (import block lines 7-22 — FieldError at :18 stays, FieldErrors at :17 to remove; confirm `compact` is exported and used at :67/:110/:238 and must NOT be removed; confirm FieldError still referenced ~:316)
    - src/Forger/Forger.tsx (lines 33-64 — getTextTransform/getTransformedValue + getEventHandlers value param types; do NOT touch MemorizeController comparator :82-110)
    - src/types.ts (lines 60-63 — transform.input/output contract in ForgerControllerProps)
    - .planning/phases/01-correctness/01-REVIEW.md (WR-03, WR-04, WR-05 — full fix descriptions)
    - .planning/phases/01-correctness/01-VERIFICATION.md (WR-04 dead-import note, line 118)
  </read_first>
  <action>
    1. WR-03 — restore compact semantics in src/logic/updateFieldArrayRootError.ts:
       - Replace `convertToArrayPayload(get(errors, name))` (line 9) with `compact`-equivalent semantics so an undefined/non-array error slot yields `[]` rather than `[undefined]`. Preferred approach: import `compact` from `../utils` (it is already exported there) and use `compact(get(errors, name))`, matching the pre-dedupe behavior. If `compact` does not coerce a non-array scalar the way `convertToArrayPayload` did, instead wrap-then-filter: build the array via `convertToArrayPayload(get(errors, name))` and then filter out falsy entries before `set`. Either way the result MUST NOT contain a leading `undefined` element when the source error slot is undefined.
       - Keep the rest of the function identical: `set(fieldArrayErrors, 'root', error[name])`, `set(errors, name, fieldArrayErrors)`, `return errors`. Keep it the default export. Do NOT reintroduce a duplicate in utils.ts (D-09 keeps logic/ canonical).
       - Remove the now-unused `convertToArrayPayload` import from this file ONLY if it is no longer referenced after the change (avoid creating a new unused import — that would re-trigger a WR-04-style finding).

    2. WR-04 — remove dead import in src/utils.ts:
       - Remove `FieldErrors` from the react-hook-form named-import block at line 17. Keep `FieldError` (line 18) and every other imported name. Do not reorder or remove anything else.

    3. WR-05 — widen event-handler/transform value types:
       - In src/Forger/Forger.tsx `getEventHandlers()` (lines 42-64): change the value parameter of the handlers that receive non-string values away from `string`. At minimum, type the Switch/Slider/Picker `onValueChange` handler (line 54) and the generic/web `onChange` handlers (lines 57, 60) and the custom `handler` branch (line 46) so a boolean/number value is not silently coerced under a false `string` annotation — use `unknown` (and let getTextTransform accept the widened type) or `any` for these handler params per the review's minimum bar. The fix must remain type-safe under strict mode (narrow inside if you choose `unknown`).
       - Update `getTextTransform` / `getTransformedValue` (lines 33-39) param types to accept the widened value type consistently with the handlers (do not leave a `string`-only signature that the widened handlers cannot call).
       - In src/types.ts `transform` (lines 60-63): widen `input`/`output` value params from `string` so the contract no longer lies for boolean/number-producing inputs. Keep the change minimal — widen the input param type (e.g. to `unknown` or a generic) without breaking existing string consumers more than necessary; the return type may stay as-is if it does not force a cast at the call sites. Do NOT regress the MemorizeController comparator or any other Forger logic.
  </action>
  <verify>
    <automated>npx tsc --noEmit; npx rollup -c</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/logic/updateFieldArrayRootError.ts no longer builds the array with a bare `convertToArrayPayload(get(errors, name))` that can yield `[undefined]` — it uses `compact` (or a falsy-filter) so an undefined/non-array error slot produces no leading `undefined` element. The function remains the default export and still calls `set(..., 'root', error[name])` then `set(errors, name, ...)` then returns `errors`.
    - Source assertion: src/logic/updateFieldArrayRootError.ts has no unused import left behind (if `convertToArrayPayload` is dropped, it is removed from the import line; if `compact` is added, it is imported from `../utils`).
    - Source assertion: src/utils.ts import block (lines 7-22) no longer contains `FieldErrors`; `FieldError` and all other names remain; `compact` is still exported and still used at its existing call sites.
    - Source assertion: src/Forger/Forger.tsx `getEventHandlers` Switch/Slider/Picker `onValueChange` and the web/generic `onChange` (and custom `handler`) value params are NOT typed `string`; getTextTransform/getTransformedValue accept the widened type.
    - Source assertion: src/types.ts `transform.input`/`transform.output` value param is widened from `string`.
    - Behavior assertion (manually verifiable, no test runner exists): WR-03 — when a field-array form's error slot for `name` is undefined and `updateFieldArrayRootError` runs (append then remove an item with a root-level array error set), the resulting `errors[name]` does NOT contain a phantom leading `undefined` element at index 0; the root error is attached correctly with no crash. WR-05 — a boolean Switch value flowing through onValueChange is no longer silently typed as `string` (the false annotation that masks coercion bugs is gone).
    - Build command: `npx tsc --noEmit` exits 0 AND `npx rollup -c` builds CJS + ESM + dts without errors.
  </acceptance_criteria>
  <done>updateFieldArrayRootError uses compact-equivalent semantics (no phantom leading undefined, WR-03); FieldErrors removed from utils.ts import with FieldError and compact intact (WR-04); event-handler + transform value params widened off the false `string` type in Forger.tsx and types.ts (WR-05); MemorizeController untouched; tsc + rollup clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer JSX → Forge | App-supplied children, onSubmit callback, wizard step props, and field values cross into the tree-walker and the new onFormSubmit guard (untrusted shape, but same-origin app code, not network input) |
| browser DOM → form submit | The web `<form>` element's native submit (Enter / type=submit) is now intercepted by `onFormSubmit` before reaching the consumer's onSubmit on intermediate wizard steps |

Forge is a headless client-side form library: no network calls, no auth, no persistence, no secret handling. This gap plan only changes client-side submit-routing behavior, an in-memory error-shaping helper, an unused import, and TypeScript annotations.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | onFormSubmit intercept on the web `<form>` | mitigate | On intermediate wizard steps onFormSubmit calls `e.preventDefault()` and `handleNext()` and returns before invoking the consumer's onSubmit, preventing premature submission of partial form data (the CR-01 BLOCKER). On the last step (or non-wizard) it delegates to `control.handleSubmit`, whose internal `event.preventDefault()` still prevents native navigation/page reload. |
| T-01-07 | Information Disclosure | wizard-aware imperative handle + last-step fallback | accept | Form data is passed only to the consumer's own onSubmit callback; no field values are logged, serialized into errors, or sent anywhere. The handleWizardSubmit→handleSubmit fallback and the wizard-aware ref handle route the same already-trusted data through RHF's validated submit. No new disclosure vector. |
| T-01-08 | Tampering | updateFieldArrayRootError payload shape (compact restore) | accept | Pure in-memory error-shaping over RHF's error object; restoring compact semantics removes a stray `[undefined]` element rather than adding any external surface. No trust boundary crossed. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes under TS 5 strict (no new `as any` on the public surface).
- `npx rollup -c` builds CJS + ESM + dts cleanly.
- Source assertions per task acceptance criteria confirm: onFormSubmit guard + `onSubmit={onFormSubmit}`, handleWizardSubmit fallback, wizard-aware imperative handle, memoized safeOnSubmit, compact-restored updateFieldArrayRootError, removed FieldErrors import, widened handler/transform value types.
- No `react-native` import is introduced; native Fragment branch is untouched.
- No new `control._*` private-API usage is introduced (all fixes use public RHF APIs already in the file).
- Behavioral assertions (no test runner exists in this repo): intermediate-step Enter does not submit; last-step submits; missing-handler control still submits on last step; field-array error slot has no phantom leading undefined.
</verification>

<success_criteria>
- CR-01 closed: in wizard mode, implicit/Enter submit on a non-final step advances the wizard (no premature onSubmit / partial submit); the last step submits with collected data gated by RHF validation (CORR-04 restored).
- WR-01 closed: the final wizard submit button never resolves to `onClick={undefined}` — it falls back to `control.handleSubmit` when `handleWizardSubmit` is absent.
- WR-02/IN-01 closed: programmatic `ref.onSubmit` agrees with the wizard nav button behavior, and `safeOnSubmit` is memoized.
- WR-03 closed: `updateFieldArrayRootError` no longer leaks a phantom leading `undefined` element.
- WR-04 closed: `src/utils.ts` has no unused `FieldErrors` import.
- WR-05 closed: event-handler and transform value params are no longer falsely typed `string`.
- `npx tsc --noEmit` and `npx rollup -c` both pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-correctness/01-03-SUMMARY.md`
</output>
